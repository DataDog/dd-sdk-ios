# Multi-scene experiments EXP-143 through EXP-199

This is the active detailed-record shard. Append one `## EXP-NNN` section per
experiment and add one compact row to [the experiment index](../EXPERIMENTS.md).
Keep exact commands, all attempts, run IDs, RUM session IDs, view UUIDs, payload
and backend observations, artifact locations, validation, and the bounded
conclusion here. Before designing a retry, search
[rejected approaches](../REJECTED_APPROACHES.md).

Historical records through `EXP-142` are frozen in
[the archive](../Archive/EXPERIMENTS_THROUGH_EXP-142.md).

## EXP-143 — External semantic-router mutations and restoration

Status: PASS after FAIL, rejected candidates, and tooling-invalid retries ·
backend. Repeated-path restoration, external replacement, rejected writes, and
canonicalized writes are accepted with pre-view lifecycle attribution.

### Question

Does the experimental iOS 27 semantic `RUMNavigationStack` preserve committed
occurrence semantics when the application's router, rather than a destination
link, replaces or rejects the path, and when the stack restores directly into
repeated equal values?

### Required cases

- programmatic same-type and different-type replacement;
- a router that rejects a proposed path write;
- a router that canonicalizes a proposed write;
- restoration to one route and directly to two equal routes;
- lifecycle work attributed only after the accepted destination occurrence is
  current; and
- no change to native `NavigationLink(value: Route)` matching.

### Acceptance

The application binding remains authoritative. Rejected proposals create no RUM
transition; canonicalized proposals create only the accepted path. Every
materialized repeated path position has a distinct RUM UUID, and a revealed
destination starts fresh before its first lifecycle callback work. Mapper and
backend view/action/Resource inventories must agree, with no automatic duplicate,
error, or crash.

### Preconditions and implementation revision

- Xcode 27.0 (`27A266a`), iPadOS 27 simulator
  `B4E4F039-CA5D-4D04-A904-1D71099BE651`.
- Each attempt proved terminate/uninstall/missing-container before install.
- Native scene `33F68994-4513-4100-B294-A1F5A5A2EE23` was stable through the
  accepted final run.
- The scenario starts the customer path at `[detail-1, detail-1]`, then commits
  `[detail-1]` and `[]`. Its required RUM sequence is D1 -> fresh D2 -> first
  Home, with no hidden Home or automatic view.
- Accepted implementation: `4d64d8a53` (`Harden restored semantic navigation
  ownership`). Accepted probe oracle: `484de074c` (`Validate restored
  destination lifecycle ownership`). Both commits are signed.
- External-router scenarios and the terminal oracle were added in `312423e9c`
  (`Add semantic router mutation probes`). Dormant-boundary canonicalization was
  hardened in `bdbfc1d5c` (`Handle canonicalized semantic navigation`).
- The final timing candidate, `a458ff6c2` (`Start canonical
  destinations before lifecycle work`), treats the iOS 27 environment trait as
  an early timing signal only after the same boundary has concrete reader proof
  for that scene. It cannot migrate or recover a disconnected boundary.

### Attempts

1. `semantic-initial-repeat-20260915-baseline-a`, session
   `1595e43d-60a3-4953-ac21-cdc1c495a24e`: **FAIL**. The released draft
   published hidden Home H1 before initial top D1. The first pop correctly
   created D2 and the second created H2, but the scenario could not observe a
   first Home after its boundary because H1 had consumed occurrence 1.
2. `semantic-initial-repeat-20260915-fix-a`, session
   `6f5acd00-7718-43f7-820f-97787fbd43d6`: **FAIL**. Gating non-current
   semantic configurations removed hidden Home and every automatic duplicate,
   but the first pop stopped D1 without starting D2. The sole initially
   materialized equal-route boundary retained its removed depth-2 occurrence
   key instead of adopting the surviving depth-1 position.
3. A state-only unit draft constructed two simultaneous materialized claims for
   the restored equal values. It failed because both claims selected the last
   equal position. This construction is **INVALID** as iOS 27 materialized only
   the top destination in both device runs; it was replaced with coverage for
   the one platform-proven materialized claim.
4. The accepted candidate retains each claim while its position survives, but
   rebases a claim when a committed contraction removes that position and the
   same route becomes the new top. Focused state validation passes 4/4,
   including normal sequential equal push/pop and restored-position rebasing.
   Clean device run `semantic-initial-repeat-20260915-fix-b`, session
   `0fad0ffc-6c2d-4614-8a36-cbf03a375dae`, passes all 14 local expectations and
   backend intake. Implementation commit: `f885f9da4`.
5. `semantic-initial-repeat-20260915-fix-c`, session
   `26d85c11-67fb-47ab-88e3-b76fdcf85007`, was a **runtime PASS but rejected
   implementation candidate**. It moved initial ownership ahead of hidden-root
   lifecycle work and passed 20/20 with the correct four views and 43 backend
   events. Source review then found deterministic bootstrap and teardown races:
   the donor generation fence was not total, a same-scene replacement state
   could not adopt the live occurrence, reconnect before queued cleanup could
   swallow a fresh reader, and a donor scene move before descriptor
   reconciliation could strand ownership. Runtime success did not override
   those failures.
6. `semantic-initial-repeat-20260915-fix-d`, session
   `b0944898-77ca-4129-adcd-ad9489285929`, was also a **runtime PASS but rejected
   implementation candidate**. It passed 20/20 and backend intake with 43 events,
   yet successive adversarial callback-order tests found five remaining families:
   a disconnected dormant replacement could not rearm; a previously used donor
   could not regain ownership in the same or another scene; a newer accepted
   generation with its descriptor pending could fence the last proven
   destination; combining donor reuse with disconnect reproduced the strand in
   either reconnect order; and an inactive donor whose local value advanced to
   the newer boundary could no longer reclaim ownership.
7. The final candidate separates the source's validated donor-boundary metadata
   from the state that owns the last proven visible destination. Reader-qualified
   replacement states are preflighted before a live owner is stopped; exact live
   identity is transferred in the same scene, while disconnect or scene migration
   starts one fresh occurrence. Non-reader and stale-generation callbacks cannot
   take those paths. Forty-nine source-order tests cover accepted identity before
   descriptor, multiple and out-of-order donor refreshes, detach/disappear,
   replacement state reuse, queued teardown, both reconnect orders, and scene
   migration. Static adversarial review returned **PASS**. Clean run
   `semantic-initial-repeat-20260915-fix-e-final`, session
   `9a7169ea-d582-4801-82ef-1a73bba6dfb3`, then passed 20/20 locally and in
   backend intake on the exact committed source.
8. `semantic-router-external-20260915-a`, session
   `9b2bfd97-dfd9-48e3-bb6f-81728359d13c`: **PASS**, 31/31. Starting from Home,
   external same-type replacement created D1 then fresh D2; different-type
   replacement then created Alternate. There was no intermediate Home, stale
   Detail, or automatic duplicate.
9. `semantic-router-rejected-20260915-a`, session
   `460ab48c-5417-40b6-9802-f6395b7f41f2`: **PASS**, 11/11. SwiftUI temporarily
   materialized the rejected Detail source and ran its lifecycle work, but the
   authoritative binding stayed at Home. No Detail RUM view was created and the
   speculative work stayed on the current Home view.
10. `semantic-router-canonicalized-20260915-a`, session
    `6ad69cc8-e7d6-43d5-8b9b-a1c36338a57f`: **FAIL**. The router accepted a
    proposed Detail as Alternate and the UI showed Alternate, but RUM never
    created Alternate. All accepted-destination lifecycle work stayed on Home.
11. `semantic-router-canonicalized-20260915-fix-a`, session
    `93ab4ae1-737b-4c20-bde5-2e013a0a4268`: **SDK timing FAIL; terminal attempt
    INVALID**. The `bdbfc1d5c` candidate created only accepted Alternate, but it
    started about 31 ms after Alternate `onAppear`; `onAppear` and immediate-task
    action/Resource pairs therefore stayed on Home. The scenario also installed
    a redundant `waitForSignal` after its delayed-task marker had already been
    observed, so the terminal oracle timed out despite the later evidence. The
    SDK timing result is independently conclusive; the timeout is not.
12. `semantic-router-canonicalized-20260915-trait-a`: **INVALID tooling
    attempt**. The short-lived Xcode interaction session expired while the
    required device-interaction reference was being loaded. No interaction was
    performed and no SDK conclusion was drawn.
13. `semantic-router-canonicalized-20260915-trait-b`, session
    `3b021a8e-5e05-4729-abce-3219fc66b357`: **PASS**, 19/19. The router again
    canonicalized Detail to Alternate. Alternate became the current RUM view
    before its `onAppear` and immediate task, speculative Detail remained
    uncommitted, and the app stayed alive with zero oracle issues.

### Local and mapper evidence

Baseline exact view order:

- launch `a9c18a1b-f512-4185-a320-fc6d13e303e5`;
- hidden H1 `91326692-dd53-46ec-833e-3b46fe5b6037`;
- initial D1 `9d92234f-2f98-4c6d-a324-e86d436c2429`;
- first-pop D2 `e9fdbe64-53a2-4827-b8be-bcff9b0acdc4`; and
- second-pop H2 `baf0559f-0b7e-465e-9e3c-5d83d66af126`.

`fix-a` exact view order was launch
`253c5556-800c-4ebc-9154-f61e8082d1ab`, then D1
`e53a2ca0-3f35-4430-ad16-3743d5934f28`; no D2, Home, or automatic view was
started. Initial hidden-Home lifecycle work used ApplicationLaunch. Initial D1
work used D1. After the first pop, binding, appearance, `onAppear`, task, and
Resource signals all incorrectly remained on D1 before it stopped without a
replacement. Both runs remained alive, returned two HTTP 202 uploads, and had no
Datadog SDK error, warning, fatal, exception, or crash evidence.

`fix-b` emitted launch `b84ffbcb-6ea5-4fd4-a7f5-036c19ed84d4`, initial D1
`c692a91d-adf5-4791-ba03-4712414ec9d8`, fresh revealed D2
`5f9d4525-b026-475d-87c5-9e29f1a7f1e3`, and first Home H1
`d73aaf1d-8aac-4da8-bf36-55897df40474`. D1 stopped before D2 started. D2 stopped
before the Home view became current, although D2's final inactive mapper
document serialized later when its retained Resource completed. Each Detail
owned six actions and six Resources; Home owned two of each. There was no hidden
Home view, lower hidden Detail view, or automatic duplicate. Two uploads returned
HTTP 202 and the app remained on Home without SDK/crash evidence.

`fix-c` closed that attribution symptom: launch
`fdcdcdaa-fc36-489b-8377-22114ecc7cb4`, D1
`a98d9b03-8d61-4965-95fd-507131aad2b5`, D2
`ce0cff20-fb7b-4f38-ae34-de99006b950a`, and H1
`4a792bc2-4319-47c2-98a1-f0ac3e298d03`; D1 owned nine actions and nine
Resources. It remained rejected because the deterministic races in attempt 5
could lose that ownership outside the happy path.

`fix-d` emitted launch `c2ef17c3-b15b-41da-91dd-868ec6cdb36e`, D1
`90a8d78e-6c0f-470c-b6eb-710d5aa0eccd`, D2
`7e5be3b8-042c-483b-970a-bb2fb1c5b58c`, and H1
`c6f3715f-e302-4ad4-91d6-a48b7e844c3f`. All hidden Home lifecycle pairs used
D1, but attempt 6's combined callback orders still rejected the implementation.

The accepted `fix-e-final` mapper sequence is:

- ApplicationLaunch `b3b8a32d-23b3-421a-b4ee-3ac15889a347`, start sequence 2
  and inactive sequence 3;
- initial visible D1 `d6b578a5-e061-4d10-ab91-3ca918e13d68`, start sequence 4
  and inactive sequence 56;
- fresh revealed D2 `847a403c-c088-4388-9985-cd27e2750652`, start sequence 57;
  and
- first Home H1 `6fbff07d-660f-4975-9923-e682c1eeded8`, start sequence 90.

D2's final inactive document serialized at sequence 104 only because its
deliberately delayed action/Resource pair remained attached to D2 until
completion. That is correct aggregate timing, not a reordered semantic
transition. The run produced no hidden Home, lower Detail, or automatic-origin
view signal.

The hidden Home callbacks all used visible D1:

| Callback | Action event | Resource event |
| --- | --- | --- |
| navigation appearance | `c310e9d2-d439-4b8f-9f25-6328a33b99c0` | `9dd487ce-7984-4edf-9c17-4c7333b828c5` |
| `onAppear` | `28c51b7e-28bd-420f-921f-2a72137f9147` | `9c99ceca-dc0e-443f-b46d-93d4f00375dc` |
| immediate task | `715045ec-1f16-4f35-ae36-1f768cfec187` | `8804350a-a1a6-44c2-90f6-257b42dd516a` |

The delayed D2 pair is action `1f02219a-c4b7-4d76-b104-17f943badeda` and
Resource `45a8032a-8139-4110-9ab6-e85683969211`. The revealed D2 marker is
`a56289fe-20c2-4f07-bd57-3995a129f96b` / `c8122adb-b5c6-4e7f-ba74-0fe1f830fbed`;
the restored H1 marker is `7d7b04ad-661c-412e-8b25-e06eef2a8ab1` /
`9bb63257-d6fd-43b1-abcd-3f015c127aea`.

#### External-router cases

The accepted replacement run emitted ApplicationLaunch
`cc9f1378-9da3-4a9c-9ed3-4c253892c9f1`, Home
`1dc4e311-3947-459e-8258-60ca6f62a022`, D1
`e8361a81-8024-4de4-9c03-d4aa280637f4`, fresh D2
`c9085505-1bba-497c-b130-89355e3f2bcb`, and Alternate
`10306e91-f8ea-437e-a97b-d25ac1d79ef2`. Every screen's lifecycle pairs used its
exact UUID.

The rejected run emitted only ApplicationLaunch
`1708f379-c9a7-4af4-b350-9f698c6540a1` and Home
`127e88b4-a56c-48ec-8023-e5cb815930f2`. Its speculative Detail callbacks used
Home and created no hidden or automatic view.

The canonicalization baseline emitted only ApplicationLaunch
`5388ed8c-3862-4238-a7ae-dcf8d30c9760` and Home
`d862d0c4-942c-435c-9ba2-dad1ffc74c99` although the UI settled on Alternate.
The first implementation candidate added Alternate
`958395c1-e849-4549-a4c5-4e2ffea658ed`, but early Alternate work remained on
Home `8b7fb8bd-1913-486e-8daa-dd742eb0638e`; only the delayed pair used
Alternate.

The accepted timing run emitted ApplicationLaunch
`f8339f0e-0953-4c5f-a3cd-5ae664cf186d`, Home
`97526062-6102-4bba-a6c3-117a27eafed2`, and Alternate
`5511a022-d226-40a1-9814-4d0ef8325d2a`. Alternate began at mapper sequence 50,
then owned:

| Callback | Action event | Resource event |
| --- | --- | --- |
| `onAppear` | `838946bb-df24-43b4-ab57-3a01a2dcf6e1` | `87104497-88d1-4104-b269-3c652ad5762d` |
| immediate task | `9daa1c51-7225-49b7-b6ea-0fac8ba428fb` | `c3257230-667d-48ac-8560-6e19c43857ca` |
| delayed task | `a657e272-b1df-47ef-be1f-80ce7542244c` | `80456e16-d211-42b1-bce1-67444ce7639f` |
| accepted marker | `e315006c-34dd-450a-bfee-8b1efcd1256a` | `772825a3-46fe-4cdd-8f42-bc8c84354f56` |

SwiftUI still ran binding-update, appearance, `onAppear`, and immediate-task
work for the speculative Detail source. That work correctly remained on Home
because Detail never became the scene's committed destination. There was no
Detail RUM view, automatic duplicate, SDK warning/error, assertion, or crash.

### Backend evidence

The final exact run-ID query returned 46 indexed events for application `Valentin
test app`, service `ios-sdk-native-multi-scene-probe`, session
`9a7169ea-d582-4801-82ef-1a73bba6dfb3`. An independent session-ID view query
returned exactly four views: ApplicationLaunch plus D1, D2, and H1 with the same
UUIDs as the mapper. Grouped intake counts are:

| Owner | Actions | Resources | Long tasks | Vitals | Views |
| --- | ---: | ---: | ---: | ---: | ---: |
| ApplicationLaunch | 0 | 0 | 0 | 0 | 1 |
| D1 | 9 | 9 | 2 | 1 | 1 |
| D2 | 6 | 6 | 2 | 0 | 1 |
| H1 | 2 | 2 | 2 | 0 | 1 |

The session reports 17 actions, 17 Resources, six long tasks, four views, one
session event, one vital, zero errors, and zero crashes. Local uploads returned
HTTP 202 with request IDs `F7B8ECF9-EC8D-4078-8BD2-13DB6DF86194` and
`4EB92DD0-387F-4A77-9324-1452F597AC29`. There was no Datadog warning, failure,
fatal signal, assertion, sanitizer finding, or crash evidence.

External-router backend intake independently matched every mapper UUID. Compact
session inventories are:

| Case | Session | Views excluding launch | Actions | Resources | Long tasks |
| --- | --- | --- | ---: | ---: | ---: |
| replacement | `9b2bfd97-dfd9-48e3-bb6f-81728359d13c` | Home, D1, D2, Alternate | 17 | 17 | 4 |
| rejected | `460ab48c-5417-40b6-9802-f6395b7f41f2` | Home | 11 | 10 | 1 |
| canonical baseline | `6ad69cc8-e7d6-43d5-8b9b-a1c36338a57f` | Home | 13 | 12 | 1 |
| late canonical fix | `93ab4ae1-737b-4c20-bde5-2e013a0a4268` | Home, Alternate | 13 | 12 | 4 |
| accepted canonical fix | `3b021a8e-5e05-4729-abce-3219fc66b357` | Home, Alternate | 14 | 13 | 1 |

The accepted canonical session contains 33 indexed events: exactly three views
including ApplicationLaunch, 14 actions, 13 Resources, one long task, one vital,
and one session event. Alternate owns four actions and four Resources; Home owns
the speculative source work. There are zero error and crash events.

### Validation and artifacts

- Baseline final log, hierarchy, and screenshot prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Initial Repeated Baseline-13_12_02_859`.
- `fix-a` final log, hierarchy, and screenshot prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Initial Repeated Fix-13_22_53_436`.
- Accepted `fix-b` final log, hierarchy, and screenshot prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Initial Repeated Fix B-13_33_34_230`.
- Rejected `fix-c` final artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Initial Repeated Fix C-14_13_38_223`.
- Rejected `fix-d` final artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Initial Repeated Fix D-15_24_31_340`.
- Accepted `fix-e-final` logs, hierarchy, screenshot, and thumbnail prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Initial Repeated Fix E Final-16_44_41_845`.
- External-replacement final artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Router External-17_33_21_803`.
- Rejected-write final artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Router Rejected-17_37_04_440`.
- Canonicalization baseline final artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Router Canonicalized-17_43_13_950`.
- Late-start canonicalization final artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Router Canonicalized Fix-19_34_53_938`.
- Accepted trait-timed canonicalization pre/post artifact prefixes:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Canonicalization Trait Retry B-20_43_46_695`
  and
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Canonicalization Trait Retry B-20_44_04_030`.
- Source-managed occurrence tests: 49/49 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/E3BD5917-2C9D-4BB5-A285-279FA71784B4.txt`.
- Complete semantic navigation cluster: 100/100 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/6B39F6D2-DC5F-4CCA-8810-7FAFB7F8EDB6.txt`.
- Final trait-authority focused tests: 4/4 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/2A6971E6-950C-430B-BD34-997B6B532E10.txt`.
- Final semantic state/source/arbiter cluster: 153/153 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/C90B1EF2-CD26-44F6-A1D8-BEFFBE094EF2.txt`.
- Complete probe: 136/136 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/9D65230D-6050-4B1F-AEAA-2636F49C72BE.txt`.
- Final complete probe: 137/137 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/EC5DE1F5-79DA-4D78-AE55-713B48EAFDB1.txt`.
- Complete DatadogRUM: 1,219/1,219 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/7F26CE9D-D3E7-4424-BD0E-8F20B3FD17AB.txt`.
  The preceding full run had one unrelated timeseries scheduling failure at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/E5DA410F-EAF4-4BD2-9841-D655111C5F09.txt`; that exact test passed alone at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/5DB9FC95-89BF-439D-8750-32D98E806D73.txt`
  before the clean full rerun.
- Final complete DatadogRUM: 1,249/1,249 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/3616DCA7-76D8-4147-8135-53D0EFAAE2C4.txt`.
- `./tools/lint/run-linter.sh`: zero violations across 713 source and 699 test
  files.
- Xcode 27.0 (`27A266a`) Release build for `RUMNativeMultiSceneProbe`: pass for
  the generic iOS 27 simulator destination.

### Conclusion and next boundary

iOS 27 restoration is lazy: the root and current top exist initially, while a
lower equal destination does not. A non-current boundary must lend concrete scene
proof without starting a RUM view, while the source owns the last proven visible
destination until its accepted boundary materializes. Reliable reader mounts may
transfer that ownership or recover it fresh after disconnect; delayed trait,
refresh, detach, and disappear callbacks remain fenced.

External router writes expose a second platform behavior: SwiftUI may reuse the
speculative proposal's boundary for a different value returned by a canonicalizing
binding. The accepted getter value, never the proposal, is authoritative. On iOS
27 the inherited scene trait can advance that already-proven same-scene dormant
boundary during body evaluation, before customer lifecycle work. It is not scene
proof and cannot migrate or resurrect state. Replacement, rejection,
canonicalization, restoration, mapper attribution, backend intake, full tests,
lint, and the Release probe build now pass. `EXP-143` is closed.

## EXP-144 — Semantic presentation replacement

Status: PASS after SDK FAIL and harness-invalid retry · backend. Direct Sheet →
full-screen Cover → Sheet replacement is atomic, never exposes the underlying
Home between presentations, and reveals one fresh Home after final dismissal.

### Question

Does the experimental iOS 27 semantic `RUMNavigationStack` treat a direct
non-`nil` presentation-binding replacement as one committed destination change,
without briefly revealing the underlying stack destination or pinning all later
presentations to the first occurrence?

The accepted path is:

```text
Home H1 → Sheet S1 → Cover F1 → Sheet S2 → Home H2
```

Every presentation occurrence must have a distinct RUM view ID, automatic
tracking must create no duplicate, and the final H2 must start before immediate
and settled post-dismiss customer work.

### Preconditions and implementation revision

- Scenario: `swiftui.semantic-api.presentation-replacement`.
- Environment: clean iPad Pro 13-inch (M5) simulator,
  `B4E4F039-CA5D-4D04-A904-1D71099BE651`, iPadOS 27.0, Xcode 27.0.
- Automatic SwiftUI tracking remained enabled around the actual experimental
  semantic-navigation SPI.
- Every conclusive attempt terminated and uninstalled
  `com.datadoghq.rum-native-multi-scene-probe`, then proved the app container was
  absent before installation.
- SDK implementation commit: signed `698b1584d` (`Handle semantic presentation
  replacement`). It holds the last mounted outgoing presentation until the
  accepted replacement boundary mounts, then atomically replaces the same-scene
  handler entry. Clearing a replacement that never mounts falls back to the last
  mounted presentation, and final container detach balances either state.
- Bidirectional fixture and recorder commit: signed `01a8466c5` (`Validate
  bidirectional semantic presentations`).

### Attempts

1. Baseline `semantic-presentation-replacement-20260915-baseline-b` reproduced
   the SDK defect. The direct Sheet → Cover mutation emitted a spurious Home H2
   between them, so final dismissal created Home H3. The oracle stopped after 12
   matches. Backend session `fc45d8cd-f8ad-45ba-983e-8384b668f7d0` contains six
   views including ApplicationLaunch and the incorrect H1/S1/H2/F1/H3 sequence.
2. Forward candidate `semantic-presentation-replacement-20260915-fix-a` passed
   35/35. Backend session `817123e6-5927-43a7-b8ef-596ffd08c8eb` contains only
   ApplicationLaunch plus H1/S1/F1/H2, with no intermediate Home. This accepted
   the atomic Sheet → Cover implementation before the fixture was strengthened.
3. Bidirectional attempt
   `semantic-presentation-bidirectional-20260915-fix-a` executed the correct
   H1/S1/F1/S2/H2 RUM sequence but was harness-invalid. SwiftUI does not promise
   the outgoing modifier's `onDismiss` callback for a direct replacement; the
   recorder therefore opened the shared Sheet-subtree interval twice and failed
   with `interval swiftui-presentation-subtree began more than once`. Session
   `5015c927-ef6e-4f98-8a4f-7893407759fd` remains useful diagnostic evidence but
   is not the accepted oracle result.
4. Final clean attempt `semantic-presentation-bidirectional-20260916-fix-b`
   passed 43/43 with no issues. The harness keeps a presentation-style subtree
   interval active across a replacement when SwiftUI omits `onDismiss`, while a
   delayed callback cannot close an interval for a currently active same-style
   presentation.

### Local and mapper evidence

The final accepted ordered semantic occurrences are:

| Occurrence | View ID | Mapper lifecycle |
| --- | --- | --- |
| Home H1 | `5d36e7a0-5b66-4db7-ab09-6df76161427f` | start 4, stop 39 |
| Sheet S1 | `feff0bf7-c8d3-4961-beda-12a3acb0ad88` | start 40, stop 76 |
| Cover F1 | `18e4ca4a-69b3-4557-98bf-c7723ac7a88d` | start 77, stop 103 |
| Sheet S2 | `df8a0453-c746-49bd-a52f-1250ac726f70` | start 105, stop 129 |
| Home H2 | `44e0784e-a290-4ea1-8048-59b26658a9ff` | start 121, active at capture |

The new view can start before the outgoing aggregate's final inactive snapshot;
the oracle intentionally permits that serialization order while requiring exact
distinct IDs and semantic order. Marker action/Resource pairs resolve to their
actual occurrence:

- `semantic-presentation-home-1`: H1 at sequences 28/32;
- `semantic-presentation-sheet`: S1 at 57/67;
- `semantic-presentation-full-screen-cover`: F1 at 84/97;
- `semantic-presentation-sheet-return`: S2 at 114/128;
- `sheet-dismissed-immediate`: H2 at 132/134;
- `sheet-dismissed-settled`: H2 at 136/145; and
- `semantic-presentation-home-2`: H2 at 142/147.

The Sheet subtree interval begins once at sequence 37 and ends at 130. No
automatic RUM view, intermediate Home, reused Sheet ID, crash, or visual defect
appears.

### Backend evidence

Accepted RUM session: `9061dbf0-4c63-402b-815b-c11a80c8b182`.

Backend intake contains 48 indexed events: 19 actions, 19 Resources, six views,
two long tasks, one session, and one vital. The six views are ApplicationLaunch
plus the exact five mapper IDs above. Every semantic view carries the final run
ID. Presentation views also carry the resolved scene-A session metadata; the
Home descriptor retains the probe's early `unresolved` diagnostic attribute, so
its scene proof comes from the exact local mapper registry rather than that
custom backend field.

Action/Resource counts by exact view are:

| View occurrence | Actions | Resources |
| --- | ---: | ---: |
| H1 | 5 | 5 |
| S1 | 4 | 4 |
| F1 | 4 | 4 |
| S2 | 3 | 3 |
| H2 | 3 | 3 |

No RUM error or crashed-session event matches the run. This independently proves
that backend reduction preserves both presentation directions and the fresh final
Home occurrence.

### Validation and artifacts

- Final focused harness tests: 2/2 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/4D1BD8E0-3559-43C7-AE59-CCE8809E2F91.txt`.
- Final complete probe: 139/139 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/ED5764A3-8015-4FFD-8988-821EDB37F476.txt`.
- Focused SDK replacement tests: 3/3 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/63B9B4CC-72F8-4791-85D3-91218CD02A12.txt`.
- Complete DatadogRUM: 1,252/1,252 at
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/386DB840-F800-4661-B77E-91B6C809AFB0.txt`.
- Baseline artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 144 Baseline Retry A-23_27_53_171`.
- Accepted forward-only artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 144 Fixed A-23_35_21_225`.
- Harness-invalid bidirectional artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 144 Bidirectional Fix-23_56_52_931`.
- Final accepted bidirectional artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 144 Bidirectional Fix B-00_04_20_798`.
- Xcode 27 Release probe build and repository lint remain green; lint reports zero
  violations across 713 source and 699 test files.

### Conclusion and next boundary

Presentation replacement is now supported by the experimental semantic
container in both deterministic directions exercised by the platform. The
outgoing presentation must remain authoritative while a new binding value is
pending; stopping it at binding mutation briefly exposes the underlying stack
and creates a false RUM view. Once the accepted replacement boundary mounts,
same-scene stop/start must be atomic. A retained outgoing snapshot is fallback
state for an unmounted replacement or container teardown, not a reason to pin
later steps to its original occurrence.

The next simulator-capable slice was `EXP-145`: run sibling-container authority
through the actual semantic SPI and prove one container cannot suppress or own
the other. Stable API review and hardware coexistence remain separate gates.

## EXP-145 — Semantic sibling-container isolation

Status: PASS after two INVALID harness attempts · backend

### Question

Does the actual iOS 27 semantic SPI retain its authority boundary when it shares
one outer SwiftUI host with an independently owned sibling navigation branch and
a scene-targeted manual view?

The experiment must prove the product's one-current-destination model, not two
concurrent semantic views. The left branch supplies the manual-authority boundary;
the right branch supplies Home and a committed Detail destination. While the
manual view is current, right-side navigation may continue underneath it but may
not emit Detail. Stopping manual authority must reveal only the latest committed
right-side Detail as a fresh occurrence.

### Preconditions and implementation revision

- Scenario: `swiftui.semantic-api.sibling-container-isolation`.
- Accepted probe and oracle commit: `144d6e0e7` (`Exercise semantic sibling
  isolation`).
- Control: unchanged `swiftui.coexistence.sibling-container-authority` from
  `EXP-127`.
- Tracking stays `.automatic`; the right branch uses the actual
  `RUMNavigationStack` SPI while the left branch retains the existing
  scene-targeted manual-authority driver.
- A controller-topology assertion must prove the right Home/Detail branch is
  distinct from the left authority branch before the semantic oracle can pass.
- The strict oracle forbids all automatic destination views, forbids semantic
  Detail during `manual-sibling-authority`, and requires manual-active work on
  the manual UUID plus immediate and settled post-stop work on fresh Detail.

### Attempts

1. `semantic-sibling-isolation-20260916-a` — INVALID harness.
   RUM session `653cc7bf-e8b3-4cf2-9ccb-a93f03926463` reached the correct
   topology and emitted Detail's delayed action before the driver reached a
   second `wait-for-signal marker:task-delayed`. The later edge-triggered wait
   timed out after the evidence already existed. The scenario now removes only
   that redundant final wait; the `EXP-127` control remains unchanged.
2. `semantic-sibling-isolation-20260916-fix-b` — INVALID harness.
   RUM session `a5634431-0a06-46de-abad-72337de88419` matched 7 expectations,
   but the oracle assumed Detail's asynchronous `task-delayed` callback must fire
   before manual authority stopped. It legitimately fired afterward and used the
   newly revealed Detail UUID. The new scenario excludes that timing-dependent
   callback from its ordered ownership contract and keeps deterministic
   `sibling-underlying-detail-active`, post-stop-immediate, and post-stop-settled
   discriminators on both sides of the authority boundary.
3. `semantic-sibling-isolation-20260916-fix-c` — PASS 19/19.
   RUM session `2a19f538-102a-4df5-842c-3ab2554843a1`. The app remained
   running, the required controller branches were independently observed, and
   no SDK crash, fatal signature, automatic destination duplicate, or oracle
   issue occurred.

### Local and mapper evidence

The accepted run records this view sequence:

1. `ApplicationLaunch` `5833d457-e4dd-4309-a3ed-2ab7f8a75f38` starts and stops.
2. Semantic Home H1 `6e3c0764-d7f9-424f-9ecc-58790884baa8` starts.
3. Manual sibling authority M1 `8d1b6fc6-d4e8-459e-8994-0e005acabe96` starts.
4. H1 stops while M1 remains current.
5. Semantic Detail D1 `e00ce6a9-a973-4412-a272-8fafa3037ba7` starts only after
   the manual-authority interval ends.
6. M1's final aggregate stop is observed after D1 starts; pending work can delay
   that mapper snapshot and does not extend semantic authority.

The controller-topology assertion passes at sequence 60: right Home and fresh
Detail use `SwiftUI.NavigationStackHostingController<SwiftUI.AnyView>` branches
outside the left manual authority. The manual interval is sequence 31 through
69; D1 starts at sequence 72.

Exact ownership is:

- Home delayed action/Resource and `sibling-home-before-authority` → H1;
- `sibling-authority-active`, Detail `on-appear`, `task-immediate`, observed
  `task-delayed`, and `sibling-underlying-detail-active` → M1; and
- `sibling-authority-stopped-immediate` plus
  `sibling-authority-stopped-settled` → D1.

The delayed callback happened inside M1 in the accepted run, but it is diagnostic
rather than an acceptance clock because attempt 2 proves it may legally occur
after the exact stop.

### Backend evidence

Datadog intake for session `2a19f538-102a-4df5-842c-3ab2554843a1` contains
exactly 30 events:

| Type | Count |
| --- | ---: |
| view | 4 |
| action | 11 |
| resource | 11 |
| long task | 2 |
| session | 1 |
| vital | 1 |

The four view groups are exactly ApplicationLaunch, H1, M1, and D1. H1 owns four
actions and four Resources; M1 owns five actions, five Resources, and one long
task; D1 owns the two post-stop actions and two Resources. The session reports
four views, 11 actions, 11 Resources, zero errors, and zero crashes. Every view
document carries run ID `semantic-sibling-isolation-20260916-fix-c`.

M1 and D1 carry native scene session
`33F68994-4513-4100-B294-A1F5A5A2EE23`. H1's early semantic descriptor records
`scene_session_id=unresolved`; the exact mapper registry and controller ancestry,
not that diagnostic field, establish its scene. This is the same early-root
metadata limitation already bounded by the semantic bootstrap experiments.

### Validation and artifacts

- Focused scenario/oracle tests: 3/3 before the accepted run.
- Full native probe: 142/142, zero failures, Xcode 27 result bundle
  `Test-RUMNativeMultiSceneProbe-2026.09.16_00-49-11-+0100.xcresult`.
- Xcode 27 Release iOS-simulator build: PASS.
- Repository lint: zero violations across 713 source and 699 test files.
- Accepted artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 145 Semantic Sibling Retry C-00_45_34_643`.
- Attempt 1 artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 145 Semantic Sibling-00_35_45_265`.
- Attempt 2 artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 145 Semantic Sibling Retry B-00_41_29_838`.

### Conclusion and next boundary

The actual semantic SPI preserves the `EXP-127` sibling authority contract. A
manual boundary in one branch neither creates nor suppresses an unrelated
automatic destination, while the right semantic branch commits underneath it
and becomes current exactly once after authority ends. Mapper and backend
ownership agree.

This closes the actual-SPI sibling discriminator, not the public integration
shape. Revised product constraints require the next semantic work to extract the
scene-scoped occurrence engine from `RUMNavigationStack`, prototype a
container-independent `RUMNavigationHost`, and validate optional capability and
explicit transition-source paths without rewriting native SwiftUI presentation
call sites.

## EXP-146 — Container-independent semantic navigation host

Status: ENGINE/ADAPTER PASS after SDK FAIL, INVALID harness attempts, and one
build-scope fix; simulator final-host removal INCONCLUSIVE; customer migration
cost, genuine OS scene disconnect, and adapter parity remain open

### Question

Can one iOS 27 scene-scoped semantic engine drive an arbitrary customer-owned
SwiftUI container through an outer host and stable type-erased transition source,
while leaving native `NavigationStack`, `.sheet`, and `.fullScreenCover` code
unchanged and preserving exact occurrence, lifecycle, authority, and downstream
attribution semantics?

This experiment asks whether the engine and adapter boundary are correct. Its
explicit `RUMNavigationTransitions` publisher is a deterministic harness adapter,
not evidence that normal applications should publish every transition or maintain
exhaustive RUM-only route and presentation metadata.

### Preconditions and implementation revision

The implementation is split across signed commits:

- `a84061840` extracts `RUMSwiftUISemanticNavigationEngine`, adds the
  experimental arbitrary-content `RUMNavigationHost`, a stable
  `RUMNavigationTransitions` source, optional
  `RUMNavigationTransitionProviding` detection, explicit-source precedence,
  target-local suppression, and generic semantic-destination handler commands;
  and
- `354422d88` adds the deterministic adapter-harness
  `swiftui.semantic-host.explicit-source` probe. It retains standard SwiftUI
  navigation and presentation APIs and adds only one outer host plus one stable
  source owned by the existing router boundary; and
- `651b173c6` adds one customer-owned generic container whose exact specialization
  exposes the optional capability while its otherwise identical opaque
  specialization retains automatic tracking. It also adds fail-closed runtime
  oracles for both paths; and
- `47bc08eca` adds a conflicting-capability precedence arm, repeated stable-source
  resolution, and adversarial source replacement. The same complete semantic
  oracle drives all three, with explicit decoy rejection and capability
  re-resolution assertions.

The host accepts any `View`. An explicit source wins over an optional capability;
a source-less, non-conforming container remains on automatic tracking. The first
source selected by a host is pinned across SwiftUI view-value reconstruction so
a computed capability cannot replay or disconnect the current occurrence.
Prepared transitions create no view; commit emits a fresh occurrence; cancel
does nothing. Exact semantic state continues through the existing per-scene
manual-authority stack so navigation can commit underneath an exceptional manual
view and reveal only the latest destination afterward.

The root source is populated when the container/router is created. The host
consumes the already published iOS 27 scene trait during body evaluation, before
descendant lifecycle callbacks. Customers do not provide an internal RUM UUID or
native scene identifier.

The probe's low-level `willNavigate` and `commit` calls are adapter controls. They
prove prepared/committed/cancelled transition semantics, but are not the proposed
normal customer API. A viable integration still needs automatic metadata by
default, sparse optional overrides, and approximately one integration per
container or existing router rather than edits proportional to destinations or
navigation methods. `EXP-147` subsequently measures and accepts that migration
shape for an existing observable router before API promotion.

### Attempts

1. `semantic-host-explicit-20260916-a` — SDK FAIL against the complete lifecycle
   contract despite the then-current oracle passing 38/38. The arbitrary host,
   standard navigation/presentation code, seven semantic occurrences, fresh
   Home IDs, post-dismiss ownership, and backend intake were correct. However,
   the probe waited for its own scene-reader task before publishing initial Home.
   Home `onAppear` and immediate task work therefore used ApplicationLaunch.
   Session `76808ca7-1512-44ee-9dbe-6d834c97f20c` contained 66 events; the
   ApplicationLaunch view owned three actions and three Resources. This attempt
   remains evidence that a delayed `setInitialDestination` is too late.
2. `semantic-host-explicit-20260916-b` — PASS 42/42 after pre-populating the
   source and reconciling the inherited scene trait during host evaluation.
   Clean preconditions were proven by terminate, uninstall, and missing-container
   checks before the current workspace was rebuilt and installed. No touch input
   was used. RUM session `f9e207c6-689f-4dd9-b9b2-fe0ed9cbac96`; native scene
   `33F68994-4513-4100-B294-A1F5A5A2EE23`.
3. `semantic-host-capability-20260916-a` — PASS 42/42. The same native
   navigation and presentation flow is wrapped by a customer-owned generic
   container whose exact specialization supplies `RUMNavigationTransitions`.
   The outer host discovers that capability without an initializer argument.
   RUM session `8a1dc3bf-b0ff-4fa2-ad5d-cc58afd7a78f`.
4. `semantic-host-automatic-fallback-20260916-a` — INVALID twice because the
   initial ordered oracle treated an earlier Home `task-delayed` marker as a
   violation while searching for the same-named Detail marker. Both clean runs
   nevertheless created an automatic navigation owner, emitted no semantic
   view, and put the later Detail action and Resource on that automatic owner.
   Sessions `dcd95107-01c9-4047-b691-4769592b4e88` and
   `a4842340-c522-4590-8544-013afaacc406`. The duplicate attempt resulted from a
   stalled device worker, not a second SDK hypothesis.
5. `semantic-host-automatic-fallback-20260916-b` — PASS 5/5 after keeping only
   automatic-view existence in the ordered oracle and retaining exact
   source-screen-qualified Detail ownership plus no-semantic-view checks as
   completion conditions. RUM session
   `17706c12-6733-4ad3-93f2-2aba973c769f`.
6. The first focused test build for the precedence/lifetime slice failed because
   `ProbeSemanticHostContract` was declared in app-only `ProbeWindowView.swift`
   while the shared scenario catalog also compiles in the test target. This was
   a build-scope defect, not runtime evidence. Moving those constants to shared
   `ProbeStep.swift` fixed the build without changing the runtime contract.
7. `semantic-host-explicit-precedence-20260916-a` — PASS 43/43. An explicit real
   source and a contradictory capability were both supplied. Only the explicit
   source's seven semantic destinations started; no decoy or automatic view did.
   RUM session `174b4c5c-a7dd-4cee-ab4f-dc0be8ca9c62`.
8. `semantic-host-capability-reconstruction-20260916-a` — PASS 43/43. SwiftUI
   resolved the capability at least twice, producing assertion
   `semantic-host-capability-reconstructed` with
   `capability-resolution-count=2`. The same stable source continued without
   replay, disconnect, or duplicate. RUM session
   `24dd4a46-4a6f-4caf-93e1-77685425e7dc`.
9. `semantic-host-capability-replacement-20260916-a` — PASS 43/43. The second
   capability resolution deliberately returned a decoy source and recorded
   `semantic-host-capability-replaced` with `capability-resolution-count=2`.
   The host kept the original source; no decoy or automatic view started. RUM
   session `4841bf45-25ac-4f63-ac2c-1de934ef9d3b`.
10. Focused host-lifetime tests capture the real DEBUG
    `RUMSceneIdentifierReader.ObserverView`. A synchronous detach/reattach
    cancels the pending final-detach generation and preserves the current
    occurrence and subscription. A final detach stops once, releases the source,
    remains isolated to its scene, and removes only its own automatic-suppression
    boundary. Posted disconnect tests fence stale state and require a fresh
    will-connect/foreground sequence before that scene may start again. These are
    deterministic internal-lifetime tests, not proof of an OS-delivered scene
    disconnect or a later run-loop remount.
11. `exp146-transient-reader-reattach-20260916T034534Z` — PASS 17/17 for the
    synchronous real-reader bounce. Session
    `fe33be6f-b953-4c9a-abce-aae37bdb2ef5` retained the same Detail UUID across
    detach/reattach, stopped it exactly once after the later navigation commit,
    and created a fresh Home H2. Before/after-bounce action and Resource markers
    remained on Detail; post-commit work used H2. No automatic view, SDK error,
    or crash was observed, and both uploads returned HTTP 202.
12. `exp146-final-host-removal-20260916T035341Z` and
    `exp146-final-host-removal-20260916T035545Z` — simulator-INCONCLUSIVE. In
    both clean attempts, simulator `backboardd` aborted in Metal/CoreAnimation
    surface creation immediately after scene B became ready and before the host
    removal step. The app produced no crash report, terminal result, upload, or
    backend event. This row must move unchanged to physical iPad/iPhone Duo; it
    says nothing about SDK final-detach or actual scene-disconnect behavior.
13. The corrected disconnect fence and lifetime slice passes its focused 4/4
    set, full `RUMViewsHandlerTests` pass 84/84, and the complete probe passes
    150/150. The first focused run exposed an obsolete test assumption that
    stale state could restart immediately after disconnect; the test now proves
    rejection until a fresh scene connection rather than weakening the fence.

### Local and mapper evidence

The accepted run records these first-active occurrences:

1. ApplicationLaunch `b393d1a2-c676-43a9-93ab-ed40a434ca3f`.
2. Home H1 `2321c3eb-b2ac-4de6-bbd0-01c467019134`.
3. Detail D1 `5fba5cea-d5ea-4b33-bb62-06886fda8782`.
4. Home H2 `3570cb5d-2ad9-4b97-a4e1-b3e516c561e5`.
5. Sheet S1 `d062af57-fb30-45b5-911a-1e57bf7f2e21`.
6. Home H3 `4e601560-6885-413f-8e0e-2a8a2d5ab839`.
7. full-screen Cover F1 `39dfe61c-089f-44c3-9496-9a9bf9cfb069`.
8. Home H4 `979ffbb2-b1b3-40ee-b76e-eab656d8aa7c`.

All four Home IDs differ. Mapper stop snapshots can arrive after the next start
while aggregate work drains, but the ordered semantic oracle accepts only the
intended occurrence sequence.

The decisive lifecycle ordering is ApplicationLaunch stop, H1 start, then Home
`on-appear` and `task-immediate`. Both action/Resource pairs use H1. The later
Home marker, Detail marker, Sheet and Cover lifecycle/markers, fresh-return
markers, and both immediate and settled dismissal pairs all use their declared
occurrence. Standard `.sheet` and `.fullScreenCover` call sites remain unchanged.
No automatic duplicate, SDK warning/error, or crash was observed; the app
remained running on H4.

The capability run reproduces the same eight-occurrence inventory. Its semantic
IDs are H1 `8ee3da9d-6b63-43a0-b878-94aa0c21803e`, D1
`37ea6129-f560-4e91-acfa-e9d3018ede3b`, H2
`756741c5-c9e5-464e-b14d-729ecc83e289`, Sheet
`ade9da29-7363-4eb8-9746-e228132e3702`, H3
`0a7df611-c0f4-4c9f-853c-37acb0b28102`, Cover
`919b9289-5b2d-4b0f-8880-6e2718ed9b2c`, and H4
`043af6b5-62e0-4cc2-a65b-c9e90f22d281`. All four Home IDs are distinct,
ApplicationLaunch owns no downstream work, and no automatic duplicate starts.

The precedence and two source-lifetime runs each reproduce that full inventory
with four distinct Home IDs, one Detail, one Sheet, and one full-screen Cover.
All three pass the complete 43-event oracle. The reconstruction and replacement
assertions prove that capability resolution actually repeated; the timeline
alone would not prove SwiftUI reconstruction. In the replacement arm, every
subsequent destination and all 52 downstream signals remain on the original
source while the decoy produces no signal or view.

The accepted opaque run intentionally proves a weaker contract. It starts
automatic `NavigationStackHostingController<AnyView>` view
`7cf06dc3-7cdf-4134-a19e-ecd4f7278574`; Detail's delayed action
`2d0ea4cb-7873-46ac-a008-9be74847c02d` and Resource
`61594ba2-ebc4-47d3-85d1-d29db3a0aab5` both use that owner. No semantic-origin
view exists and automatic tracking is not suppressed. As expected without a
trustworthy semantic signal, this does not create exact Home and Detail
occurrences: launch owns initial Home work, and the automatic controller view
owns later Home and Detail work before the automatic root fallback resumes.

The transient-reader run starts Home H1
`eb7184a1-78f7-49d8-a501-48d2e8ed92b4`, then Detail
`716bf6e0-9524-475f-a08d-a84985b5c4a1`. Capturing and synchronously bouncing the
real observer view preserves that exact Detail UUID and source subscription.
The later source commit stops Detail once and starts fresh Home H2
`4ee57ed2-8a62-4117-85f4-04074df3a213`. This is positive evidence for
generation-cancelled transient teardown only. The focused final-detach tests are
positive evidence for source release and scene-local suppression cleanup; the
two failed simulator attempts provide no runtime evidence for final removal.

### Backend evidence

Datadog intake for the three accepted integration paths contains:

| Type | Explicit source | Optional capability | Opaque fallback |
| --- | ---: | ---: | ---: |
| view | 8 | 8 | 4 |
| action | 26 | 26 | 6 |
| resource | 26 | 26 | 6 |
| long task | 5 | 8 | 2 |
| session | 1 | 1 | 1 |
| vital | 1 | 1 | 1 |

The session-wide view query returns exactly the eight IDs above, once each, and
every document carries run ID `semantic-host-explicit-20260916-b`. H1 and D1 own
five actions and five Resources each; H2 owns two of each; Sheet and Cover own
three of each; H3 and H4 own four of each. ApplicationLaunch owns no action or
Resource after the fix. Error/crash aggregation returns no bucket.

The capability session likewise contains exactly launch plus the seven declared
semantic occurrences, 26 actions, and 26 Resources. The opaque session contains
launch, two short-lived automatic root-fallback occurrences, and the automatic
navigation-controller occurrence. All three Detail action/Resource pairs are on
that controller UUID, including the delayed pair above. Neither accepted session
has an error bucket or crash count.

Datadog intake for the precedence and source-lifetime discriminators is:

| Run | Session | Views | Actions | Resources | Error events |
| --- | --- | ---: | ---: | ---: | ---: |
| explicit precedence | `174b4c5c-a7dd-4cee-ab4f-dc0be8ca9c62` | 8 | 26 | 26 | 0 |
| stable reconstruction | `24dd4a46-4a6f-4caf-93e1-77685425e7dc` | 8 | 26 | 26 | 0 |
| adversarial replacement | `4841bf45-25ac-4f63-ac2c-1de934ef9d3b` | 8 | 26 | 26 | 0 |

Each inventory is exactly ApplicationLaunch plus H1, D1, fresh H2, Sheet,
fresh H3, Cover, and fresh H4. Each UUID occurs once. No session contains
`ProbeSemanticCapabilityDecoyView`, an automatic duplicate, downstream work on
ApplicationLaunch, or an error/crash event. Action and Resource grouping is the
same exact 5/5/2/3/4/3/4 distribution across H1/D1/H2/Sheet/H3/Cover/H4.

The transient-reader session contains 32 events: ApplicationLaunch, H1, Detail,
and H2 plus the six expected marker-owner buckets. Backend ownership matches the
local mapper before the bounce, after the bounce, and after the source commit;
crash and error counts are zero. The two final-host-removal attempts have no
backend rows and therefore cannot support a lifecycle conclusion.

### Validation and artifacts

- Original focused semantic-host SDK regressions: 11/11.
- Disconnect/lifetime-focused set after the reader changes: 4/4.
- Full `RUMViewsHandlerTests`: 84/84.
- Full native probe after the disconnect/lifetime matrix: 150/150, result bundle
  `/tmp/dd-sdk-ios-exp146-probe/Logs/Test/Test-RUMNativeMultiSceneProbe-2026.09.16_05-06-19-+0100.xcresult`.
- Complete DatadogRUM run: 1,262/1,263 initially; the sole unrelated
  `TimeseriesSessionCollectorTests/testWhenBackgrounded_pauseAlwaysStopsSampling`
  timing failure passed immediately in isolation.
- Repository lint: zero violations across 713 source and 699 test files.
- API-surface verification: PASS without updating a checked-in baseline.
- Xcode 27 Release iOS-simulator probe build: PASS.
- Accepted artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Host Explicit Source Retry B-01_56_52_133-`.
- Initial failing artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Host Explicit Source-01_42_29_247-`.
- Capability artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Host Capability Matrix A-02_36_04_545-`.
- Accepted opaque-fallback artifact prefix:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/Semantic Host Automatic Fallback Retry B-02_58_47_998-`.
- Invalid opaque-fallback artifact prefixes:
  `Semantic Host Automatic Fallback A-02_42_06_875-` and
  `Semantic Host Automatic Fallback Retry A-02_54_17_472-` in the same artifact
  directory.
- Explicit-precedence artifact prefix:
  `EXP 146 Explicit Precedence 20260916 A-03_32_20_137-` in the same directory.
- Stable-reconstruction artifact prefix:
  `EXP 146 Capability Reconstruction 20260916 A-03_40_11_414-` in the same
  directory.
- Adversarial-replacement artifact prefix:
  `EXP 146 Capability Replacement 20260916 A-03_42_25_026-` in the same
  directory.
- Transient-reader run ID:
  `exp146-transient-reader-reattach-20260916T034534Z`; session
  `fe33be6f-b953-4c9a-abce-aae37bdb2ef5`; assertion
  `semantic-host-transient-reader-reattached` at sequence 65.
- Final-host-removal simulator crash reports:
  `/Users/valentin.pertuisot/Library/Logs/DiagnosticReports/backboardd-2026-09-16-045412.ips`
  and
  `/Users/valentin.pertuisot/Library/Logs/DiagnosticReports/backboardd-2026-09-16-045609.ips`.
- Xcode 27 Release iOS-simulator build after the lifetime slice: PASS with
  Derived Data at `/tmp/dd-sdk-ios-exp146-release`.

### Conclusion and next boundary

EXP-146 validates the scene-scoped occurrence engine and its adapter boundary.
Given a trustworthy stable source, one outer host can establish the initial
destination before customer lifecycle work, preserve committed/cancelled
semantics, create fresh return occurrences, coexist with automatic tracking, and
attribute actions and Resources to the correct occurrence. Explicit-over-
capability precedence and stable source pinning are necessary for deterministic
behavior across SwiftUI reconstruction.

The explicit publisher does not validate the accepted customer integration. The
probe deliberately calls low-level transition methods at navigation boundaries;
requiring normal applications to do that would make integration cost grow with
navigation methods and would violate the approved product constraint. Likewise,
the probe's complete metadata is an oracle, not a requirement for exhaustive
business naming. Correctness must work with automatic metadata and allow sparse
overrides only where customers want them.

Opaque input remains crash-safe and useful through automatic tracking, but it
cannot manufacture semantic information the platform or container does not
expose. A production adapter may consume an existing router/coordinator stream,
adapt a customer-owned capability, or derive native SwiftUI signals; it must not
force screens, standard sheets/covers, or every navigation method to become
Datadog-aware.

The synchronous real-reader bounce and focused teardown tests strengthen the
lifetime implementation but do not close genuine OS scene disconnect or delayed
remount. `EXP-147` is the separate migration discriminator: it measures a
realistic application's diff and rejects shapes whose correctness cost scales
with routes. That gate now passes for the existing-router candidate; the
fixture-local policy was the next SDK move at this checkpoint and is subsequently
implemented by EXP-148. Native/custom/third-party parity and public API promotion
remain separate gates. Physical hardware remains required for
final host removal, genuine disconnect, and two simultaneously enhanced scenes.

## EXP-147 — Customer integration migration cost

Status: PASS · source audit + tests + backend

### Question

Can a realistic SwiftUI application gain exact scene-scoped navigation tracking
with integration work proportional to its independent containers or existing
routers, while keeping screens and ordinary navigation/presentation methods free
of RUM calls?

### Fixture and comparison

Build a customer-shaped fixture containing many heterogeneous and repeated routes,
multiple independent `NavigationStack` containers, standard `.sheet` and
`.fullScreenCover` presentations, an existing observable router/coordinator path,
a native-SwiftUI path without a custom router, and one intentionally opaque
third-party-style container. Preserve an uninstrumented baseline, then compare it
with each candidate container/router-boundary adapter.

The low-level `RUMNavigationTransitions` calls from EXP-146 may implement a
dedicated adapter, but they must not be copied into fixture screens or navigation,
pop, present, and dismiss methods. Correctness uses automatic metadata by default.
Optional custom names are sparse overrides; an exhaustive resolver is permitted
only when the fixture already owns one or deliberately chooses full custom naming.

The checked-in fixture now contains 25 heterogeneous route cases, seven
presentations, three independent observable-router containers, one native
SwiftUI/local-state container, and one opaque third-party-style container. The
seven customer-owned model, screen, and baseline-navigation files contain no
Datadog import, `RUMNavigationHost`, `RUMNavigationTransitions`, `willNavigate`,
or `commit` call. The router retains twelve ordinary customer navigation
methods. Its `NavigationStack`, `.sheet`, and `.fullScreenCover` call sites are
shared unchanged by baseline and instrumented compositions.

The candidate exact arm subscribes once to the router's already accepted
`@Published` state in one dedicated adapter. It derives a route-count-independent
in-memory identity and safe automatic name/path, strips associated values from
emitted metadata, and permits a sparse override. The override intentionally maps
`preferences` to the observably different `Account preferences`. A presentation
identity ignores hidden underlying-path changes so such a change cannot create a
duplicate presented occurrence. The Combine sink weakly captures the transition
source; the adapter no longer retains itself through its cancellable.

This automatic metadata implementation is a fixture-local stand-in for the
SDK-owned policy the eventual integration requires. It validates migration shape
and semantics, not an accepted public declaration. The native/local-state and
opaque arms each add one outer host and deliberately retain scene-aware automatic
tracking because neither exposes a trustworthy semantic transition stream.

### Source-diff measurements

- screen files modified for instrumentation: **0**;
- customer navigation methods modified: **0 of 12**;
- exact integrations: **3**, one per observable router/container;
- automatic-fallback integrations: **2**, one around the native/local-state arm
  and one around the opaque arm;
- exact observable-router composition delta: **+11 lines** inside the three-case
  container switch, including one sparse override; **+20 checked-in source
  lines** when the probe-only SPI/test import gates and iOS 27 availability line
  are also counted;
- dedicated adapter implementation: counted separately and never copied into a
  screen or customer navigation method;
- route-growth result: adding `scheduledMessages`, its ordinary button, and its
  display title required **0 RUM lines**. The adapter SHA-256 remained
  `d993bc9f33ac6be1d8d527736f13278fa1dc4dd718da7e4dc6af2ba55a9c4bc6`
  across that change. Xcode 27 builds before and after are
  `BuildProject-Log-20260916-054641.txt` and
  `BuildProject-Log-20260916-054820.txt` under Xcode's temporary ActionArtifacts;
- presentation-growth result: adding `shareThread`, its ordinary sheet button,
  and its display title required **0 RUM lines**. The adapter, instrumented
  composition, runtime driver, scenario catalog, probe window, and app entry
  remained byte-identical with SHA-256 values
  `1e965859babbd88a179499db302d7ada2e86cde97832845002981b76926d674c`,
  `cf306b9f32fe559014399371054a46221a7a81d637ff5f12996c1e0ca7d45aa7`,
  `959521ab38e15273b9b593a01257383b0f0524b050fd2449a9dc1d39ae02b307`,
  `3faf0519209cb7eac8b1503aba09e8a1e12ff9bdbfbe7f2bc712de87fe602d23`,
  `a35761efc04ad45cd4ebfc4b308b199f54dc07277b1d498e019da404813b8755`,
  and `baa1c4e88f00649d55a109afaf6282760e929c2413b62432862c6adff01c1e7c`
  respectively; and
- an automated source-shape regression test enforces the Datadog-free customer
  files, twelve unmodified router methods, unchanged standard SwiftUI calls,
  route-growth independence, three exact boundaries, two fallback boundaries,
  and the genuinely sparse override.

### Attempts

1. `exp147-router-stream-20260916T050744Z` reached a local 38/38 terminal PASS,
   emitted two accepted HTTP 202 uploads, and produced session
   `803666cd-b7d2-42c1-894d-c3ab0acbdd7d`. It is **INVALID** acceptance evidence:
   `EXP147RuntimeProbeView.swift` and `EXP147RouterStreamAdapter.swift` changed
   while the delegated device run was active. The result is retained solely to
   prevent accidental reuse.
2. `exp147-router-stream-20260916T051346Z` was the first valid frozen-source
   router-stream run. It passed 38/38 locally and in backend session
   `0b1a23d2-8e77-407f-ba1a-632a56cd9dca`, with seven distinct semantic
   occurrences, exact ownership for all eleven action/Resource phases, two HTTP
   202 uploads, and no automatic duplicate, error, or crash. It is valid
   intermediate evidence, but it predates the required presentation-growth
   change and therefore does not alone close EXP-147.
3. The added `shareThread` sheet changed only customer model/screen/display code.
   Xcode 27 build-for-testing passed, followed by **154/154 tests**. The final
   result bundle is
   `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/Test-RUMNativeMultiSceneProbe-2026.09.16_06-28-50-+0100.xcresult`.
   A generic iOS Simulator Release build also succeeded under Xcode 27.
4. Final clean run `exp147-router-stream-20260916T052101Z` passed **38/38**
   locally and produced backend session
   `5f3f47cb-5845-41c3-a1ef-d3b9e95c16d8`. A terminate, uninstall, and missing-
   container check preceded install. All thirteen frozen source hashes matched
   before launch, immediately after the oracle, and after backend validation.
   The exact semantic timeline was:
   - Messages H1 `bfacd19b-b53c-47a2-96d8-0ef56318bf0c`;
   - Thread `d5fe8baa-2d2d-4922-bdfe-6c6cb4fa03b8`;
   - Messages H2 `3e795812-77a2-4177-9a7a-45db4bf1cb05`;
   - Compose `8fd83480-3d5e-4476-82c2-475f173a6d0b`;
   - Messages H3 `429ed980-e513-4585-a7c6-8d2e98c76010`;
   - Attachment `d00951e3-1acc-4653-87bd-20f24473cb92`; and
   - Messages H4 `97d81c69-664c-49a2-8134-ddf849cc52e9`.

   ApplicationLaunch `62afb338-2d9f-4983-b887-f41ea780f317` was the only
   non-semantic view. Each prior semantic occurrence stopped once and H4 remained
   active. Backend intake contained exactly one action and one Resource for each
   of the eleven expected phases, with the same owners as the local oracle and no
   automatic duplicate. Upload request IDs
   `77E0C4EB-EA89-43EC-B79E-45578D7DAF58` and
   `EAF10F00-49B1-4278-AFA6-566D6BCCAD75` both returned 202. The session had zero
   error and crash events. The interaction session was closed and the simulator
   released.

### Measurements

- screen files modified;
- navigation methods modified;
- customer-owned lines added outside a dedicated adapter;
- integrations required per independent container/router;
- RUM-code delta when one route and one presentation are added afterward;
- whether standard agent-generated `NavigationStack`, `.sheet`, and
  `.fullScreenCover` code remains valid without Datadog knowledge; and
- runtime semantic parity for initial, push, pop, sheet, cover, dismissal,
  cancellation, fresh-return occurrence IDs, and automatic fallback.

### Acceptance

- zero screen-file edits for baseline semantic tracking;
- zero per-navigation-method RUM calls;
- unchanged standard sheet and cover call sites;
- approximately one integration per independent container or existing router;
- adding a destination requires no new RUM code unless the customer chooses a
  sparse metadata override;
- correctness does not require an exhaustive RUM-only route/presentation resolver;
- opaque flows retain scene-aware automatic capture rather than forcing a
  navigation refactor; and
- local mapper and backend evidence retain the EXP-146 occurrence and attribution
  contract for every flow the chosen adapter claims to understand.

### Stop condition

If a candidate requires route-proportional instrumentation, record it as a failed
customer integration even when it passes EXP-146's semantic oracle. Do not promote
that shape into the public API. Use the measured diff and runtime gaps to choose
the next native, router-stream, capability, or third-party adapter experiment.

The stop condition is met for the existing-router candidate: both growth deltas
are zero and the final frozen source passes the local mapper and backend oracle.
This accepts the migration shape, not the fixture-local adapter as a public API.
The next experiment must move automatic metadata and generic transition policy
into an SDK-owned iOS 27 prototype while preserving these measurements.

## EXP-148 — SDK-owned observable-router adapter

Status: PASS after intermediate · tests + backend

### Question

Can the accepted EXP-147 migration shape move into DatadogRUM without changing
the customer's screens, router methods, or standard SwiftUI, while preserving
the complete occurrence and downstream-attribution oracle?

### Preconditions and implementation revision

The iOS 27 experimental `RUMNavigationHost` now accepts an application's existing
accepted-state `Publisher` at one container boundary. An SDK-owned observer
projects each state to the scene's current root, route, or presentation, derives
safe automatic metadata, applies optional sparse overrides, and feeds the shared
EXP-146 engine. The EXP-147 fixture retains only a thin boundary call. Customer
screens, twelve navigation methods, `NavigationStack`, `.sheet`, and
`.fullScreenCover` remain unchanged.

The implementation deliberately separates occurrence identity from metadata.
Associated customer values are not serialized or used in automatic names. A
source that can contain equal route values may supply an in-memory occurrence
token, such as path depth or a coordinator transition identity, so consecutive
equal destinations still create fresh RUM view IDs.

Review before the final run found and corrected four adapter defects:

- equal route identities were initially deduplicated, so the SDK now accepts an
  occurrence discriminator and tests depth 1 -> 2 -> 1 with one fresh occurrence
  at every step;
- a publisher without an initial value initially activated semantic authority
  before establishing a view. The host now leaves automatic tracking active until
  the first trustworthy destination arrives;
- erroneous background delivery used independent tasks. Accepted states now
  enter the main queue in FIFO submission order; and
- while an explicitly configured observed source was waiting for its first value,
  an incidental content capability could become permanently selected. A configured
  explicit or observed input now owns precedence even while pending and cannot
  fall through to a lower-priority capability.

### Attempts

1. Intermediate clean run `exp148-sdk-observed-20260916T060728Z` passed 38/38 in
   session `613c5e81-8e01-437e-b80a-b2a0d8d1aa19`, with the exact eight-view
   timeline and 22 ownership buckets. It remains valid evidence for the exercised
   path but is **not** final acceptance: the pending-source precedence edge above
   was discovered after its source freeze and was not exercised by its synchronous
   `@Published` fixture.
2. The corrected source passed the nine focused observed-source/precedence tests,
   all 153 probe tests, and a generic Xcode 27 Release iOS Simulator build. The
   API-surface verifier reported only the intentionally experimental navigation
   declarations and previously proposed scene-targeted manual-view overloads; no
   checked-in API baseline was changed.
3. Clean run `exp148-sdk-observed-final-20260916T062010Z` used a new run ID,
   a terminate/uninstall/missing-container boundary, and a manifest covering the
   SDK implementation, probe project, and every probe source. All hashes matched
   before launch, immediately after the local oracle, and after backend queries;
   session `407abf6c-f037-4b7f-861a-28f72001ccc1` passed 38/38 with the exact
   eight-view and 22-bucket backend result.
   It passed 38/38 with exact backend ownership, but a subsequent full lint found
   one serious private-parameter-ordering violation. The run remains valid pre-
   lint evidence and is not the authoritative exact-source acceptance.
4. The mechanical private-parameter reorder then passed repository lint, the
   repeated 9/9 focused tests, 153/153 probe tests, and the Release build. A new
   clean run `exp148-sdk-observed-postlint-20260916T063742Z` repeated the complete
   oracle from a new uninstall/missing-container boundary. Its three-point SDK,
   project, and complete probe-source manifest stayed identical. This is the
   authoritative post-lint acceptance.

### Local and mapper evidence

The final run passes 38/38 with no issue and no automatic-origin view. Its exact
timeline is:

- ApplicationLaunch `dd7534ec-a16e-4b7c-9583-f3e4c90bd7df`;
- Messages H1 `40e78f00-7d67-4f33-825e-b7af4dd39436`;
- Thread `c1439a0a-9440-41e3-83d6-5a1813367dcc`;
- Messages H2 `472431fd-70fa-49a3-880d-3f0c4910ae8d`;
- Compose `368c49dc-0e5f-43ea-8479-4ded95f53bd9`;
- Messages H3 `c71843fe-6af8-4dff-ba42-b9feded46433`;
- Attachment `928fbedd-d15b-462f-b6aa-b69767922493`; and
- Messages H4 `bc940c99-c71b-4148-9249-7be0f35885b7`, still active at the
  terminal assertion.

Every earlier semantic occurrence stops once. Each of the eleven declared phases
produces exactly one action and one Resource on its expected occurrence: H1 owns
`home-1`; Thread owns `detail-1`; H2 owns `home-2`; Compose owns `sheet-1`;
H3 owns both Sheet-dismiss callbacks plus `home-3`; Attachment owns
`full-screen-cover-1`; and H4 owns both cover-dismiss callbacks plus `home-4`.

### Backend evidence

Session `c951c32e-12a7-4bc4-bc5e-bdaf2f16f8aa` contains exactly the eight views
above, all tagged with the final run ID. The eleven action/Resource pairs produce
22 exact owner buckets, each with count one and the same ownership as the local
mapper. The health query returns one session row, `crash.count = 0`, and
`error.count = 0`. Upload request IDs
`2992C51B-E477-4CDB-8211-B4178B6B3F2D` and
`3403A83F-A321-4517-BB80-06F22A83B80C` both returned HTTP 202.

### Validation and artifacts

- Focused observed-source and precedence tests: 9/9, result bundle
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/Test-DatadogRUM-2026.09.16_07-34-37-+0100.xcresult`.
- Native probe suite: 153/153, result bundle
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunAllTests/Test-RUMNativeMultiSceneProbe-2026.09.16_07-34-57-+0100.xcresult`.
- Repository lint: zero violations in 713 source and 699 test files.
- Xcode 27 generic Release iOS Simulator build: PASS with Derived Data at
  `/tmp/dd-sdk-ios-exp148-release`.
- Frozen SDK hashes: `SwiftUIViewModifier.swift`
  `7e07548e9f802b3a630cc72887d20aef583f53125fa8f632eed43ff4c746e725`;
  `RUMViewsHandler.swift`
  `af4b02c871683f9809a5c10e0428f56fee19b795285c01799e26e97428573d6a`.
- Frozen probe project hash:
  `0083ff4dd2b3e83668f7e660b3e81685e8944464e7ce240b31d0f6dcc4b3d623`.
- The thin fixture adapter and composition hashes were respectively
  `81ea9d6d37c8f0bea294882bef401168490711206735e6d8ec222750556a9dcf`
  and `f1c4c8d0a7f7ee732783ba3d4bcd36f3a9fd09734b61bf0c6d19ba8ec1354330`.

### Conclusion and next boundary

EXP-148 accepts the SDK-owned observable-router candidate for applications that
already expose a trustworthy accepted current-destination stream. It preserves
EXP-147's route-count-independent migration budget and EXP-146's exact semantic
oracle without promoting the low-level transition calls as normal customer code.

This is still an experimental iOS 27 API shape. It does not prove exact semantics
for an opaque native or third-party container, approve public names, or close
genuine OS disconnect/remount. The next simulator slice compares observed-router,
optional-capability, explicit third-party, native-convenience, and honest opaque
fallback behavior through one parity matrix, including two-scene isolation and
actual subscription reconstruction. Physical hardware still owns final removal
and genuine disconnect/reconnect.

## EXP-149 — Observed-source lifetime and scene isolation

Status: PASS · focused tests; genuine OS lifecycle remains hardware-gated

### Question

Does the SDK-owned observed-state integration retain one stable subscription
through actual SwiftUI value reconstruction, isolate two observed sources by
scene, release only the disconnected source, and leave an unrelated automatic
container eligible?

### Preconditions and implementation revision

This experiment starts from accepted EXP-148. It does not alter the SDK
implementation or reinterpret EXP-146's low-level transition source as the
normal customer path. The accepted-state observer remains owned by the SDK and
the application still integrates once at its existing router/container boundary.

The implementation hash remains the authoritative EXP-148 post-lint hash:

- `SwiftUIViewModifier.swift`:
  `7e07548e9f802b3a630cc72887d20aef583f53125fa8f632eed43ff4c746e725`.

The final test-source hashes are:

- `SwiftUIViewNameExtractorTests.swift`:
  `b1c05fd3c4b5845b1d2215cb4380ed72637e8d3aa974678adf3896a570302fec`;
- `RUMViewsHandlerTests.swift`:
  `27f6b114067d010a0ef2776751d2d4f9d9327cdb63ef704022cbbf0f4d3d5028`.

### Attempts

1. The first smoke selection added the real-host reconstruction test and the
   observed A/B disconnect test. Both passed, 2/2, in
   `Test-DatadogRUM-2026.09.16_07-50-53-+0100.xcresult`.
2. The suppression test was strengthened to include an unrelated automatic
   subtree. The three changed tests passed, 3/3, in
   `Test-DatadogRUM-2026.09.16_07-51-48-+0100.xcresult`.
3. The complete semantic-engine, observed-adapter, pending-authority, lifetime,
   scene-isolation, and suppression selection passed 20/20 in
   `Test-DatadogRUM-2026.09.16_07-52-25-+0100.xcresult`.

No failed or invalid attempt preceded the final selection.

### Local and mapper evidence

`testObservedNavigationHostSubscribesOnceAcrossSwiftUIReconstruction` mounts the
actual `RUMNavigationHost(observing:)` in a `UIHostingController`. Its outer view
first renders publisher A, then reconstructs the same host identity while
rendering publisher B. Render counters prove both selections were evaluated;
subscription counters remain A = 1 and B = 0. The host therefore keeps its
original SDK-owned observer instead of replaying or switching sources during
ordinary SwiftUI reconstruction.

`testObservedNavigationHosts_areIsolatedBySceneAndDisconnectExactSource` creates
two `RUMNavigationObservedTransitions` instances and attaches their host states
to scenes A and B. Initial Messages and Settings starts target A and B
respectively. A posted disconnect plus exact host cleanup stops only A. A later
Thread update from A emits no command, while B advances from Settings to
Preferences with B-targeted stop/start commands. A's suppression state and
selected source are released; B's remain active.

`testSemanticHostFinalDetachReleasesOnlyItsAutomaticSuppressionSubtree` proves
two enhanced controller subtrees are suppressed independently and a third,
unrelated subtree remains eligible before and after A's final detach. The final
20-test selection also re-runs delayed first-value authority, configured-source
precedence while pending, fresh equal-route occurrences, FIFO background
delivery, presentation-hidden-state filtering, low-level final detach, and
posted scene-disconnect fencing.

There is no mapper payload in this deterministic experiment. Command targets and
subscription counts are the appropriate assertions for the internal lifetime
boundary; EXP-148 already owns the frozen mapper/backend occurrence proof.

### Backend evidence

Not applicable. This experiment changes and validates deterministic lifetime and
scene fencing only. It does not make a new backend-attribution claim.

### Validation and artifacts

- Final focused selection: 20/20, result bundle
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/Test-DatadogRUM-2026.09.16_07-52-25-+0100.xcresult`.
- Initial focused selection: 2/2, result bundle
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/Test-DatadogRUM-2026.09.16_07-50-53-+0100.xcresult`.
- Strengthened suppression selection: 3/3, result bundle
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/RunSomeTests/Test-DatadogRUM-2026.09.16_07-51-48-+0100.xcresult`.

### Conclusion and next boundary

EXP-149 closes the simulator-capable observed-source lifetime and scene-isolation
matrix. The SDK-owned candidate has one stable observer per host identity, exact
scene-local teardown, no stale post-disconnect transition, peer continuity, and
local rather than process-wide automatic suppression.

This result does not prove that UIKit/SwiftUI or iOS delivered a real scene
disconnect, delayed remount, or reconnect. Those remain unchanged hardware rows.
The next simulator slice is parity across the native convenience, observed
router, optional capability, explicit third-party adapter, and opaque automatic
fallback, with exactness claimed only for trustworthy semantic inputs.

## EXP-150 — Render-time current-destination boundary

Status: FAIL · mapper + backend; deterministic tests remain green

### Question

Can a normal application without a publisher expose only its already accepted
current root, route, or presentation once per container, while retaining the
same exact occurrence and reveal-before-callback behavior as the accepted
observable-router adapter?

### Preconditions and implementation revision

The candidate added an iOS 27 experimental
`RUMNavigationHost(currentDestination:metadata:in:content:)` initializer. A
stable `@StateObject` retained the transition source, while each host
reconstruction reconciled the current destination value before resolving the
host source. The EXP-147 fixture supplied that value from the same router state;
its `NavigationStack`, sheet, cover, screens, and navigation methods remained
unchanged.

The frozen runtime hashes were:

- `SwiftUIViewModifier.swift`:
  `16c3e82f08496e5fa1ef039e572e5434c6066f34607caba9c0bc0f093cd0775f`;
- `RUMViewsHandler.swift`:
  `af4b02c871683f9809a5c10e0428f56fee19b795285c01799e26e97428573d6a`;
- probe project:
  `0083ff4dd2b3e83668f7e660b3e81685e8944464e7ce240b31d0f6dcc4b3d623`;
- sorted probe Sources:
  `89fcd8f33ceb82861f79fbb2eb80cb42e8fe6767e5da1a234262e4d2e63a58fa`.

### Attempts

1. State-level parity initially looked correct. Three focused tests passed,
   including a direct-value timeline compared with the observed, explicit, and
   capability sources.
2. The complete probe plan passed 154/154, and a broadened SDK selection across
   the semantic engine, authority registry, and complete `RUMViewsHandlerTests`
   passed 107/107. Repository lint reported zero violations in 713 source and
   699 test files.
3. A clean iPadOS 27 run used an explicit terminate, uninstall, and failed
   app-container lookup before install. All four frozen hashes matched before
   and after. Run `exp150-current-destination-20260916T072321Z` reached a
   terminal FAIL after matching 17 of 38 expectations.
4. A generic Release build was accidentally run through `/Applications/Xcode.app`,
   which is Xcode 26.6. It compiled but is tooling-invalid for the requested
   Xcode 27 gate. The current source still requires an explicit
   `/Applications/Xcode_27.app` Release build after the candidate is revised or
   removed.

### Local and mapper evidence

The failure is an observation-boundary timing defect, not an engine or handler
failure. The application assigns accepted router state and synchronously emits
the dismissal marker. SwiftUI reevaluates the outer host afterward, so the
candidate reconciles the new Home destination one signal too late.

- Sheet dismissal: immediate action and Resource remained on Compose
  `f77cfb3d-ae9b-4350-aaad-42d2125b3cdc`; fresh Home
  `1b1d9d98-0676-4d04-818f-1d5e2a44ad26` started afterward. Settled dismissal
  work used that Home occurrence.
- Full-screen-cover dismissal: immediate action and Resource remained on
  Attachment `4bc3f676-75c3-45ca-b649-6918a71d5ad1`; fresh Home
  `ba498efe-8d0f-4041-a5f3-8a359a3650f6` started afterward. Settled dismissal
  work used that Home occurrence.

The app remained running. No SwiftUI publish-during-update warning, SDK error,
crash, or visual defect occurred. The only warning was unrelated simulator
accessibility duplicate-class noise.

The immediate markers in this run are emitted after the accepted state mutation;
they are deliberately stronger than a platform `.sheet(onDismiss:)` callback.
They prove that a value read only during the next render cannot satisfy the
approved guarantee for synchronous post-dismiss customer work. A separate
actual-`onDismiss` discriminator may characterize a weaker boundary, but it
cannot turn this candidate into the exact API.

### Backend evidence

Session `2294a609-76f9-47be-a643-ab51edc5b638` contains eight views, eleven
actions, and eleven Resources. Datadog intake independently reports:

- `sheet-dismissed-immediate` action and Resource on Compose, while
  `sheet-dismissed-settled` and `semantic-home-3` use the fresh Messages view;
- `full-screen-cover-dismissed-immediate` action and Resource on Attachment,
  while `full-screen-cover-dismissed-settled` and `semantic-home-4` use the
  fresh Messages view; and
- no error event bucket.

### Validation and artifacts

- Focused direct-value tests: 3/3,
  `Test-DatadogRUM-2026.09.16_08-08-56-+0100.xcresult`.
- Full probe: 154/154,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_08-13-37-+0100.xcresult`.
- Broadened SDK selection: 107/107,
  `Test-DatadogRUM-2026.09.16_08-17-57-+0100.xcresult`.
- Frozen-run logs:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP150 Current Destination-08_26_20_145-logs.txt`.
- Frozen-run hierarchy and screenshot share the same artifact directory and
  `EXP150 Current Destination-08_26_20_145` prefix.

### Conclusion and next boundary

Reject the value-only current-destination initializer as an exact customer
integration. State-level transition parity tests are insufficient when the
adapter receives its value only during SwiftUI evaluation; every candidate must
also prove callback ordering in a live presentation flow.

The next candidate must receive a trustworthy signal during the accepted
mutation itself, or explicitly claim weaker semantics. On Xcode 27, the new
one-shot Observation API with `.didSet` is worth a focused ordering experiment
for existing `@Observable` routers because it can report the stored value during
the mutation without requiring a Combine publisher. Continuous observation is
not equivalent: its callback is delivered at the next suspension point and is
too late for this contract. Plain local `@State` and opaque third-party state
remain scene-aware automatic tracking plus sparse manual exceptions unless a
container-local binding/materialization adapter proves an earlier boundary.

## EXP-151 — One-shot Observation router boundary

Status: PASS · tests + mapper + backend

### Question

Can one integration around an existing iOS 27 `@Observable` router observe its
accepted destination early enough to create a fresh RUM occurrence before the
router setter returns, while leaving `NavigationStack`, sheets, covers, screens,
and navigation methods unchanged?

### Preconditions and implementation revision

The candidate reuses the accepted semantic engine and the same realistic
EXP-147 fixture that rejected render-time observation in EXP-150. The router is
both `@Observable` and a compatibility `ObservableObject`; the new adapter uses
Xcode 27's one-shot `withObservationTracking(options: [.didSet])`, reads the
stored destination synchronously, reconciles it, and rearms from the callback.
It does not use continuous Observation, replace a standard SwiftUI container,
or claim exact support for opaque local `@State`.

Frozen runtime hashes:

- `SwiftUIViewModifier.swift`:
  `fe036d3286de0df4bf9ae39762b1e2e73980d3e5abe40862c1e75e56ce442ae4`;
- `RUMViewsHandler.swift`:
  `af4b02c871683f9809a5c10e0428f56fee19b795285c01799e26e97428573d6a`;
- probe project:
  `0083ff4dd2b3e83668f7e660b3e81685e8944464e7ce240b31d0f6dcc4b3d623`;
- combined probe Sources:
  `0144f4933efe891ec88e674a55fddcb45f7f7d2e8a8e5f72f935e2e0fe5f15ad`.

The first accepted run used pre-review modifier hash
`4d0a6e59c1b8f063ca2880a6d09f3710736a9228791a1b406e40393ec0e3f732`.
It remains useful historical evidence, but the post-review run below supersedes
it as the authoritative checkpoint.

Signed implementation checkpoint: `24cf5078a`. Signed migration fixture,
harness, and probe-test checkpoint: `7da52ae6d`. Both preserve the frozen
runtime hashes above.

### Attempts

1. The first focused test build failed because an `@Observable` test fixture was
   `private`; the macro-generated sibling source could not access it. Widening
   only the fixture's visibility fixed compilation. This did not change SDK
   behavior.
2. Focused tests then passed 4/4, including synchronous ordering across repeated
   `.didSet` mutations, deallocation safety, standard-navigation composition,
   and parity with the shared accepted-destination timeline.
3. The first Xcode 27 Release build passed but reported a Swift 6 isolation
   warning where the migration fixture also conformed to `ObservableObject`.
   Marking that legacy conformance `@preconcurrency` removed the candidate-owned
   warning; the repeated Release build passed.
4. A clean frozen iPadOS 27 run used scenario
   `swiftui.semantic-host.observation-router-adapter` and run ID
   `exp151-observation-router-20260916T080750Z`.
5. Independent review found three gaps: incomplete cross-platform availability
   proof, no nested-reentrant mutation test, and an overbroad implication that a
   projection over several independently mutated properties was atomic. The SDK
   availability annotations and documentation were tightened; the contract now
   requires one atomically updated accepted-state property.
6. Focused tests now pass 8/8. They cover sequential generations, nested mutation
   from a transition observer, independent-property intermediate states,
   deallocation, SwiftUI `StateObject` reconstruction, invalid background
   mutation crash safety, unchanged standard navigation, and adapter parity.
7. The affected SwiftUI test file passes 193/193, the native probe passes
   154/154, the explicit Xcode 27 iOS Release build passes, and
   `spm-build-visionos` passes. The visionOS build emitted only existing warnings
   outside this candidate.
8. A second clean-boundary run used the same scenario and run ID
   `exp151-observation-router-postreview-20260916T083131Z`. All four frozen hashes
   matched before and after execution.

### Local and mapper evidence

The authoritative post-review strict semantic oracle passes 38/38 with no
issues. The seven semantic
occurrences are:

| Occurrence | RUM view ID |
| --- | --- |
| Home 1 | `61be3854-1108-4390-8cbf-f4c02962a4a1` |
| Detail 1 | `4ea0d56d-3d17-43ee-bd49-eea0e137734f` |
| Home 2 | `7c513cb5-88f1-4820-8e12-091f0e89e413` |
| Sheet 1 | `d7abcb72-f59d-433d-aadb-52990e11ea54` |
| Home 3 | `b24b9bfc-3697-4a95-8508-8801059673e9` |
| Full-screen cover 1 | `e37cc2f2-c88b-4c49-9d62-5ffac4dc9f68` |
| Home 4 | `8d17235e-2b7b-4a2e-acb2-27ec1c60e70d` |

Both `sheet-dismissed-immediate` and `sheet-dismissed-settled` action/Resource
pairs use Home 3. Both full-screen-cover dismissal pairs use Home 4. This closes
the exact ordering failure exposed by EXP-150 rather than merely correcting the
eventual state. The app remained running, both uploads returned HTTP 202, and no
crash or SwiftUI publishing/view-update warning appeared. The only runtime
warning was unrelated simulator duplicate accessibility-class noise.

### Backend evidence

RUM session `ea9adc0e-5a00-4290-9580-fb4df6bc18e7` contains:

- eight views, including `ApplicationLaunch` plus all seven distinct semantic
  occurrences;
- 11 actions and 11 Resources;
- five long tasks, one session event, and one vital;
- no error event bucket and a session crash/error count of zero.

Raw intake assigns both sheet dismissal action/Resource pairs to Home 3
`b24b9bfc-3697-4a95-8508-8801059673e9` and both cover dismissal pairs to Home 4
`8d17235e-2b7b-4a2e-acb2-27ec1c60e70d`. Presentation work remains on the Sheet
and Attachment occurrences. Backend ownership therefore agrees with the local
mapper timeline.

### Validation and artifacts

- Focused Observation/adapter tests: 8/8,
  `Test-DatadogRUM-2026.09.16_09-33-05-+0100.xcresult`.
- Broadened affected SwiftUI file selection: 193/193,
  `Test-DatadogRUM-2026.09.16_09-29-16-+0100.xcresult`.
- Full native probe: 154/154,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_09-29-37-+0100.xcresult`.
- Explicit Xcode 27 generic iOS Release build: PASS after removing the
  fixture-owned Swift 6 warning.
- Xcode 27 `spm-build-visionos`: PASS; candidate source compiles in the xros
  module and only pre-existing unrelated warnings remain.
- Repository SwiftLint: PASS across 713 source and 699 test files with zero
  violations on the post-review slice.
- API-surface verification: expected mismatch only for the unaccepted
  experimental navigation and scene-targeted manual-view prototypes. Neither
  Swift nor Objective-C reference baselines was updated.
- Frozen-run logs:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP151 Observation Router Postreview-09_32_48_582-logs.txt`.
- The hierarchy, screenshot, and thumbnail share the same artifact directory and
  `EXP151 Observation Router Postreview-09_32_48_582` prefix.

### Conclusion and next boundary

Accept one-shot `.didSet` Observation as a trustworthy iOS 27 adapter input for
an existing `@Observable` router when one atomically updated property represents
the complete accepted destination. It satisfies the
one-integration-per-container constraint, starts fresh revealed occurrences
before synchronous post-mutation work, and reproduces exact backend ownership
without changing standard SwiftUI.

This is not stable API approval and it is not evidence for plain local `@State`.
Before promotion, characterize actual sheet/cover `onDismiss` callbacks,
two-scene adapter isolation, and custom/third-party adapter parity. Sequential,
nested, reconstruction, teardown, and invalid background-mutation behavior now
have deterministic coverage. Opaque native state continues through scene-aware
automatic tracking plus sparse manual exceptions.

## EXP-152 — Native SwiftUI dismissal callback characterization

Status: PASS · tests + mapper + backend

### Question

After the accepted `EXP-151` Observation adapter commits a fresh underlying
destination, do SwiftUI's actual `.sheet` and `.fullScreenCover` `onDismiss`
callbacks run late enough for immediate customer work inside those callbacks to
use that fresh RUM occurrence, without creating another Home view?

### Preconditions and implementation revision

This is a harness-only characterization. It does not change DatadogRUM. A
probe-only copy of the realistic EXP-147 navigation container keeps the accepted
`EXP151RUMObservationBoundary`, standard `NavigationStack`, `.sheet(item:)`, and
`.fullScreenCover(item:)`. It adds native presentation-content `onAppear`
signals and native `onDismiss` callbacks solely as test discriminators.

The content-appearance barrier is required. A router destination proves accepted
state, not that SwiftUI mounted the presentation; dismissing before `onAppear`
would make a missing callback a harness race rather than an SDK result. The
callback records the router presentation, current screen, and accepted mutation
number before emitting uniquely named immediate and settled action/Resource
pairs. The existing `EXP-151` synchronous post-mutation oracle remains unchanged
and stronger.

Signed harness checkpoint: `0a67f3e36`
(`Add native presentation dismissal probe`). Frozen runtime hashes:

- `SwiftUIViewModifier.swift`:
  `fe036d3286de0df4bf9ae39762b1e2e73980d3e5abe40862c1e75e56ce442ae4`;
- `RUMViewsHandler.swift`:
  `af4b02c871683f9809a5c10e0428f56fee19b795285c01799e26e97428573d6a`;
- probe project:
  `0083ff4dd2b3e83668f7e660b3e81685e8944464e7ce240b31d0f6dcc4b3d623`;
- combined probe Sources:
  `2703437b85e2f2b69cca3ec55e98977984956a93325e7dc32234a1887691dc91`.

### Attempts

1. Read-only source analysis established that EXP-151's dismissal markers were
   emitted immediately after router mutation and were not native SwiftUI
   callbacks. Existing outer `onDismiss` callbacks were inactive for that
   fixture, so changing the accepted scenario would have conflated two timing
   boundaries.
2. A separate scenario,
   `swiftui.semantic-host.observation-native-dismiss-callbacks`, added four
   fail-closed assertions: Sheet content appeared, Sheet `onDismiss` entered,
   cover content appeared, and cover `onDismiss` entered. The focused scenario
   contract test passed 1/1.
3. The complete native probe passed 155/155. The first `xcresulttool` summary
   read was blocked by the read-only shell's temporary-cache permissions; the
   identical read outside that sandbox succeeded and confirmed all 155 tests.
   This was a tooling retry, not an experiment attempt.
4. Repository SwiftLint passed across 713 source and 699 test files with zero
   violations. The Xcode 27 iOS Release build passed with only previously known
   repository warnings.
5. A clean run on the iOS 27.0 iPad simulator used run ID
   `exp152-native-dismiss-callbacks-20260916T094300Z`. Termination found no
   running probe, uninstall succeeded, and querying its application container
   proved it absent before installation. All four hashes matched before launch,
   immediately after the terminal oracle, and after final capture.

### Local and mapper evidence

The strict semantic oracle passes 38/38 with no issues. Each native assertion
fires exactly once and passes:

| Native boundary | Recorder sequence | Observed accepted state |
| --- | ---: | --- |
| Sheet content appeared | 57 | `presentation=compose`, `screen=sheet`, mutation 3 |
| Sheet `onDismiss` entered | 77 | `presentation=nil`, `screen=home`, mutation 4 |
| Cover content appeared | 98 | `presentation=attachment(1)`, `screen=full-screen-cover`, mutation 5 |
| Cover `onDismiss` entered | 123 | `presentation=nil`, `screen=home`, mutation 6 |

The seven semantic occurrences are:

| Occurrence | RUM view ID |
| --- | --- |
| Home 1 | `4554fe1e-1946-4cdb-bf0f-a9d5db079514` |
| Detail 1 | `4bed09c3-90d6-4618-99ef-d3e7ce3dbdf5` |
| Home 2 | `2aabdddc-646d-44fa-b18e-cb91666ed6af` |
| Sheet 1 | `59dce09f-8508-42e3-90d5-1df08b2e29d7` |
| Home 3 | `5d170694-993b-460e-8a1e-68764bcfbd41` |
| Full-screen cover 1 | `b7f4a28c-f972-42f7-ae95-52e4a9c2b417` |
| Home 4 | `83c0af19-7aa8-4897-a6a2-7dfb9399c704` |

All four Home IDs are distinct. No automatic-origin semantic destination and no
extra Home caused by `onDismiss` appears. Sheet native-callback immediate and
settled actions and Resources each occur exactly once on Home 3. Cover
native-callback immediate and settled actions and Resources each occur exactly
once on Home 4. The app remains `Running`; three uploads return HTTP 202. No
crash, semantic error, or SwiftUI publishing/view-update warning appears. Only
known simulator CoreTelephony and duplicate accessibility-class noise remains.

### Backend evidence

RUM session `fee27d61-1eb7-4eb6-8525-7af74a53ed7e` contains:

- eight views: ApplicationLaunch plus the seven semantic occurrences above;
- 11 actions, 11 Resources, seven long tasks, one session, and one vital;
- zero RUM error events and session error/crash counts of zero.

All four `*-native-on-dismiss-*` actions and all four matching Resources are
present exactly once. Backend ownership matches the mapper: both Sheet callback
pairs use Home 3 `5d170694-993b-460e-8a1e-68764bcfbd41`, and both cover callback
pairs use Home 4 `83c0af19-7aa8-4897-a6a2-7dfb9399c704`.

### Validation and artifacts

- Focused EXP-152 scenario contract: 1/1.
- Full native probe: 155/155,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_10-39-43-+0100.xcresult`.
- Xcode 27 iOS Release build: PASS.
- Repository SwiftLint: PASS across 713 source and 699 test files.
- Frozen-run logs:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP152 Native Dismiss Callbacks-10_48_24_612-logs.txt`.
- The hierarchy, screenshot, and thumbnail share the same artifact directory and
  `EXP152 Native Dismiss Callbacks-10_48_24_612` prefix.

### Conclusion and next boundary

The accepted Observation router mutation precedes SwiftUI's native dismissal
completion callback. By callback entry the presentation is absent, the scene's
current destination is Home, and a fresh Home RUM occurrence already owns both
immediate and settled callback work. No SDK change is required for this timing
path.

This result does not make `onDismiss` the adapter's accepted-state signal and
does not replace the stronger synchronous post-mutation oracle. Direct
presentation replacement can omit outgoing `onDismiss` callbacks (`EXP-144`),
and interactive swipe dismissal/cancellation still needs a physical device or
human-driven experiment.

## EXP-153 — Callback-driven third-party navigation adapter

Status: PASS · tests + mapper + backend

### Question

Can a custom or imported navigation library that owns its visual container and
exposes synchronous accepted-state callbacks use one adapter at the container
boundary, without retroactive conformance, screen edits, or RUM calls in every
navigation method, while preserving the complete occurrence and attribution
contract?

### Preconditions and implementation revision

The probe adds a genuinely custom visual container backed by a library-shaped
navigator. It uses `ZStack` and custom transitions rather than
`NavigationStack`, `.sheet`, or `.fullScreenCover`. The navigator owns route and
presentation occurrence identities and exposes its current accepted snapshot
plus a synchronous callback registration. Cancelled interactive transitions do
not publish; committed transitions publish the accepted snapshot before the
navigation method returns.

One probe-only adapter converts that callback into a current-value publisher and
feeds the existing SDK-owned `RUMNavigationHost(observing:destination:metadata:)`.
The adapter is retained by one `StateObject` boundary around the unchanged
custom container. Customer screens and navigation methods contain no RUM calls,
the library type does not retroactively conform to a Datadog protocol, and no
DatadogRUM source changes were needed.

Signed harness checkpoint: `d6d813736`
(`Exercise callback-driven semantic navigation`). Frozen runtime hashes:

- `SwiftUIViewModifier.swift`:
  `fe036d3286de0df4bf9ae39762b1e2e73980d3e5abe40862c1e75e56ce442ae4`;
- `RUMViewsHandler.swift`:
  `af4b02c871683f9809a5c10e0428f56fee19b795285c01799e26e97428573d6a`;
- probe project:
  `0083ff4dd2b3e83668f7e660b3e81685e8944464e7ce240b31d0f6dcc4b3d623`;
- complete probe Sources tree:
  `2387e59bf553e020eda9c93b9eec1cd7eb7dad9b108e609ec868a9de07708def`.

### Attempts

1. The first focused compile exposed an unconstrained `CurrentValueSubject`
   failure type in the callback bridge. Giving the subject its full
   `Snapshot, Never` type fixed only the fixture's generic inference.
2. The next compile exposed a closure whose ignored parameter was written
   without `_ in`. Correcting the fixture closure completed compilation.
3. The focused scenario, navigator, lifetime, and source-shape selection passed
   7/7. It covers synchronous initial/accepted delivery, equal-route occurrence
   identity, cancelled versus committed interactive pop, atomic Sheet-to-cover
   replacement, registration release, the complete scenario contract, and the
   absence of native-container or retroactive-conformance shortcuts.
4. The complete probe passed 161/161. Repository SwiftLint passed across 713
   source and 699 test files, and the Xcode 27 generic iOS Simulator Release
   build succeeded with only known repository warnings.
5. The first device-interaction session was created by the parent task, but its
   key was invisible to the delegated device worker. No install, launch,
   interaction, or container mutation occurred. After releasing that stale
   session, the worker opened a uniquely named session and retained the already
   proven clean boundary. The tooling rule is now in `TOOLING_RUNBOOK.md`.
6. The clean iPadOS 27 run used scenario
   `swiftui.semantic-host.third-party-callback-adapter` and run ID
   `exp153-third-party-callback-20260916T103122Z`. Termination found no running
   app, uninstall succeeded, and `get_app_container` returned `ENOENT` before
   installation. Every frozen hash matched before and after capture.

### Local and mapper evidence

The strict semantic oracle passes 42/42 with no issues. The extra initial
`on-appear` and `task-immediate` action/Resource pairs prove the custom
container's initial Home exists before normal customer lifecycle work. The
adapter lifetime assertion also passes with
`registrations=1 active=1` after the complete flow.

| Occurrence | RUM view ID | Action/Resource pairs |
| --- | --- | ---: |
| Home 1 | `3c398d2d-3807-43c7-875b-80ac5043c450` | 3 |
| Detail 1 | `6309f5b5-639e-4a5d-817d-e8ebbfce8b0f` | 1 |
| Home 2 | `9ed3a875-ade6-437b-b8a1-3c28cf2504fc` | 1 |
| Sheet 1 | `2ac66453-fbde-4ea5-b80a-def1c8fd55b3` | 1 |
| Home 3 | `5720235c-701c-4d8c-ba2f-81b038562b78` | 3 |
| Full-screen cover 1 | `d3db662a-21fe-44b1-8d43-3dc7983f4498` | 1 |
| Home 4 | `8f5f382e-b4f4-45d8-8a12-dae07a850d7f` | 3 |

All four Home IDs are distinct. Both Sheet dismissal pairs use Home 3; both
cover dismissal pairs use Home 4. There is no automatic destination duplicate.
The app remained `Running`, the single-registration assertion passed, and two
uploads returned HTTP 202. Local logs contain no RUM error, crash, hang, or
disconnect; known simulator CoreTelephony, duplicate accessibility-class, and
post-upload NTP noise are non-product diagnostics.

### Backend evidence

RUM session `f2d2fb24-02d6-4bd9-9df9-ee353b899e69` contains:

- eight views: `ApplicationLaunch` plus the seven semantic occurrences above;
- 13 actions, 13 Resources, one long task, one session, and one vital;
- zero RUM error events and session error/crash counts of zero.

An independent session-ID query finds exactly eight views, all carrying the
current run ID. Grouping every action and Resource by phase and exact view UUID
produces 26 one-event buckets: `on-appear`, `task-immediate`, and
`semantic-home-1` use Home 1; each destination marker uses its named
occurrence; Sheet immediate/settled dismissal plus `semantic-home-3` use Home 3;
and cover immediate/settled dismissal plus `semantic-home-4` use Home 4.
Backend ownership therefore matches the mapper exactly.

### Validation and artifacts

- Focused EXP-153 selection: 7/7,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_11-24-43-+0100.xcresult`.
- Full native probe: 161/161,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_11-25-28-+0100.xcresult`.
- Xcode 27 iOS Simulator Release build: PASS.
- Repository SwiftLint: PASS across 713 source and 699 test files.
- Frozen-run logs:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 153 Third Party Callback Worker 1035-11_36_43_882-logs.txt`.
- The hierarchy, screenshot, and thumbnail share the same artifact directory and
  `EXP 153 Third Party Callback Worker 1035-11_36_43_882` prefix.

### Conclusion and next boundary

Accept a dedicated boundary adapter over a third-party navigator's reliable
synchronous accepted-state callback as a practical integration shape. It reuses
the SDK-owned publisher host, meets the one-integration-per-container migration
budget, and needs neither a new SDK input primitive nor navigation-library
conformance. This is adapter-author evidence, not a claim that opaque state can
be inferred exactly or approval of the experimental public spelling.

Custom/callback-driven parity is now closed. Next run two independent
Observation routers in two real native scene sessions as a serial simulator
discriminator, while keeping simultaneous visibility, focus handoff, genuine
disconnect, and interleaved concurrent-window acceptance in the physical-device
queue.

## EXP-154 — Serial two-native-scene Observation parity

Status: PASS after HARNESS FAIL · tests + mapper + backend

### Question

Can two real `WindowGroup` scene sessions independently mount the accepted
one-shot Observation adapter and produce scene-local navigation occurrences,
without using simultaneous visibility as a simulator precondition?

### Preconditions and implementation revision

The scenario
`swiftui.semantic-host.observation-router-two-scenes-serial` reuses the existing
per-window `@StateObject` router and `RUMNavigationHost`. It requires only
`.multipleScenes`, never `.simultaneousVisibleWindows`, and drives scene A fully
before opening and driving scene B. Every marker uses exact scene-context
handoff. The strict oracle contains 22 ordered timeline expectations plus three
completion conditions: no automatic-origin view and the final B/H2 action and
Resource.

Signed harness creation checkpoint `7fd6a891a` adds the scenario and its
contract test. The accepted correction removes one redundant readiness wait;
its signed checkpoint is `f92d72909`. Frozen accepted-source hashes are:

- complete probe `Sources` tree:
  `b79256e68cda076727d544204a0817f01f1cf64318e90a80bf86230da4e08bdf`;
- `ProbeScenarioCatalog.swift`:
  `254b545195e442a5b732cdeb7348e3e62848950b9a44a2da92ddb2afe08f09a5`;
- `ProbeScenarioRunnerTests.swift`:
  `c725f413cb73b44d7ae2bb3e3d59279e70591059f71181f19d761946ad32a669`.

The corrected focused selection passes 2/2, the full native probe passes
162/162, repository lint reports zero violations across 713 source and 699 test
files, and the Xcode 27 generic iOS Simulator Release build exits zero.

### Attempts

1. `exp154-observation-two-scenes-serial-20260916T110658Z`, session
   `f05982b5-5c6a-42f1-bdb3-78498c34c36f`: **HARNESS FAIL; no SDK or platform
   verdict**. The clean boundary passed and frozen source remained unchanged.
   Scene A completed H1 → D1 → fresh H2 with exact marker ownership, and scene B
   independently mounted H1. `open-window` then consumed and acknowledged B's
   `scene-ready` signal at sequence 51. The immediately following explicit
   `wait-for-scene-ready` began at sequence 53 and timed out because it required
   a second readiness event. The app remained running, three uploads returned
   HTTP 202, and no SDK error, crash, hang, or disconnect occurred. Backend
   intake contains the same partial launch/A-chain/B-H1 state. This attempt
   rejects the duplicated wait, not the Observation integration.
2. `exp154-observation-two-scenes-serial-fix-20260916T111921Z`, session
   `a6a5afd5-8089-4996-9805-ed62fb76927d`: **PASS**, 25/25 with no issues. The
   exact simulator boundary again terminated, uninstalled, and proved the data
   container absent before installation. Pre/post source hashes matched. Both
   real native scenes completed H1 → D1 → fresh H2; the app remained running,
   two uploads returned HTTP 202, and no SDK error, crash, hang, or disconnect
   appeared.

### Local and mapper evidence

Native scene session identifiers were:

- scene A: `9D24EB93-6C40-469F-99A6-17E865C1B013`;
- scene B: `992B021F-5EEE-4250-B379-8346637AD8E0`.

The mapper emitted seven distinct view IDs:

| Scene and occurrence | View ID | Marker owner |
| --- | --- | --- |
| ApplicationLaunch | `1d16a12f-e74c-4869-a25d-6a774be8e772` | none |
| A Home H1 | `d0ed5c8d-702a-4778-8f31-289fa595be62` | `observation-a-home-1` |
| A Detail D1 | `a64dbbf8-e7aa-4030-9368-6d34dde61f6b` | `observation-a-detail-1` |
| A Home H2 | `1c93cebb-9a14-4ad9-b3a6-17be3fbd1f4e` | `observation-a-home-2` |
| B Home H1 | `e6febef1-33f6-4910-96f9-257e3110255b` | `observation-b-home-1` |
| B Detail D1 | `80d694c0-00d9-494d-a9ef-06b160fad7a4` | `observation-b-detail-1` |
| B Home H2 | `e1168364-f1a5-4b1d-acb9-77f0252fd8f2` | `observation-b-home-2` |

Each listed marker produced exactly one action and one Resource on the same
listed view ID. Source context also retained the expected logical scene and
screen. The negative automatic-origin condition passed.

### Backend evidence

The run-ID query returns all 26 ingested events in one session:

- seven views;
- six actions;
- six Resources;
- five long tasks;
- one session; and
- one vital.

There is no error bucket. The session document reports `view_count=7`,
`action_count=6`, `resource_count=6`, `error_count=0`, and `crash_count=0`.
Every action name and Resource URL carries its A/B marker, and every pair owns
the same UUID as the mapper inventory above. All seven session views therefore
carry the accepted run ID; no stale restored-view document is hidden outside
the filtered result.

### Validation and artifacts

- Focused corrected selection: 2/2,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_12-12-41-+0100.xcresult`.
- Full native probe: 162/162,
  `Test-RUMNativeMultiSceneProbe-2026.09.16_12-13-02-+0100.xcresult`.
- Xcode 27 iOS Simulator Release build: PASS.
- Repository SwiftLint: PASS across 713 source and 699 test files.
- Accepted-run artifacts:
  `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/DeviceInteractionSynthesize/EXP 154 Observation Fix Worker 1221-12_21_06_082-*`.
- Harness-failure artifacts use the
  `EXP 154 Observation Two Scenes Worker 1209-12_10_42_692` prefix in the same
  action-artifact tree.

### Conclusion and next boundary

Accept serial two-native-scene parity for the Observation adapter. Two real
`UIScene` sessions independently mount the boundary and keep occurrence creation,
action attribution, and Resource attribution scene-local. The result closes the
simulator-capable native-scene discriminator and is sufficient to prepare the
navigation API review candidate.

It does not prove simultaneous visibility, focus or background handoff,
interleaved navigation, peer continuity, close/disconnect, final host removal,
or restoration. Those remain unchanged physical-device rows. The next
simulator-capable implementation slice is the bounded Operation
`.current(in:)` target prototype; the rest of explicit downstream targeting
follows that shared abstraction.

## EXP-155 — Explicit cross-scene Operation targeting

Status: PASS after three INVALID attempts · tests + mapper + backend

### Question

Can the first customer-shaped Operation target, `.current(in:)`, override an
intentionally wrong process representative on every step while preserving
application-wide `(name, operationKey)` identity, cross-scene start/end views,
parallel keys, reverse completion, and all existing inferred fallbacks?

### Preconditions and implementation revision

Signed SDK checkpoint `b0524bb5b` adds the iOS 27 experimental Swift
`RUMOperationViewTarget.current(in:)`, Debug-only Objective-C companion, and
start/succeed/fail overloads. UIKit is synchronously reduced to the scene's
persistent identifier; no scene or view object crosses onto the RUM queue and no
internal RUM UUID is exposed. The command keeps its explicit target separate from
independently captured inference, so resolution remains explicit live view,
inferred live view, last-proven snapshot, then compatibility representative.
Custom protocol conformers and the NOP path call the existing API exactly once.

Signed scenario checkpoint `3e567a494` adds
`operations.explicit-target.cross-scene-serial`. Every Operation call explicitly
targets A or B while the surrounding marker makes the opposite scene the process
representative. The first live attempts exposed that this non-navigation scenario
had inherited a stale occurrence-source probe which no longer seeded its accepted
destination. Signed correction `eb1dd2fdc` gives it explicit per-scene Home
boundaries and isolates the Operation discriminator from navigation behavior.

### Attempts

1. `exp155-operation-explicit-target-serial-20260916T121958Z` was **INVALID**.
   Despite terminate/uninstall/missing-container proof, the simulator window
   server restored only logical scene B. Step zero timed out waiting for A, so no
   Operation call ran.
2. `exp155-operation-explicit-target-serial-retry-20260916T122623Z` was
   **INVALID**. Scene A connected, but an immediate accessibility capture timed
   out and the scenario later found only launch/fallback/automatic host views,
   never `ProbeHomeView`. The capture could have interfered with the run.
3. `exp155-operation-explicit-target-serial-final-20260916T123613Z` repeated the
   same missing Home on a fresh simulator with no interaction capture before
   terminal. This conclusively identified a harness failure: the legacy
   occurrence wrapper did not publish accepted state. No Operation step ran and
   the app remained healthy.
4. Corrected run
   `exp155-operation-explicit-target-serial-manual-boundaries-20260916T124918Z`
   is **PASS**, 24/24 with no issues. The clean iPad mini simulator boundary
   terminated, uninstalled, and proved the container absent before install. Two
   uploads returned HTTP 202 and no SDK error or crash occurred.

### Local and mapper evidence

RUM session `bcb168fd-b5df-42ed-b416-b505a42e6e76` used two native scenes and
two distinct explicit Home owners:

| Scene | Native scene ID | RUM view ID |
| --- | --- | --- |
| A | `F0579DB0-CDF7-4B84-BAAB-34B11B4D6E57` | `b770b6fd-d818-4244-9ea4-1529e86d29b6` |
| B | `55AFA938-FA1E-41EB-9521-5BDA90B48B9A` | `44acd2ab-fdda-45a2-ac7e-a12b400be922` |

All eight invocation assertions passed: cross-success starts in A and succeeds
in B; cross-failure starts in A and fails with `error` in B; parallel alpha
starts in A, parallel beta starts in B, beta succeeds in B, then alpha succeeds
in A. Ten surrounding action/Resource pairs stayed on their exact A or B Home,
even though each Operation target opposed the current process representative.

### Backend evidence

Backend intake contains exactly eight raw `operation_step` vitals plus the
unrelated launch vital. Their ordered owners are A start/B end, A start/B failed
end, A alpha start, B beta start, B beta end, and A alpha end. The reducer emits
exactly four Operations:

- cross-success: A `b770b6fd…` → B `44acd2ab…`, success;
- cross-failure: A `b770b6fd…` → B `44acd2ab…`, failure `error`;
- parallel-alpha: A `b770b6fd…` → A `b770b6fd…`, success;
- parallel-beta: B `44acd2ab…` → B `44acd2ab…`, success.

The beta end raw step precedes alpha end. An independent session-ID query finds
exactly launch plus the two expected Home views, and every semantic view carries
the current run ID. Neither Home reports an error or crash.

### Validation and artifacts

- Focused scenario/driver selection: 3/3.
- Complete native probe: 163/163.
- Repository SwiftLint: zero violations across 713 source and 699 test files.
- `make api-surface-verify`: expected failure only for the already-unapproved
  navigation/manual prototypes and the new Swift/Objective-C Operation target
  symbols; reference baselines remain unchanged.
- Accepted screenshot:
  `/tmp/exp155-simulator-artifacts.GZcXvv/exp155-manual-boundaries-pass.png`.

### Conclusion and next boundary

Accept `.current(in:)` as an implementation and customer-call-site proof, not as
an approved stable API. It is sufficient to target every Operation step to the
requested scene without pinning the Operation to its origin and without exposing
internal identity. Exact names, availability, Objective-C spelling, and later
manual-key/controller target forms still require normal API review.

The explicit-target contract no longer needs simultaneous hardware. The original
inferred `EXP-131` discriminator remains in the physical-device queue because it
asks whether a genuinely concurrent call site supplies trustworthy scene context
without an explicit target.

## EXP-156 — Physical iPad automation preflight

Status: PASS · physical-device tooling

### Question

Can Xcode 27 automation establish a stable, signed physical-iPad runtime before
running the unchanged `operations.cross-scene.lifecycle` (`EXP-131`) scenario?

### Preconditions and implementation revision

Xcode listed `Miguel's iPad` as an eligible arm64e iPadOS 27.0 destination. The
device identifies as iPad (A16), model `iPad15,7`, OS build `24A435`, hardware
UDID `00008120-000A2DDA22880032`, and CoreDevice identifier
`355D50F8-824E-536A-943D-6448ED3554E8`. It was paired with Developer Mode
enabled. No source or harness revision was made for this attempt.

The Xcode interaction-session APIs exposed simulators only and rejected the
physical iPad by display name, hardware UDID, and CoreDevice identifier. The
attempt therefore moved to Xcode 27's `xcodebuild` plus `devicectl` physical
workflow without changing the scenario.

### Attempts

The non-mutating preflight could not prove a stable connection:

1. one lock-state query succeeded and reported that the device had been unlocked
   since boot, but did not prove that it was currently awake and unlocked;
2. process inventory failed with CoreDevice error 4000 because the device
   disconnected immediately after connecting;
3. app inventory failed with `Network.NWError 60` (`Operation timed out`);
4. a reconnect allowed another lock-state query, but process inventory again
   failed with error 4000;
5. the final inventory reported the paired device as disconnected and reachable
   only through the unstable local-network transport.

A second fresh preflight stopped even earlier at the mandatory transport gate:
`devicectl list devices` again reported `available (paired)` while structured
state remained `connection.state="disconnected"` over `localNetwork`. A separate
USB inventory contained no matching iPad. No app/process query or mutating call
was attempted on this retry.

A third attempt established a stable wired connection. Structured CoreDevice
state reported `wired`; IORegistry independently exposed the exact USB serial.
Lock-state, app inventory, process inventory, and repeated app inventory all
succeeded without error 4000, `NWError 60`, disconnect, or timeout. The exact
bundle was absent, uninstall exited zero, and post-uninstall app inventory
remained empty. The physical build initially selected `arm64e` and failed against
local `arm64` Swift package modules; the unchanged retry targeting exact-device
`arm64` succeeded.

The first resulting app was unsigned. Effective settings remained
`CODE_SIGNING_ALLOWED=NO`, `CODE_SIGNING_REQUIRED=NO`, and
`CODE_SIGN_IDENTITY=iPhone Developer`, with no development team. A restricted
worker-context query initially reported zero valid code-signing identities. A
controlled install failed with CoreDevice
error 3002, `ApplicationVerificationFailed`, and `0xe800801c` (`No code signature
found`). Final exact app and process inventories remained empty. No launch or
`EXP-131` step occurred.

That identity conclusion was a sandbox false negative, not a host prerequisite.
The login-keychain context reports two valid Apple Development identities for
the same developer. The installed wildcard development profile belongs to team
`JKFCB4CN7C`, includes this physical iPad, and embeds one of those two identities
(`EEA12DB943AB73979621A65F9BCA5BCB4452C567`). The other valid identity is not
embedded and is therefore not eligible for this profile.

Three non-source signing paths were then characterized:

1. automatic signing with provisioning updates reached Xcode's account gate and
   failed because no developer account is configured in Xcode;
2. manual or automatic scheme-wide identity/profile overrides leaked into Swift
   package targets, which reject provisioning settings or conflict with their
   automatic signing mode;
3. a fresh exact-device `arm64` unsigned build was copied to a temporary
   artifact, given the existing profile and minimal concrete entitlements, and
   signed depth-first with the exact matching identity. Authoritative user-
   context `codesign --verify --strict --deep` exits zero for the result. The
   outer app and both nested dylibs have the Apple Development authority chain
   and team `JKFCB4CN7C`; profile/certificate/device/bundle compatibility also
   passes.

After the iPad was unlocked, repeated wired app/process inventories and the
fresh clean boundary passed. The verified copied artifact installed successfully
and launched with command-line scenario arguments. This closes the transport,
signing, install, and launch preconditions; the scenario-level result belongs to
`EXP-157` below.

### Local and mapper evidence

The unsigned attempt has no run ID, local oracle, native scene ID, RUM view ID,
upload, screenshot, or mapper result because that app never ran. The later
signed launch evidence is separated into `EXP-157`. Preserved tooling artifacts:

`/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/ActionArtifacts/default/XcodeListRunDestinations/CC8DD433-A499-451F-96F4-84986C115033.txt`

- successful physical build:
  `/tmp/exp131-physical-cTfYfh/build-arm64.xcresult`;
- initial architecture-mismatch build:
  `/tmp/exp131-physical-cTfYfh/build.xcresult`;
- exact unsigned-install rejection:
  `/tmp/exp131-physical-cTfYfh/install-unsigned.log`;
- unsigned app:
  `/tmp/exp131-physical-cTfYfh/DerivedData/Build/Products/Debug-iphoneos/RUMNativeMultiSceneProbe.app`;
- automatic-signing failure:
  `/tmp/exp131-physical-signed-cKqhtP/build-signed.xcresult`;
- scheme-wide manual and exact-identity override failures:
  `/tmp/exp131-physical-manual-tmBUgi/`;
- fresh unsigned build and copied signed artifact:
  `/tmp/exp131-physical-postsign-GUoydv/build-unsigned.xcresult` and
  `/tmp/exp131-physical-postsign-GUoydv/signed/RUMNativeMultiSceneProbe.app`;
- authoritative signing verification:
  `/tmp/exp131-physical-postsign-GUoydv/codesign-strict-deep-verify.log`,
  `/tmp/exp131-physical-postsign-GUoydv/codesign-details-and-entitlements.log`,
  `/tmp/exp131-physical-postsign-GUoydv/nested-codesign-verification.log`, and
  `/tmp/exp131-physical-postsign-GUoydv/profile-compatibility.log`.

### Backend evidence

The first unsigned attempt produced none. The later signed launch created RUM
session `985ba953-122a-4250-992b-1f0f6b6aef7f`; its scenario-level events are
recorded in `EXP-157`. They are not evidence about the signing workflow itself.

### Validation and artifacts

For the unsigned attempt, the clean uninstall was the only successful app
mutation and the rejected install left neither app nor process on the iPad. The
later signed attempt passed another clean boundary, installed, launched, and
terminated successfully. It remains installed but not running for the prepared
retry, and no `devicectl` console or capture process remains. The successful
signed build used source HEAD `b450d665c`; the protected project and local
xcconfig changes were not edited or staged.

### Conclusion and next boundary

The transport, signing, install, and launch blockers are closed. Physical RUM
scenarios can use the verified temporary-copy signing workflow without changing
the project. The earlier zero-identity statement must not be reused: credential
discovery must run in the actual login-keychain context. Scenario or window-
topology failures are separate runtime evidence and must not reopen this tooling
gate.

## EXP-157 — Physical inferred Operation retry

Status: PASS · physical Operations + backend; INCONCLUSIVE · simultaneous visibility

### Question

On the physical iPad, does `operations.cross-scene.lifecycle` create distinct A
and B Home views, execute all eight inferred Operation steps, and reduce them to
the required A→B, A→A, and B→B ownership?

### Attempt 1

The signed app launched with run
`exp131-physical-20260916T143336Z` and RUM session
`985ba953-122a-4250-992b-1f0f6b6aef7f`. Scene A registered as native scene
`8A14B612-6297-492D-8A05-C72EBA9C869E` at 1180×820 regular/regular. The clean
command-line manifest selected the intended scenario with all 22 steps and both
required capabilities.

The run stopped at step 1 with zero matched expectations: it timed out waiting
for `rum-view:home#1`. RUM emitted ApplicationLaunch view
`51de3458-6bbd-49e3-a026-9947d95ea64d`, automatic fallback
`c5127f1c-942a-43c5-8788-0741a3a3a2f8`, and automatic
`NavigationStackHostingController<AnyView>` view
`2dc27a28-5f79-4d6d-b1f1-50895ef1f082`, but no explicit semantic Home. The
driver therefore never opened scene B. All eight Operation calls and all of
their marker pairs have an exact executed count of zero. No crash, hang, SDK
error, or device disconnect occurred.

This is not an inferred-Operation verdict. The scenario performed no navigation
but still used the legacy navigation-occurrence source for its Home setup. The
same irrelevant coupling had already invalidated early `EXP-155` attempts.

### Harness correction

`operations.cross-scene.lifecycle` now uses explicit per-scene Home boundaries
while retaining:

- the simultaneous-visible-windows capability;
- the exact A/B open and marker sequence;
- all eight inferred, source-less Operation calls;
- four application-wide `(name, operationKey)` identities;
- beta-before-alpha completion; and
- the unchanged local and backend ownership oracle.

The two focused catalog tests pass 2/2 and the complete Xcode 27 probe suite
passes 163/163. A fresh exact-device `arm64` app was built from source hash
`75e9762ded6dbfa905f4637b9d96633c809cc154ed1aa98ca7c30d6acbead99a`, copied,
signed with the already validated identity/profile and minimal entitlements,
and passed nested plus strict/deep verification. The signed harness correction
is commit `4695d092c` and the retry artifact is at
`/tmp/exp131-physical-manual-retry-72Llwb/`.

### Attempt 2: corrected physical run

After a fresh wired/unlocked inventory, exact terminate, uninstall, and absence
check, the corrected artifact installed and launched as run
`exp131-physical-manual-20260916T160435Z`. Its RUM session is
`1dd8d491-19a4-4b67-bfa5-13df5b2a73a6`.

The local oracle passes 24/24 with no issues. It established two real native
scene sessions and distinct Home views:

| Scene | Native scene session | Home view |
| --- | --- | --- |
| A | `8A14B612-6297-492D-8A05-C72EBA9C869E` | `8bdba083-559c-4254-a97f-f08d960bf644` |
| B | `D9BA1A0F-059E-4B29-B3A7-B530DA6FAB68` | `1f5c426f-b4dc-4328-a645-265294f86a7a` |

All eight inferred Operation calls executed in the required order:

| Sequence | Step | Scene | Instance |
| ---: | --- | --- | --- |
| 48 | start | A | cross-success |
| 56 | succeed | B | cross-success |
| 64 | start | A | cross-failure |
| 72 | fail (`error`) | B | cross-failure |
| 80 | start | A | parallel-alpha |
| 88 | start | B | parallel-beta |
| 98 | succeed | B | parallel-beta |
| 108 | succeed | A | parallel-alpha |

All ten surrounding marker action/Resource pairs own the intended Home: A owns
home-A, both cross-operation starts, and alpha start/end; B owns home-B, both
cross-operation ends, and beta start/end. No SDK error, fatal, crash, hang,
disconnect, or connection invalidation was observed.

### Backend evidence

The unique run-ID query returns exactly eight `operation_step` events plus the
unrelated app-launch vital. Every raw step has the expected scene-local view.
The four reduced Operations are exact:

| Instance | Status | Start view | End view |
| --- | --- | --- | --- |
| cross-success | success | A `8bdba083…` | B `1f5c426f…` |
| cross-failure | failure (`error`) | A `8bdba083…` | B `1f5c426f…` |
| parallel-alpha | success | A `8bdba083…` | A `8bdba083…` |
| parallel-beta | success | B `1f5c426f…` | B `1f5c426f…` |

Raw beta completion precedes alpha completion. No extra or orphan Operation is
present. Aggregated run-ID intake contains three views (ApplicationLaunch plus
A/B Home), 16 actions, 16 Resources, nine vitals, four Operations, and one
session, with no error or crash bucket.

### Topology qualification

This run proves two native scenes and correct cross-scene inferred attribution,
but not simultaneous on-screen presentation. Both scene-ready geometries were
full-screen 1180×820. At completion B was foreground-active, A was background,
and the screenshot shows only B full-screen. The declared
`simultaneous-visible-windows` capability is therefore not itself evidence that
the OS arranged both windows. A human must arrange Stage Manager or split view
before that visual/topology subcondition can close.

### Artifacts and next boundary

Attempt 1 artifacts are under `/tmp/exp131-physical-postsign-GUoydv/`. The
standalone `devicectl --log-output` file retained launcher status but not the
bridged application stdout; the complete JSONL transcript remains in task
history item `exec-672f4207-a781-4abd-811a-e9d8c4234760`. The post-terminal
screenshot is black because the display slept after termination and is not UI
evidence.

Attempt 2 artifacts are under `/tmp/exp131-physical-manual-retry-72Llwb/`.
The full console is
`console-exp131-physical-manual-20260916T160435Z.log`; the terminal screenshot
is `exp131-physical-manual-20260916T160435Z-terminal.png`. The exact final probe
process was terminated, the app remains installed, and no host capture process
remains.

The inferred Operation contract is accepted. Keep only simultaneous visibility
as a human-arranged follow-up, and continue the automatable physical queue with
the same-key scene-targeted manual-view scenario when hardware is available.

## EXP-158 — Explicit scene-targeted one-shot action

Status: PASS · tests + mapper + backend

### Question

Can the reusable experimental view target select the current view of a specific
`UIWindowScene` for a one-shot RUM action, even when another scene is the process
representative, while leaving the existing source-less last-interacted behavior
unchanged?

### Preconditions and implementation revision

The Operation-only experimental `RUMOperationViewTarget` was generalized to
`RUMViewTarget`. The original Swift spelling remains as an experimental type
alias so existing prototype call sites continue to compile. The Debug-only
Objective-C prototype is now `DDRUMViewTarget`.

An iOS 27 `@MainActor` SPI overload adds a `view:` target to one-shot
`addAction`. The command carries explicit and inferred candidates separately.
Session resolution uses a live explicit scene target when possible; if that
scene cannot resolve to a live view, it preserves the independently inferred
target. Custom `RUMMonitorProtocol` implementations and the NOP monitor fall
back to the legacy method exactly once. Existing `addAction`, `startAction`, and
`stopAction` behavior is unchanged; this experiment does not yet propose
scene-targeted long-running actions.

### Local and mapper evidence

Scenario `actions.explicit-target.cross-scene-serial` ran on the iOS 27 iPad
mini simulator with run ID
`exp158-sim-66f922ee-19a3-4941-b8ca-c518216e6b0d`. It created two native
scene sessions serially, made B representative, and passed its 9/9 oracle. RUM
session `f67c75b2-e839-4701-a283-7e4355682b6a` used distinct Home views:

| Scene | Home view |
| --- | --- |
| A | `919e0c80-8c8d-459c-a496-847245c9033c` |
| B | `aa145a43-2b88-4ce9-ad67-eb268386506f` |

The decisive events were:

| Marker | Call-site scene | Expected and actual owner |
| --- | --- | --- |
| `explicit-action-representative-b` | B | B |
| `explicit-action-a-overrides-b` | A with explicit A | A |
| `explicit-action-b-overrides-a` | B with explicit B | B |
| `explicit-action-legacy-fallback-to-b` | A with no explicit target | B, the last-interacted representative |

The final legacy Resource also encoded scene A in its probe URL while retaining
B ownership, confirming that the action addition did not silently change the
source-less compatibility rule. The outgoing upload returned HTTP 202.

Focused API, NOP/custom-handler, monitor, and session-scope regressions pass.
The complete probe suite passes 164/164. The complete DatadogRUM Xcode 27 suite
passes 1,255 test cases with zero failures; the result bundle reports 1,291
device/configuration invocations because parameterized tests expand there. The
Xcode 27 Release simulator build succeeds for both architectures, and the
Objective-C API smoke suite passes 8/8. Repository lint reports zero violations.

### Backend evidence

The exact run-ID query returns 30 RUM events. The explicit A action owns A; the
explicit B action owns B; and both the legacy source-A action and final Resource
own representative B. The session contains three views (ApplicationLaunch plus
A and B Home), ten actions, eight Resources, seven long tasks, one vital, and one
session event, with no error or crash bucket.

### API and compatibility result

The experimental `RUMViewTarget.current(in:)` concept is useful beyond
Operations and can share one internal scene-current resolver. Explicit and
inferred candidates must remain separate so an unavailable explicit scene can
fall through to the already inferred representative instead of dropping the
action. A successfully resolved explicit user action becomes the new
last-interacted representative through the existing interaction path.

This result accepts the engine and customer-shaped one-shot action prototype,
not stable API names or availability. It does not cover `startAction`/`stopAction`,
manual-key or controller target forms, simultaneous on-screen windows, or other
downstream signal families. Those remain separate slices.

### Validation and artifacts

The runtime used simulator `92C9918B-FEC6-4440-9D6D-8E09475950BB` after a
proven terminate, uninstall, and absent-container boundary. The complete probe
result contains 164 passes. The full RUM result is
`/tmp/EXP158RUMFull-66F922EE.xcresult`; the Objective-C smoke result is
`/tmp/EXP158ObjC2-66F922EE.xcresult`. API-surface verification fails only because
the branch intentionally contains unapproved experimental navigation, manual
view, Operation, and action declarations; no baseline was updated and the
generated diff contains no unrelated surface.

### Conclusion and next boundary

Explicit scene targeting for one-shot actions is accepted on the simulator and
in backend intake without degrading legacy last-interacted attribution. Continue
the simulator-capable target matrix one signal family at a time. Resume
simultaneous-window, real disconnect, gesture, and same-key manual-view rows only
when capable physical hardware is available.

## EXP-159 — Explicit scene-targeted long-running actions

Status: PASS · bounded live-view API batch, tests + mapper + backend

### Question

Can the shared scene-current target select long-running action start and stop
independently while keeping equally named A/B actions isolated, preserving
legacy representative fallback, and leaving existing action lifetime unchanged?

### Preconditions and implementation revision

Definition recorded before implementation. Restart HEAD is `670843f9d`, the
signed documentation-only successor of `c2d1f1f9a`; both signatures verify.
At definition time, accepted runtime and suite results remained EXP-158. Neither that
experiment nor its tests were rerun for the restart. The two interrupted audits
from the previous session produced no accepted conclusion and were restarted
from committed source.

The [current-source audit](../ASSESSMENT.md#downstream-routing-audit-for-exp-159)
separates view-current actions/errors/mutations from Resource-key completion.
Long-running actions are the smallest useful extension because they already use
the action bridge's command and scope machinery; no new owner registry or
cross-module protocol is needed.

Implementation boundary:

- Add iOS 27 `@MainActor` experimental `startAction(..., view:)` and
  `stopAction(..., view:)` extension overloads and Debug-only Objective-C peers.
- Extend the internal capability/bridge; custom and NOP monitors receive their
  existing start/stop method exactly once. Add no public protocol requirement.
- Carry explicit and inferred candidates independently on start/stop commands;
  generalize only action target resolution before representative selection.
- Select a live explicit view even if its action slot is empty. Only a missing
  live view triggers inferred fallback; absence of an action is not permission
  to stop another scene's action.
- Preserve one action slot per view, duplicate-start handling, existing stop
  metadata, timeout/resource rules, navigation completion, and automatic scroll
  routing. `.current(in:)` resolves at processing time and is not a frozen action
  handle. A stop after navigation addresses the new current view, while the
  outgoing action has already ended on its original view.
- Leave Resource/error/view-mutation APIs, Operations, network formats, generated
  models, API baselines, swizzles, build scripts, and both protected paths alone.

### Defined deterministic gates

1. Explicit A/B overrides contradictory exact-view and scene-only inference at
   both start and stop, emits once per action, and updates the representative
   through the existing user-interaction path.
2. Equal action names coexist on distinct A/B views; B completes before A and
   each receives only its own stop metadata. A stop name is not a lookup key.
3. A second explicit B stop with an empty B action slot cannot close A's action.
4. Unresolvable explicit scenes preserve exact/scene inference; absent handoff
   preserves source-less representative fallback at both calls.
5. Navigation ends only the outgoing scene's action; targeting the new current
   view does not revive the old action or stop the peer. Duplicate starts and
   cross-scene expiry retain their existing semantics and never copy peer stop
   attributes into an expired action.
6. Existing one-shot/inferred/automatic-scroll behavior and exactly-once
   custom/NOP Swift plus Objective-C forwarding remain compatible. Debug and
   Release compile; API verification must show only expected prototype additions
   without changing either baseline.

### Defined simulator discriminator

Proposed named scenario:
`actions.explicit-target.long-running-cross-scene-serial`.
Use the existing explicit non-navigating Home fixture, serial native scene
creation, and `.multipleScenes`; no activation loop or simultaneous-visibility
requirement. Keep call-site source labels distinct from mapper-observed owners.

| Phase | Required observation |
| --- | --- |
| Mount A and B | Distinct native scene identifiers and semantic Home view UUIDs |
| Represent B, explicitly start A | A action opens while B is the prior representative |
| Explicitly start B | B action opens while A is representative; both use the same start name |
| Represent A, explicitly stop B | Exactly one B-owned completion with B stop metadata |
| Stop B again | No second B completion and no premature A completion |
| Explicitly stop A | Exactly one A-owned completion, after B, with A metadata |
| Represent B, invoke legacy start/stop from source A | Exactly one B-owned legacy completion |
| End | No duplicate tested actions, no unintended views, no RUM errors/crashes |

Use distinct final names to make backend queries unambiguous while keeping the
start names equal. Deterministic tests must prove isolation directly and reject
adversarial missing/duplicate/swapped/early-stop evidence. Runtime must finish
within the existing action timeout; a timeout-induced completion cannot pass as
an explicit stop. Establish readiness before starting actions, use signal-driven
steps, and require stop attributes in the mapper and backend rather than merely
counting callbacks.

### Restart tooling evidence

Fresh discovery on 2026-09-17:

- Explicit toolchain: `/Applications/Xcode_27.app`, Xcode 27.0 (`27A266a`), also
  selected by `xcode-select`.
- Working `XcodeListWorkspaces`, scheme, and destination calls resolve SDK
  `workspace-DLY6WWrRlF` and probe `workspace-f8CNLBYwfd`. These are only valid
  for this current connection; no remembered interaction key was used.
- Probe active destination: iPad mini (A17 Pro), iOS 27.0. Fresh `simctl` and
  `devicectl` inventories identify available booted simulator
  `92C9918B-FEC6-4440-9D6D-8E09475950BB`. The physical iPad is unavailable, so
  EXP-129 and the physical queue stay paused.
- Datadog's available connector has no identity tool. After skill discovery, a
  fresh bounded aggregate for
  `@context.probe.run_id:resume-auth-preflight-20260917-670843f9d` succeeds with
  zero buckets and no authentication error. This proves authenticated read
  access only, not an experiment's ingestion or organization identity.
- `git verify-commit` initially could not create a temporary file in the read-only
  sandbox; the permitted retry verifies both signatures. The project-file hash,
  protected file metadata, and index entries were captured without opening or
  logging the local xcconfig. Its actual index state is `A `; the project is
  ` M`. Keep those exact states, rather than an older runbook's historical `AM`.

### Attempts, validation, and artifacts

No EXP-159 implementation, test invocation, runtime attempt, backend session,
or acceptance artifact exists at definition time. No frozen archive was loaded.
Subsequent attempts must use fresh run IDs and artifact paths, frozen source
fingerprints, exact clean-install proof, and an independent session/view/action
backend inventory. Record failed, invalid, and inconclusive attempts separately.

### Execution update and bounded driver correction (2026-09-17)

Signed SDK `24212cb48` and harness `cdd2239df` implement the defined slice.
Focused SDK checks pass 147/147; full RUM passes 1,260/1,260; a fresh probe
build passes 166/166; Release and repository lint pass. The initial selective
probe invocation returned 164 passes and two new tests with no result, so it is
INVALID as current-build evidence. Fresh derived data closes that tooling issue.

First runtime `exp159-actions-long-running-20260917T000713Z-e68dd58b`, session
`6338f588-63e4-4a2e-a2af-793f3115a93f`, is INCONCLUSIVE for concurrent live
views; preserve raw oracle FAIL. Native A went to background after both starts;
its view closed and its action completed after 13.8ms without the requested stop
metadata. The later A representative marker was absent and B timed out. This
normal lifecycle transition invalidates the required concurrent-action setup;
it is not evidence of an explicit-stop routing defect. No early UI synthesis
occurred, and all 459 frozen source files were unchanged.

Before the next attempt, amend only the driver: establish both native Home
readiness barriers, check both mapper-observed views are still active, then
submit the same ordered representative/start/stop/empty-stop/legacy calls in
one synchronous main-actor batch. This removes suspension inside the API-only
discriminator. Do not modify the exact owner/count/final-name/stop-phase oracle
or extend action lifetime through background. An unavailable live-view pair is
INCONCLUSIVE. The result can prove API routing while both views are live; it
cannot close a simultaneous-visible or long-duration cross-window hardware gate.

### Accepted result

Signed correction `4611d5f0b` passes the full probe again (166/166).
Run `exp159-actions-batch-20260917T001427Z-1114a1cd` passes 15/15 with exact
native A/B identities. Session `7b500739-d8a9-4625-b021-7a6f47d03f05` has
ApplicationLaunch plus exactly two semantic Home views, each carrying the new
run ID in an independent session-only backend inventory.

| Completion | Source | Owner view | Action UUID | Duration |
| --- | --- | --- | --- | --- |
| `long-running-finished-b` | B | `07d837d4-5dc5-482e-a808-dd891b39e790` | `7cba6475-6323-4062-9e2c-305e2bacfb49` | 200,987 ns |
| `long-running-finished-a` | A | `dfc37e9e-329b-4be2-976e-eb2ac373c33a` | `a953c5a4-fffe-4a38-a114-0590846e3a73` | 764,012 ns |
| `long-running-legacy-finished-b` | A | B above | `30ee7bcf-487a-4977-aca0-0e8682aeac2b` | 40,054 ns |

Mapper sequences are 67, 71, and 75. Backend stop uptimes are
613745.1829328333, 613745.1833138333, and 613745.18356575 respectively.
All final names and stop-phase attributes match; all seven controlled action
phases occur exactly once, with no early/empty-slot completion. Intake totals:
13 actions, 10 Resources, six long tasks, three views, one session, one vital,
zero RUM errors or crash-count matches. This does not validate those incidental
long tasks/vitals as separate telemetry gates.

Full SDK tests: 1,260/1,260 (1,296 expanded invocations); focused: 147/147;
probe: 166/166; Objective-C: 8/8; Release simulator build: PASS; lint: zero
violations in 713 source and 699 test files. API-surface verification still
fails on the expected experimental additions: exactly 46 Swift and ten
Objective-C added lines for the existing navigation/manual/target surfaces,
including these two overloads. The diff was reviewed; neither baseline changed.

All 459 source files remain frozen through runtime and backend verification:
`9616c1906943627e50b0a66eb7c27c2d2b3f94b733f3f9f9e4fae09067083c8f`.
Runtime artifacts:
`/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/exp159-corrected-runtime-cbbch993/summary.json`.
Tests, Release/API logs, and sanitized backend evidence:
`/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/exp159-validation-c0q55of5/`.
The interaction session is closed. The stdio-only console filter initially
omitted assertion sequence 49 because OSLog embedded its complete JSON object
midline. The original capture is preserved; decoding that exact object from raw
logs recovers contiguous sequences 1–94 without inferred content. This is a
capture issue, not an oracle or ownership change.
Both user-owned paths retain their original metadata/index state. No push.

### Conclusion and next boundary

Accept targeted long-running action API routing while two tracked views remain
live, with unchanged lifetime, fallback, and custom/NOP behavior. Native
background may correctly end an outgoing action; long-duration simultaneous
window use remains a hardware gate. Next, implement the requested finite release
checklist, early compatibility/performance baselines, incremental responsibility
review, and repeatable acceptance workflow before expanding telemetry APIs.
Work is paused at the user's request after the EXP-159 acceptance commit; none
of those follow-up implementations has started.

## EXP-160 — Early compatibility and performance baseline

Status: MIXED · C01/C02/C04/C05/P01/P04 PASS; P02/P03 FAIL; C03 INCONCLUSIVE; C06 ENVIRONMENT BLOCKED

### Gate and question

C01–C06 and P01–P04: does the accepted multi-scene architecture preserve ordinary
automatic/manual/legacy/custom/NOP behavior and stay within explicit dispatch,
allocation, lifetime and reentrancy budgets before any further expansion?
The frozen protocol, exact baseline/candidate and predeclared thresholds are in
[BASELINES.md](../BASELINES.md). Use the same fixture in isolated checkouts,
Release measurements and ABBA order; retain failures and all raw samples.

### Environment and decisive oracle

iOS 27 and 26.5 simulators are available. Minimum iOS 15 runtime is unavailable;
compile-only evidence cannot close C06. Physical iPad remains unavailable.
Each gate closes independently only when its defined measurement/test passes.
No fallback from allocation churn to resident-memory figures is permitted.

### Implementation boundary

Add an isolated baseline fixture and repeatable runner, with no production SDK
change, project-file mutation, credentials, stable API change or new dependency.
Record benchmark limitations and any regression before selecting a fix.

### Execution and results

The [durable result](../Results/EXP-160-baseline.json) and separate numeric
sample artifact preserve revisions, source/fixture/binary hashes, commands,
24 final ABBA launches, ordinary-app cases and focused lifecycle/reentrancy runs.
The original protocol hash remains unchanged. Both pinned SDKs built Release
with minimum deployment15; no production SDK source was changed.

| Gate | Result |
| --- | --- |
| C01/C02/C04/C05 | PASS: ordinary automatic/manual scene apps, custom/NOP and older26.5 behavior preserve exact occurrence/action/Resource relationships. |
| C03 | INCONCLUSIVE overall:26.5 legacy navigation plus actual OS background/foreground auto/manual pairs pass. SDK27-linked no-manifest apps trap in UIKit in both arms on27.0; preserved UUID-matched crash excerpts identify the platform boundary. |
| C06 | ENVIRONMENT BLOCKED: no15 runtime. Minimum-target compilation is separate proof. |
| P01 | PASS: filtered-touch median166→333ns on both systems; enabled handoff median2041ns(27) /2000ns(26.5), added1708/1667ns versus5000ns budget. All recorded ordinary median/p95 comparisons pass. |
| P02 | FAIL: ordinary workload0 allocations in both arms, but enabled handoff adds3 allocations and416 requested bytes/event versus frozen1 allocation/64-byte budget on both systems. Calibrated heap churn, not live heap. |
| P03 | FAIL: each disconnect/activity registry retains220 entries after20 warm-up+200 cycles; controller weak survivors0. First100 live-heap growth31,072–47,472bytes and second10015,616bytes meet byte budgets, but registry ownership fails. Mounted SwiftUI teardown remains unproven. |
| P04 | PASS: separate focused variant on each runtime executes90,000 exact context/flag boundary checks,20,000 original dispatches and10,000 caught throws with0 errors. ABBA timing binaries remain unchanged. |

Invalid allocation-fixture attempts are preserved: first access to thread-local
counter storage recursively allocated through the logger and crashed both arms.
The allocation-free counter replacement calibrated exactly3 operations/165 bytes
before measurement. This fixture failure is not attributed to the SDK. The first
legacy evaluator incorrectly assumed no scene manifest implied connectedScenes=0;
actual26.5 lifecycle proof uses the AppDelegate/manifest and real OS callbacks.
The first reentrancy smoke checked scene labels only; the stronger full-context
variant closes P04 without repeating or replacing timed samples.

### Conclusion and repair admission

Six early baseline gates close. P02/P03 block expansion and are scheduled with
D04 shared handoff ownership and D03/D08 lifetime/epoch repairs respectively.
Do not raise thresholds. Keep physical performance, real OS scene teardown,
minimum-supported runtime and named-core isolation as separate requirements.
The [review triage](../REVIEW_TRIAGE.md) records the finite next repair order.

## EXP-161 — Automated acceptance pipeline

Status: PASS · A01; two tooling-invalid attempts preserved

### Gate and question

A01: can one run connect tool/device/auth preflight, clean install, immutable
source and build identity, an existing named scenario, topology, the strict
semantic oracle and independent backend ownership checks, then emit one compact
durable result without hand-copying verdicts between documents?

### Decisive oracle and scope

First supported scenario: `actions.explicit-target.long-running-cross-scene-serial`.
This reuses an accepted fixture to validate the workflow; it is not another
action API experiment or a stronger simultaneous-visibility claim. Require both
native scene identities, all 15 original assertions, exact final names/owners/
stop metadata and B-before-A order, no early/empty-slot completion, and legacy
source-A→representative-B. Independent session-only backend view inventory must
reject restored run identifiers hidden by a run-filtered query.

The runner must fail closed for stale fixture/build identity, source mutation,
missing or duplicate signals, consumed readiness reused by another step, wrong
owners/final metadata, reused occurrence IDs, and critical evidence captured
after its required boundary. Negative controls retain the EXP-142 lesson: correct
view inventories and callback totals alone do not prove downstream ownership.
Preserve raw mixed console lines and recover only complete JSON objects; never
infer gaps. Backend waiting has a deadline and incomplete intake cannot pass.

### Environment and implementation boundary

Simulator plus the authenticated Datadog connector. The runner may exchange
structured requests/results with an external connector bridge; no credentials
are stored or extracted. One process retains run identity and state through
backend completion. Unsupported scenarios or physical/human requirements must
be rejected explicitly, not treated as generic acceptance. Produce a sanitized
summary with revision/build hashes, topology, each gate result, query provenance,
artifact references, failures and rerun instructions. Preserve protected paths.

### Attempts

1. `exp161-20260917T005359Z-91f9789deb9c`: authenticated preflight, fresh
   166/166 tests and clean install passed; app terminal PASS15/15 with86 contiguous
   signals. Runner emitted FAIL at `no Home snapshot before critical batch` because
   it incorrectly required an occurrence field absent from native mapper JSON.
   Classified INVALID oracle construction, not SDK failure; raw verdict preserved.
   Session `6f8704fc-5be9-4d8a-bd83-41491cb17339`. No backend acceptance was reached.
   Artifacts: `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/exp161-acceptance-2o58j5wp/run`.
   Correction resolves the first Home UUID per scene and exact native source,
   pins the complete scenario contract, and adds real-envelope negative controls.
2. `exp161-20260917T010538Z-c79b2ab1545b`: fresh166/166, clean install and local
   15/15 passed. The connector driver treated a yielded short helper command as
   failure because its exit_code was not available yet. Session
   `470ffbaf-0591-4e78-965a-c5d229b8d8d1`. Classified INVALID orchestration; a bound
   failed connector response ends the runner without manufacturing backend PASS.
   Artifacts: `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/exp161-acceptance-7ko1qcj2/run`.
   The helper must collect yielded process output before reading its exit code;
   the long-running acceptance process stays asynchronous so the bridge can work.

3. `exp161-20260917T011012Z-9bb629c7c630`: complete automatic PASS in142seconds.
   Fresh authenticated read,166/166 tests, frozen sources/build, clean install,
   local15/15 with86 contiguous signals, then7 exact backend actions,3 independent
   session-only views and0 errors/crashes. Session
   `4190b7c5-fbfe-4a7b-8a27-2daeadd485e1`. No manual bridge response or source edit.
   App terminated cleanly; all458 frozen paths and protected workspace state
   remained unchanged. Artifact root:
   `/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/exp161-acceptance-qw2b9iu8/run`.

### Accepted identity and durable evidence

Signed SDK HEAD `af63657f08dbecb66e7c3ae97ed53fc8f7b065b9`;
source fingerprint `91454d5e699a86d13514aaa3c87796e87b0a95781a7c6a5591c1256dc4406d08`;
executable SHA256 `35df8f6e6c6f7c40af40478d0174e1f9df98b9970e11e5fcea4a373ad307671f`.
[Durable PASS](../Results/acceptance/exp161-20260917T011012Z-9bb629c7c630.json)
contains exact local/backend owners, final names, stop times/durations and build
identity. The two prior summaries retain their original raw FAIL/INCONCLUSIVE
verdicts; the experiment classifies both as tooling-invalid.24 Python oracle
controls and3 Node helper-polling regressions pass. Unsupported scenarios reject
rather than inherit this result. Native identities prove two scenes; simultaneous
visibility, physical lifecycle and other telemetry families remain separate gates.

Definitions and thresholds were recorded before implementation. The attempted
signed definition commit failed because the configured SSH signing agent was
unreachable; no unsigned commit or push was created. The working definitions
remain the predeclared contract, and failed attempts retain their original result.
A second path-limited signing attempt after completion also failed, despite a
successful public-key listing. At that checkpoint no resumed-turn commit existed.
On 2026-09-17 the user changed the policy: sign when available, continue committing
unsigned locally if the agent is unavailable, and require signatures before any
remote push. The next actual signing attempt succeeded. The original accepted
runner and summaries are preserved in signed `a6e45d2bb`, baseline fixtures/results
in signed `6552ae261`, and release planning/review triage in signed `85c0d47a0`.
Nothing was pushed.

The subsequent policy change allows an unsigned HEAD through local acceptance
preflight while recording its signature state; existing signatures must verify.
The accepted runtime above still refers to the original frozen runner at
`a6e45d2bb`. Its semantic oracle and build/identity checks are unchanged. The new
preflight is checked with isolated Git fixtures for unsigned commits, signature
text in a message and an invalid signature; this is a tooling-policy change, not
a new runtime acceptance or a reason to rerun EXP-159/161.
All 27 Python controls pass: the 24 existing semantic/operational controls and
three new signature-preflight regressions.

## EXP-162 — Restore watchOS Resource and macOS WebView compilation

Status: PASS · D01/D02 closed; remaining routing and runtime gates unchanged

### Gates, pinned source and scope

D01 and D02 are the next finite release gates. The control SDK is signed
`fccf600eeda639d2c7a73748791e4d884823b729`; no SDK source differs from the
accepted EXP-159 checkpoint. Define this experiment before changing production
code. Keep the two platform repairs separate from D04 core isolation, routing
expansion, SwiftUI extraction, public API, generated models and wire formats.

The Resource handler compiles on watchOS but calls `RUMUIEventNetworkContext`,
whose Foundation-only adapter is currently enclosed by a non-watchOS guard.
The first candidate makes that existing adapter available on every supported
RUM platform while keeping UIKit event handling and its resolver guarded.
The WebView candidate extracts native scene metadata only where UIKit exists;
macOS forwards the same message and slot with absent scene metadata.

### Decisive controls and acceptance

1. Build the complete unchanged DatadogRUM target and dependencies for watchOS
   at minimum deployment 9.0; capture the missing-helper compiler failure.
   Build the complete unchanged DatadogWebViewTracking target and dependencies
   for macOS at minimum deployment 12.6; capture the NSWindow.windowScene failure.
   A failure before those targets compile is tooling/environment evidence only.
2. Apply the smallest platform guards. Build both complete targets again, in
   Debug and Release using the same Xcode 27 SDK and source manifest settings.
   Passing isolated expressions alone cannot close either gate.
3. Run focused iOS Resource/action-handoff tests and the WebView message/ownership
   suites. Existing exact-view, scene-only, absent-context and interleaved Resource
   checks must pass; native message forwarding and scene metadata must be retained.
4. Check changed Swift files with the applicable lint configuration; audit all
   usages and preserve the protected project/configuration/review files exactly.

No backend rerun is required for a conditional-compilation repair. A passing
build/test closes only its named compatibility gate, not D04/D11, minimum-iOS
runtime coverage, watchOS runtime behavior or physical multi-window acceptance.
If complete-module compilation exposes another defect, record the exact blocker
before changing scope; do not substitute a narrowed probe for the full build.

### Environment and artifact contract

Fresh discovery: Xcode 27.0 (27A266a), watchOS/macOS SDK 27.0, no installed
watchOS simulator runtime. The live SDK workspace is `workspace-DLY6WWrRlF`
with scheme `DatadogRUM`; current destination is iPhone 18 Pro iOS 27.0.
Available iOS 26.5 does not substitute for the missing iOS 15 runtime. No physical
iPad is available. A fresh bounded Datadog aggregate successfully established
read access without retrieving customer events; no backend ingestion is claimed.

Build artifacts start at the newly created local directory
`/var/folders/54/lrjgxzh90n174wzdwxhnlnnh0000gp/T/exp162-uam1nn18`.
Extract only named source/private/resource paths from the pinned revision into
isolated package directories. Include every production source of the selected
targets; preserve package deployment targets, Swift 5 mode, SPM_BUILD and resource
settings. No protected Xcode project or local xcconfig enters those packages.
Record extraction/source/package hashes, exact commands, exit codes, diagnostics
and result locations. Raw compiler logs remain local; the durable result contains
compact failure/pass evidence. Re-resolve workspace IDs and create a fresh root
if this experiment is restarted in another session.

### Execution

The definition was signed in `1aeca86ff` before production edits. The watchOS
control exited 65 at Resource handler line 135 with the missing helper. The
full macOS control exited 65 at the invalid scene expression with Swift's
“failed to produce diagnostic for expression” error; the earlier narrowed
member probe established the unavailable `NSWindow.windowScene` member. Neither
control failed in setup or dependency resolution.

The platform-neutral network-context adapter now sits outside the watchOS guard;
UIKit handling remains guarded (signed `e420528f7`). WebView scene extraction is
conditional on UIKit and forwards nil metadata on macOS (signed `af8864528`).
The complete candidate contains 297 source/private/resource files. Every file
matches committed `af8864528`; each build preserved its source identity.

| Validation | Result |
| --- | --- |
| watchOS RUM Debug / Release | Both exit 0; all 131 production Swift files compiled for arm64 and arm64_32, emitted modules verified |
| macOS WebView Debug / Release | Both exit 0; all 6 production Swift files compiled for arm64 and x86_64, emitted modules verified |
| iOS Resource/action handoff | 70/70 pass, no skips or not-run tests |
| iOS WebView forwarding/ownership | 28/28 pass, no skips or not-run tests; three unrelated rollover tests were not selected |
| Changed-source SwiftLint | Strict source configuration, zero violations |
| Workspace safety | Both user-owned dirty paths and the untracked review remain unchanged; commits use explicit source paths |

The [durable result](../Results/EXP-162-platform-compatibility.json) preserves
all six attempts, source and package manifests, commands, logs' hashes, emitted
module hashes and iOS result locations. Xcode emits a misleading “command failed
with exit code 0” wrapper line around some warnings even in passing builds;
actual exit status, compiled source lists and emitted modules establish the
result. Raw build/test artifacts remain local.

This closes compile gates D01/D02 only. No minimum-iOS, watchOS runtime, physical
multi-window or new backend ownership claim is made. D09/D11/D12 remain next;
D04 core isolation and D11 WebView fallback are unchanged by these guards.

## EXP-163 — Preserve overdue action stop attributes without peer leakage

Status: PASS · D12 closed; T02 restored from its accepted evidence plus this repair

### Gate, source and smallest useful slice

D12 is independent of D09/D11 and has a deterministic controlled-clock oracle,
so execute it first within the early compatibility repair group. The control
revision is `0f91747a410d1b8b4b9bf7dee287bcd36c5d6d83` (SDK source `af8864528`).
The EXP-162 evidence checkpoint used the authorized unsigned-local fallback
when the signing agent timed out; the two SDK platform-fix commits are signed.

`RUMSessionScope.process` currently expires every action with an empty keepalive
before routing its actual command. `RUMUserActionScope` already preserves
attributes from its own overdue stop, but that command arrives after the action
has been removed. Restrict the repair to routing action-command expiration:
resolve recipients before advancing their action timeouts, give recipients the
original action command, and advance only nonrecipient actions with empty local
attributes. Preserve existing non-action command timing and view/Resource routing.
Do not change timeout duration, expired-action name/type rules, public API,
restoration, core isolation, WebView correlation or performance thresholds.

### Predeclared acceptance

1. Add a failing session-scope regression on unchanged production source: legacy
   continuous action starts at t=0 and stops at t=11. Its emitted event must keep
   start-only attributes, merge stop-only attributes and give stop values precedence
   on shared keys. The original owner, action UUID and 10-second capped duration
   must remain unchanged. Expired name/type behavior is not broadened.
2. Cover scene and exact-view recipients with two overdue concurrent actions:
   explicit A stop while inferred B owns representation must preserve A's own
   stop attributes, expire B once with B's original attributes and exclude all
   foreign stop metadata. Check exact UUIDs/counts and final empty action slots.
3. Cover the same overdue-stop attribute merge for a discrete action; retain the
   existing peer discrete-action timeout negative control. A missing-scene stop
   must expire peers without donating attributes or creating an action.
4. Run the affected session/view/action scope and existing explicit-action monitor
   tests on the current iOS 27 simulator. Assert no skips/not-run regressions;
   preserve action navigation, target fallback and on-time stop behavior. Run
   strict source/test lint for the changed files and audit all expiration usages.
5. Keep failed control results and candidate summaries with source/test hashes,
   actual revisions, environment and artifact locators. No backend or hardware
   acceptance is claimed by a controlled-clock unit result. EXP-159's accepted
   backend slice remains valid; do not rerun it merely to add this discriminator.

### Environment and execution

Fresh Xcode discovery selected `workspace-DLY6WWrRlF`, scheme `DatadogRUM`,
iPhone 18 Pro iOS 27.0 with Xcode 27.0 (27A266a). The live destination list still
has iOS 26.5 but no minimum-iOS runtime or available physical iPad/iPhone. The
current turn's authenticated Datadog read succeeded; this scope needs no new
backend query. Create fresh artifacts and obtain the new test list after adding
regressions. Definition was signed in `d86ded7f8` before test/production edits.

### Execution and result

The first control reproduced missing own stop attributes in continuous, discrete,
and concurrent targeted cases (three failures); both peer controls passed. It
also exposed a 24 ns Date precision mismatch in the discrete deadline assertion.
That test used a fixed reference-time clock for the next unchanged-source control:
all deadline/identity checks passed, and only the three metadata cases still
failed. Both original summaries are retained, including the test-oracle issue.

Signed `084dff4c1` defers expiration for action commands until their recipients
are known. Recipients receive their real action command; nonrecipients receive
empty-attribute time advancement. Non-action command timing remains unchanged.
The complete session/view/action scope and monitor selection passes 212/212,
with zero skips or not-run tests. Strict lint passes both changed files with zero
violations. Source and test hashes match the committed candidate after execution.

The [durable result](../Results/EXP-163-action-stop-attributes.json) retains all
three attempts and exact result locations. New checks preserve own start/stop
merge precedence, action/view UUIDs, capped duration and existing expired name/
type behavior for legacy and explicit scene/exact-view targets. Missing-scene
and peer-discrete controls exclude foreign metadata; existing monitor tests
retain on-time completion, fallback, duplicate-start and navigation behavior.

D12 closes. T02's review hold is removed by this missing discriminator, together
with its accepted EXP-159 exact mapper/backend evidence. No accepted backend
run was repeated and no physical long-duration or other family gate closes.
D09 and D11 remain next. The production safety review now has a live disposition
table, as subsequently requested by the user, and is included in explicit-path
commits while preserving its historical findings.

## EXP-164 — Preserve controller API caller-thread compatibility

Status: PASS · D09 closed

### Gate, source and repair boundary

Control is `6b7a53dd7941ca75b6c455b61d09673c24d7d613`, with SDK source
`084dff4c1`. The control documentation checkpoint is unsigned locally after a
signing-agent timeout; the SDK fix is signed. Existing nonisolated controller
start/stop APIs must retain their caller-thread contract. UIKit hierarchy
extraction is allowed only on main; background callers retain the existing
inferred/identity/representative routing without a new synchronous main wait,
actor requirement, registry or public API. D11 and core isolation remain separate.

### Predeclared decisive checks

- On unchanged production source, background start/stop with a getter spy must
  reproduce hierarchy reads. Add guards only after the failing test runs.
- Candidate background calls must read no `viewIfLoaded` hierarchy, must preserve
  caller handoff when available and ordinary fallback otherwise, and must finish
  without synchronous dispatch to main. Existing identity stops must preserve peers.
- An isolated single-scene app must mount an actual controller/window, preserve
  main-thread native scene targeting under contradictory handoff, and exercise
  background calls with actual UIKit objects. Prove Main Thread Checker is loaded
  in that process and require no candidate background-UI diagnostics. The unchanged
  control must fail the getter/ownership oracle; preserve its diagnostics separately.
- Reuse existing isolated package/build/install helpers without changing EXP-160's
  frozen fixtures or thresholds. Pin source, fixture and binaries; require clean
  install, fresh run identity and durable compact results. No local xcconfig or
  protected project enters the isolated package. An unmounted scene or unloaded
  checker is inconclusive, never a pass. No physical multi-window claim follows.
- Run affected monitor/manual-controller/convenience tests and strict changed-file
  lint; record exact counts. Do not repeat accepted unrelated experiments.

### Environment and execution

Fresh discovery uses SDK workspace `workspace-DLY6WWrRlF`, iPhone 18 Pro iOS
27.0, Xcode 27.0 (27A266a). The latest destination query has no available physical
iPhone/iPad; watch availability changed since EXP-162 and is not needed. iOS 26.5
remains available, minimum-iOS runtime does not. Datadog read access was checked
this turn; this local compatibility gate does not require a backend query.
Definition was committed in local `1060d4f89` before implementation.

### Attempts and acceptance

The first unit build failed on a fixture closure argument label; its build
summary is retained as INVALID. After correcting that label, both unchanged-SDK
getter tests failed on exactly two hierarchy reads per background start/stop pair.
The candidate main-thread guard preserves the previous inferred fallback, with
no dispatch-to-main wait, new state or public API. All 31 affected monitor and
convenience tests pass with no skips/not-run tests; strict source/test lint passes.
The source/test checkpoint is local `a9abc092b`, unsigned after an agent timeout.

The isolated runtime runner imports only existing package/extraction/hash helpers.
Its first complete attempt was INVALID because the fixture returned nil from a
nonoptional view mapper; both compile logs are retained. The corrected fixture
ran in a fresh attempt with frozen SDK/fixture/package/binary identities and
verified clean installations/installed binaries. Both complete Core/RUM Debug
apps build at minimum iOS 15 on Xcode 27; execution is iOS 27 only.

| Runtime discriminator | Control | Candidate |
| --- | --- | --- |
| Named checks | 11/19 | 19/19 |
| Actual controller mounted in native scene | PASS | PASS |
| Main Thread Checker loaded | Yes | Yes |
| Background hierarchy reads | 5 | 0 |
| Main Thread Checker diagnostic headers | 4 | 0 |
| Frozen SDK/fixture/binary after execution | Unchanged | Unchanged |

All five worker submissions run off main. The candidate preserves native scene
selection for main-thread calls under contradictory handoff, representative and
inferred background routing, and nonrepresentative identity-stop peer ownership.
The context-queue `currentSessionID` callback is the drain barrier before each
observation, not a timed guess. One actual UIWindowScene is mounted; the peer
is an injected logical RUM branch, not a physical/concurrently visible window.

[EXP-164-controller-threads.json](../Results/EXP-164-controller-threads.json)
contains source/result hashes, the two unit failures, 31-test result, INVALID and
accepted runtime attempts, all 19 checks and compact diagnostic headers. Raw
build/console logs remain local. The repeatable fixture/runner lives under
`tools/multi-scene/controller-threads/`. D09 closes; D11 is next. No backend,
minimum-iOS runtime, hardware, performance or other ownership gate closes.

## EXP-165 — Preserve legacy native/WebView Replay correlation

Status: PASS · D11 closed

### Gate, source and repair boundary

Control is local `074f644ad6a60e7a324e7da66b503b95e9b20dc0`, with SDK
source `a9abc092b`. An ordinary single-scene app can start a string-key native
view outside UI dispatch, leaving it scene-less. The actual mounted WebView
bridge supplies a scene ID, so strict lookup currently drops its Replay container.
Preserve this existing association without selecting a peer scene or extending
multi-scene Replay support. No public API, wire format or endpoint changes.

The bounded repair permits legacy cache fallback only when the application does
not declare multiple scenes and the cache contains no scene-owned view. Exact
scene lookup remains authoritative; explicit cache callers keep strict defaults.
Declared multi-scene applications remain fail-closed even with only legacy cache
entries. Historical timestamp, Replay filtering, TTL and metadata stripping stay
unchanged. This is D11 compatibility, not T10 two-container acceptance.

### Predeclared decisive checks

- Reproduce the missing native container with unchanged SDK source in receiver
  tests; controls cover Replay-disabled/future entries, explicit peers, mixed
  legacy/scene history and the source-less ambiguous case.
- Candidate tests cover the declared-multi-scene fallback prohibition and exact
  lookup/default behavior in addition to the positive legacy case. Audit every
  cache and receiver constructor/consumer before accepting the repair.
- Clean-install a frozen isolated app using the real Core, RUM, WebView bridge and
  Session Replay. Mount an actual WKWebView in its UIWindowScene; start a legacy
  string-key native view outside a handoff; require actual Replay-enabled native
  context and browser/native container ID equality in captured emitted payloads.
  Require private routing metadata to be absent from the written payload.
- Exercise peer and explicit-owner controls through that same mounted bridge.
  No arbitrary peer container is permitted. Missing native topology, missing
  Replay enablement, stale result, restored run ID or changed frozen identity is
  INVALID/INCONCLUSIVE, never a pass. Preserve failing control and invalid attempts.
- Run affected cache, receiver, feature-construction and bridge tests plus strict
  changed-file lint. Use a new run directory and frozen source/build/fixture
  identities; do not rerun accepted unrelated experiments. Local payload evidence
  closes this compatibility gate; backend two-container ownership belongs to T10.

### Environment

Fresh Xcode MCP discovery selected SDK workspace `workspace-DLY6WWrRlF`, scheme
DatadogRUM and iPhone 18 Pro iOS 27.0, Xcode 27.0 (27A266a). iOS 26.5 remains
available; iOS 15 does not. The iPhone 16 Pro Max on iOS 27.2 and Apple Watch are
now paired/available; the iPad remains unavailable. These devices do not satisfy
the concurrent iPad/Duo topology requirement. A bounded authenticated Datadog
aggregate succeeded; this compatibility experiment uses local emitted payloads.
Both protected paths match their previous index/stat identities, and the project
file hash is unchanged. Local HEAD is unsigned under the authorized fallback.

### Execution and acceptance

Definition `32f446de5` was signed before implementation. The unchanged SDK fails
one of five predeclared receiver checks: the legacy container is absent. Replay
filtering, future-event exclusion, peer exclusion and exact-scene selection pass.
Signed candidate `9a1ee83a5` adds explicit legacy-fallback opt-in in ViewCache and
passes the application's existing multi-scene declaration into the receiver.
Declared multi-scene apps cannot use the fallback. Cache callers remain strict
by default, and any scene-owned history disables the compatibility fallback.

The 71 cache/receiver/feature-construction tests and all 31 bridge tests pass,
with no skipped or unexecuted tests. Strict lint passes three source and two test
files. Initial test formatting violations were corrected before checkpointing;
no SDK behavior changed after the passing test run.

Both isolated full-module apps build at minimum deployment iOS 15 and execute
on the freshly resolved iOS 27 simulator. The first runtime attempt is INVALID:
the local collector handled gzip but omitted the SDK's deflate encoding, so both
browser acknowledgements timed out. Its raw status was FAIL; the durable record
explicitly classifies this as tooling-invalid and preserves the manifests/logs.
After correcting decoding, a new frozen build/install/run passes:

| Mounted bridge discriminator | Control | Candidate |
| --- | --- | --- |
| Actual WKWebView in sole native scene, actual Replay context | PASS | PASS |
| Exact emitted native view has Replay | PASS | PASS |
| Legacy browser container equals native view ID | FAIL | PASS |
| Peer exclusion and exact-scene ownership | PASS | PASS |
| All named checks | 18/19 | 19/19 |
| Frozen SDK/fixture/executable after execution | Unchanged | Unchanged |

The fixture sends Browser bridge envelopes through actual JavaScript and waits
for each exact emitted payload before changing native ownership. Private scene
metadata is stripped. Both native Replay enablement and native RUM Replay flags
are real, with no synthetic context injection. The peer is a logical branch in
one native window; no two-window or Browser SDK distribution claim follows.
Four collector controls cover deflate/gzip, persistent readiness and malformed
payload rejection. Raw payloads stay local; committed evidence contains compact
ownership summaries and hashes only.

[EXP-165-webview-correlation.json](../Results/EXP-165-webview-correlation.json)
retains the failed control, 102-test result, INVALID and accepted runtime attempts,
identities and ownership summaries. D11 closes; T10 two-container/backend,
minimum-iOS runtime and hardware gates remain open. D04 with P02 is next.

## EXP-166 — Isolate UI-event handoff by SDK lifecycle and reduce allocations

Status: PASS · D04 and P02

### Gate and control

Control is signed `8e8b4522c23368aaeab5f810c5ba7230b63f9930`, with SDK
`9a1ee83a5`. D11 is accepted and is not rerun. Source audit confirms unqualified
handoff reads in Monitor/subscriber routing, the Resource handler, RemoteLogger,
DatadogTracer and NetworkInstrumentationFeature. The shared value has no SDK
owner, and an authoritative nil from a foreign instance suppresses valid context.
P02 already measures 3 allocations/416 requested bytes per enabled dispatch;
the frozen acceptance budget remains at most 1 allocation/64 requested bytes.

### Bounded implementation contract

Use one opaque identity for each core lifetime, shared by that core's feature
scopes and network instrumentation. Invalidate it when the core stops; retaining
an inherited task must not retain or reactivate a core. All consumers resolve
only the matching live owner. Application IDs, session IDs, feature object
addresses alone and default-core assumptions are not ownership keys.

Nested dispatch must preserve an outer owner's context while another owner is
active, including authoritative nil. Preserve task inheritance, original dispatch
count, pending-action merging, session expiration validation and weak lifetime.
Consolidate hot-path storage only with measured allocation evidence; do not trade
ownership or caller-thread safety for a budget pass. New communication stays
internal/SPI and Foundation-only; no public product API or dependency is added.

### Predeclared decisive checks

- Reproduce foreign nonnil and nil ownership on unchanged production source;
  include actual action-dispatch-to-monitor routing across two distinct feature
  scopes and both distinct and identical RUM application IDs.
- Check every consumer: native/OTel Trace starts, Logs and mirrored errors,
  URLSession first-/third-party captured Resources, direct RUM actions/errors and
  view mutations. A no-RUM consumer must never acquire another core's RUM IDs.
- Nested A→B→A, thrown/early return, synchronous execution inside an existing
  task, inherited child task and detached work preserve the proper scope. Old
  inherited work after stop/reinitialize cannot use either a stale generation or
  the replacement's context. Core/feature weak references must be releasable.
- Core tests prove all scopes share one token, different core instances never do,
  and stopping invalidates it. Custom/NOP scopes have a safe absent-owner default.
- Re-run the affected module selections and changed-file strict lint. Compile
  platform-neutral consumers for supported non-UIKit targets when impacted.
- Measure the enabled and ordinary dispatch paths in Release against the frozen
  EXP-160 workload/thresholds on the same toolchain/simulator. Retain calibrated
  allocation count and bytes, median/p95, exact reentrancy and frozen source/build
  identities. Adapt only fixture bindings to internal interface changes, with an
  explicit diff; do not rewrite accepted evidence or increase thresholds.

D04 closes only after every consumer/lifecycle discriminator passes. P02 closes
only after measured count and requested bytes meet the existing budget; net heap
or a small extracted context helper cannot replace the full dispatch workload.
Any failure remains in the same gate/experiment until resolved. D05/D06 and
SwiftUI retention/authority are separate following gates, not added scope here.

### Environment

Xcode 27.0 (27A266a), freshly discovered SDK workspace and iOS 27/26.5 simulator
availability from this execution; minimum iOS 15 runtime remains unavailable.
Use new artifact roots and actual current test lists. Physical iPad/Duo topology
is not required for these ownership/allocation gates. Both protected paths remain
unchanged after the signed EXP-165 component commits. No push is authorized.


### Attempts and accepted result

Unchanged production source fails all three actual-dispatch cross-core controls:
foreign application, same application with another session, and authoritative
foreign nil. Signed SDK `5eb3c1aac` passes 282 affected tests: Core 22, Internal 79
plus 2 concurrency controls, RUM 109, Logs 29 and native/OpenTelemetry Trace 41.
Six strengthened consumer-session/live-clock checks also pass; they are repeats
with stronger assertions, not extra unique tests. No test is skipped or unrun.
The probe builds, and strict configured lint passes 11 production plus 12 test/mock
files. Complete watchOS RUM and macOS Core Debug/Release builds all pass.

The first pure TaskLocal candidate failed the new synchronous-snapshot control.
The repair reuses one thread-local storage cell, restoring entry snapshots on
normal/throwing exits while child tasks refresh through a weak scene provider.
The opaque owner invalidates on core stop/deinit; scopes and network share it,
foreign nil cannot suppress a consumer, nested owners remain separate, and no
core is retained by the token or provider. The first full-dispatch diagnostic
measured 1 allocation/112 bytes; removing the duplicated clock and full target
from the resolver reached 1/64. Both diagnostic attempts remain separate from
acceptance. Initial fixture compilation and third-party-callback expectation
corrections are preserved, not counted as SDK failures.

The final frozen Release attempt completed 24 fresh ABBA launches on iOS27/26.5
plus two full-context assertion launches. Ordinary dispatch allocates 0/0; enabled
handoff allocates exactly 1/64 per event in all four candidate measurement
processes, versus EXP-160's 3/416. Handoff median/p95 is 1208/1500ns on27 and
1209/1500ns on26.5; excess over ordinary is 875/1083ns and 876/1125ns respectively,
within the unchanged 5000/10000ns limits. All ordinary median/p95 budgets pass.
Each runtime's reentrancy oracle checks 90,000 boundaries, 20,000 original calls
and 10,000 thrown exits with zero errors. Custom/NOP behavior remains exact.

Frozen SDK/fixture/binary identities and the original protocol/fixtures remain
unchanged. The committed SDK sources exactly match the measured candidate.
Copied fixture differences only bind the new owner-scoped SPI. Durable records:
[ownership and attempts](../Results/EXP-166-handoff-isolation.json),
[performance identities/results](../Results/EXP-166-handoff-performance.json),
and [numeric samples](../Results/EXP-166-handoff-performance-samples.json).
The repeatable runner is `tools/multi-scene/handoff-isolation`; raw console and
compiler logs remain local. No backend or physical-window proof is claimed.

### Conclusion and next boundary

D04 and P02 close; P01/P04 remain closed after their frozen budgets pass again.
Release progress is 19/66 gates, with 6 of 12 assessed review findings closed.
D05/D06 old-owner resolution and lazy session restoration are next. Retained
scene state, SwiftUI authority, final API/CI and hardware gates remain open.


## EXP-167 — Preserve navigation ownership across session restoration

Status: PASS · D05 and D06

### Question and pinned control

Can explicit stop, immediate expiration and lifecycle-delayed expiration restore
eligible peer scenes without changing the incoming navigation's old owner?
Control is signed `5a58460f612f45ac46f98315d9c5a3955acce605`, whose SDK is signed
`5eb3c1aace8d07f03f2bb113afcf5243f07b6e4e`. D04/P02 is accepted. This experiment
admits only D05/D06 restoration repairs; public targets and downstream expansion
remain separate.

### Source audit and proposed boundary

`RUMApplicationScope.startNewSession` excludes the old representative before the
unchanged source-less start resolves against the remaining restored peer. A
source-less stop must resolve its matching view identity before representative
fallback. Lazy timeout/max-duration restoration also omits the concurrent-peer
rule already used by immediate refresh. Ownership must be decided against the
old session exactly once and carried through exclusion, session creation and
command delivery. Preserve single-scene and foreground/background policy.

Use one internal navigation/restoration decision shared across the three paths.
A navigation start or stop must not revive the view it replaces or closes; every
eligible peer gets a fresh new-session occurrence and exact subsequent owners.
Preserve explicit scene targets, legacy scene-less behavior, sampling and session
preconditions. No public API, wire format, new dependency or extraction is needed.

### Predeclared decisive tests and environment

- Reproduce A/B with B representative, `stopSession`, then source-less start C:
  A survives in its own scene, C replaces B, and both new-session view IDs are
  fresh. Prove exact outgoing view inventory, not only two active views.
- Reproduce the same boundary followed by an identity stop for nonrepresentative
  A: B survives and receives subsequent actions/Resources in the new session.
- For inactivity timeout and maximum duration separately, let a lifecycle command
  expire the session without creating another. Follow with start and stop in A;
  B must survive with a fresh view, correct session precondition and exact local
  telemetry ownership. Keep activity fresh before the max-duration boundary so
  inactivity cannot accidentally satisfy that control.
- Compare explicit-stop, immediate and delayed expiration; include inferred and
  explicit targets, representative changes, ordinary scene-less single-scene
  behavior, and background policy with tracking enabled/disabled. Reject phantom
  restored occurrences, duplicate views, stale IDs and peer attribution.
- Run affected application/session/view scope tests and strict changed-file lint.
  Use current discovered iOS 27 simulator test selectors; no backend or physical
  lifecycle acceptance is claimed from these controlled scope tests.

D05 closes only when old-owner resolution is preserved for both source-less
start and identity stop. D06 closes only when timeout and max-duration delayed
paths retain eligible peers without reviving ineligible foreground views in the
background. Keep every failed/invalid attempt in this record. D03/P03 lifetime
and D07/D08 authority/reconnect follow after this slice; no accepted EXP is rerun
merely to resume.

### Attempts and implementation

1. Definition `313a05304` was signed before implementation. The six predeclared
   scope controls all fail on unchanged production source: source-less start
   loses A, identity-stop A loses B, and delayed timeout/max-duration start/stop
   loses eligible peers. These are semantic failures, not build failures.
2. Signed SDK `0aaafa7bd` resolves process navigation against the old ordered view
   set before excluding its owner, then carries that target into dispatch. A
   shared policy chooses restored peers for explicit, immediate and lazy paths;
   the refreshed-session initializer accepts that exact set. No replaced/closed
   branch is spuriously restored. Legacy nil-scene and background policy remain.
3. Six controls pass, then all 198 affected application/session/view tests pass.
   Thirteen new test methods execute 44 cases. Exact outgoing view UUID sets and
   immediate peer action/Resource owners are checked. Strict lint passes for two
   source files and one test file; only argument whitespace changed after tests.
4. The first complete native fixture attempt is INVALID: scene activation's
   callback can arrive before UIApplication reports active, consuming readiness.
   No semantic verdict is inferred. Bounded retry after the callback fixes the
   harness; fresh control/candidate source copies and binaries are rebuilt.
5. The second run uses iPad Air 11-inch (M4), iOS 27.0, with two actual distinct
   UIWindowScenes and mounted controllers. Unchanged control passes 20/47 and
   fails all six navigation-owner checks; candidate passes 47/47. Owners are
   captured immediately after navigation, before marker submission. Both arms
   prove clean install, new run IDs and unchanged source/fixture/binary hashes.

### Durable evidence and limits

[EXP-167-session-restoration.json](../Results/EXP-167-session-restoration.json)
contains selectors, six control failures, candidate results, source hashes,
complete named runtime checks, native topology, compact build identities and the
invalid first attempt. The repeatable runner is
`tools/multi-scene/session-restoration/run.py`; its README specifies the oracle.
Xcode is 27.0 (27A266a); SDK deployment remains iOS 15. Native topology is real,
but expiration command times are controlled. No backend, physical lifecycle or
minimum-runtime result is claimed. EXP-166 performance remains frozen.

### Conclusion and next boundary

D05/D06 close. The finite register is 21/66 and production review is 8/12 closed.
Next define D03 mounted keyed registration release, paired with P03 measurements;
P03 also requires D08 and retirement of disconnected registry state. Keep the
remaining semantic authority/presentation and hardware gates separate.

## EXP-168 — Release keyed SwiftUI registrations and instrumentation

Status: PASS · D03; P03 measurement; R02 review after repair

### Question and pinned control

Does a removed keyed SwiftUI boundary release its occurrence registration and
instrumentation, including the real SDK teardown that removes swizzling?
The signed control is `f95f8cf600b6bc3b53056bce5ad2890bc2c6f8f8`, SDK
`0aaafa7bd21eaecd9908086f9043f813b3cbc419`. EXP-167 is accepted and must not be
repeated as a resume check. This definition precedes production changes.

### Hypothesis and bounded change

Both keyed modifier variants store a callback that calls a bound modifier method.
Registration → callback → modifier → State registration closes an ownership cycle
and retains instrumentation. Weak source/state fields cannot break that cycle.
Replace these callbacks with an independently owned immutable descriptor context
whose state, handler and arbiter references are weak. Explicit registration
cancellation must clear the callback and invalidate stale epochs without treating
a temporarily hidden retained route as destruction. Preserve synchronous accepted
navigation, exact revealed-route identity and queued final-detach behavior.

No public API, new dependency, swizzle implementation change or file extraction.
The attachment-backed fallback shares the safe callback path; a compile/unit
check does not claim an iOS 15 runtime. P03's disconnected-history growth requires
D08 and remains a separate open budget after this bounded lifetime repair.

### Predeclared decisive oracle and environment

- Build the same isolated mounted fixture against the pinned control and candidate.
  Use actual SwiftUI hosting controllers and a real UIWindowScene, SDK automatic
  instrumentation enabled, and a keyed occurrence source that outlives removal.
- After five warm-up and twenty measured mount/remove cycles, weak registration,
  tracking-state, reader and hosting-controller survivors must be zero after
  bounded main-queue draining. Count live weak objects, not process RSS alone.
  Confirm a keyed registration and RUM occurrence existed before every removal.
- Release the SDK core after host removal. Weak instrumentation, handler and
  arbiter must clear. Observe real method implementations before enable, while
  instrumented and after stop; all observed selectors must return to the exact
  original implementation. Never mutate methods from the fixture.
- The unchanged control must reproduce retention; setup/topology failure is
  INVALID or INCONCLUSIVE, not a passing negative control. Clean installations,
  frozen source/fixture/binary hashes and unique run IDs remain mandatory.
- Cover cancellation/rebind epoch rejection, released weak targets, retained-route
  reveal with fresh descriptor/occurrence and unaffected peer routing. Run the
  affected SwiftUI state/occurrence/arbiter and instrumentation tests, plus strict
  changed-file lint. Review R02's authority/occurrence responsibilities against
  these discriminators before changing its status.
- Run mounted lifetime acceptance on available iOS 27 and 26.5 simulators. Keep
  minimum runtime, physical disconnect, backend and final independent review
  separate. No numerical baseline threshold may be raised.

D03 closes only with mounted weak release and balanced SDK teardown, not an
extracted ARC-shape probe alone. P03 remains open until all its D08/registry and
retained-state criteria pass. Record every failed/invalid attempt. Then continue
D07/D08 semantic authority/reconnect and D10 accepted presentation state.

### Attempts and accepted implementation

1. Definition `ca01939f4` is signed before implementation. The actual mounted
   control on iOS 27 reproduces six lifetime failures: registrations/states,
   instrumentation/handler/arbiter and selector restoration. Readers/controllers
   release. The first 26.5 control also retains 25 registrations/states, but is
   INCONCLUSIVE because the fixture wrongly required its absent 27-only arbiter.
2. The revised fixture explicitly requires arbiter presence on27 and absence
   on26.5. Fresh complete control/candidate builds use that identical fixture.
3. Signed SDK `7b77f60eb` moves both modifier callbacks onto a small context that
   weakly references state, handler and arbiter. Cancellation clears the callback
   and unregisters its source/epoch; hidden retained routes are not cancelled by
   ordinary disappearance. No swizzle code changes or new API are introduced.
4. Five focused context tests and all 303 affected SwiftUI/handler/instrumentation
   tests pass. Strict source/test lint has zero violations. Cancellation/rebind,
   live weak targets, fresh descriptor/occurrence and interactive cancel/single
   commit are explicit new discriminators; existing peer and stale-route tests pass.
5. Native 27/26.5 candidates pass 37/37 each after five warm-up and twenty measured
   mount/remove cycles. Both controls retain 25 registrations and 25 states;
   candidates retain zero registrations/states/readers/controllers. After SDK
   release, weak core/instrumentation/handler/arbiter references clear and the
   exact original implementations of viewDidAppear:, viewDidDisappear: and
   sendEvent: return. Control totals are31/37 on27 and32/37 on26.5.

### Evidence, review and limits

[EXP-168-swiftui-lifetime.json](../Results/EXP-168-swiftui-lifetime.json) contains
both attempts, every cycle count/check, full affected selectors, test locators,
source/fixture/build identities and interpretation limits. The repeatable fixture
is `tools/multi-scene/swiftui-lifetime/run.py`. Actual source registration objects
are observed through weak references using read-only reflection; no SDK hook or
replacement lifetime implementation is tested. Sources deliberately outlive hosts.

R02 is reviewed again in COMPONENT_REVIEW.md and closes its bounded authority/
occurrence source obligations. D03 closes. The release register is23/66 and the
production safety review9/12. P03 remains open: its disconnected-history failure
was not repaired or remeasured. R04 still needs D08 and delayed retained remount;
R05/R06 and final independent/API/hardware review remain separate. No backend,
minimum-runtime or physical lifecycle acceptance is claimed.

### Next boundary

Define D07 pending semantic authority, then D08 accepted reconnect publication
with P03 registry retirement; follow with D10 accepted presentation state.

## EXP-169 — Keep automatic tracking until semantic input is ready

Status: PASS · D07

### Question and pinned control

Can explicit and optional-capability hosts subscribe to an empty transition source
without suppressing automatic tracking, then acquire local authority when the
first accepted destination can be published? Pin control `58e06cfb1d12f1d291f5318de2a1669217d71227`
(local unsigned evidence checkpoint), signed SDK `7b77f60eb`. D03/R02 are accepted;
this definition precedes changes to semantic authority.

### Bounded change and decisive oracle

Separate source selection/subscription from effective automatic suppression.
Keep the source pinned through reconstruction. No initial snapshot, no handler or
no scene attachment must mean no semantic authority. When all are available,
publish the accepted destination synchronously before customer marker work and
activate suppression only for that host subtree. Cancellation/proposals and
repeated initial metadata must not manufacture occurrences. Final detach releases
only its own authority. Preserve nil-before-first-value observed input behavior.

- First reproduce empty explicit source, empty optional capability and absent
  instrumentation using the real authority registry attached to UIKit subtrees.
  Assert source selection/subscription separately from suppression and commands.
- First accepted input must start exactly one semantic occurrence in the correct
  scene; immediate marker ownership must use it. Before input, normal automatic
  tracking must remain eligible. Unrelated sibling/peer suppression is unchanged.
- Use a mounted actual RUMNavigationHost fixture on an iOS 27 simulator with
  automatic SwiftUI tracking enabled, explicit and capability variants, frozen
  control/candidate sources/binaries, clean installation and unique run IDs.
  Require exact before/after snapshots and mapper owners at the input boundary.
- Update the two existing tests that currently assume suppression with a nil
  handler: positive subtree teardown must establish real publication, while the
  source-pinning test must preserve source identity without claiming authority.
  Do not weaken them into callback-count checks.
- Run affected authority, semantic host/engine and view-handler tests, plus strict
  changed-file lint. No new public API, dependency or file extraction.

D07 closes only with the pending-source/absent-instrumentation controls and mounted
first-input ownership. D08 separately owns disconnected-scene rejection and fresh
reconnect generations; R05 separately owns multiple-observer nested delivery.
P03 still owns registry retirement. Physical lifecycle, backend, minimum-runtime
and final independent/API review remain open. Preserve every invalid attempt.

### Environment preflight

Xcode workspaces were rediscovered before this slice: Datadog, IntegrationTests
and native probe are available. iOS 27/26.5 simulators are available; minimum15 is
not. Current devicectl discovery reports the paired iPad(A16), iPhone16ProMax27.2
and Apple Watch unavailable, so no physical gate can close from this slice.
The local scenario uses dummy credentials/loopback only; reauthenticate before
any subsequent Datadog backend query. No accepted experiment is rerun to resume.

### Attempts and implementation

1. Definition `9abc3d979` was committed before production changes. Four actual
   authority-registry controls fail on unchanged SDK source: empty explicit,
   empty capability, missing handler and missing attachment all suppress too early.
2. SDK `a9aaf25a7` removes suppression from source subscription and activates it
   only after snapshot/scene/handler publication prerequisites. Source pinning
   remains unchanged. Two older tests had assumed authority with a nil handler;
   the positive subtree test now proves two real starts, while the pinning test
   expects no authority without instrumentation.
3. All four controls pass, followed by119 affected authority/engine/lifetime/
   view-handler tests. Strict lint passes. Definition and SDK commits are unsigned
   locally after signing-agent timeouts under the user's explicit policy.
4. The first native comparison passes: identical frozen fixture, clean install,
   unique run IDs and stable SDK/fixture/installed-binary identity. Control is19/29
   and fails automatic eligibility/ownership before input for both explicit and
   capability hosts. Candidate is29/29. An action submitted synchronously after
   first input, before any additional render or lifecycle callback, has the exact
   semantic view and session. Duplicate initial input and cancelled proposals
   produce no unwanted occurrences. Native scene and authority registry are real.

### Evidence and remaining boundary

[EXP-169-pending-authority.json](../Results/EXP-169-pending-authority.json) records
all controls, selectors, test locators, native checks/mapper rows and frozen build
identities. `tools/multi-scene/pending-authority/run.py` repeats the comparison.
D07 closes; release progress is24/66 and production safety findings10/12.
D08 still owns disconnected rejection and reconnect generations, P03 owns
registry retirement, and R05 owns nested multiple-observer delivery. No backend,
physical lifecycle, minimum-runtime or final API/reviewer acceptance is claimed.
Next define the D08 stale-trait-between-disconnect-and-remount regression.

## EXP-170 — Accept semantic reconnects only after a live attachment

Status: PASS

### Question and release gate

Can a retained semantic host reject stale scene traits and rejected consumer
publication, then establish exactly one fresh occurrence at a valid reader
remount without consuming the source generation or disturbing a peer? This
experiment is admitted for D08. P03 registry retirement, R05 observer fan-out and
H09 genuine physical disconnect ordering remain separate gates.

### Frozen control and decisive acceptance

Control is `7826eabc1` (SDK `a9aaf25a7`), after accepted EXP-169. Commit this
definition before implementation. First reproduce the reported sequence with the
actual RUMViewsHandler and command subscriber, not a permissive handler stub:
initial A/B publication, disconnect A, stale A attachment/source reconstruction,
real handler connection/foreground, and reader remount. Rejected attempts must
not acquire authority or consume the generation; the accepted remount must start
exactly one fresh identity, with no duplicate stop or peer change.

- Distinguish initial inherited scene hints from live reader mounts after a
  disconnect. Cover a stale hint both before and after connection but before
  reader mount, an early reader callback while still disconnected, duplicate
  reader/body reconciliation, latest input while waiting, scene migration,
  repeated disconnect cycles, absent handler/input and final detach/remount.
- Make publication acceptance observable internally. A host records an active
  occurrence only when the handler accepts it. Preserve inactive-scene staging,
  manual/automatic precedence and exact peer ownership; do not create public API.
- Use a mounted iOS27 fixture with the actual host, real native scene/reader,
  injected deterministic lifecycle ordering and exact before/after view IDs.
  Submit immediate Resource and Log work at the accepted reader boundary, before
  any extra rendering or deferred assertion can repair ownership. Preserve a peer
  scene's owner. Verify restored and stopped occurrence counts and UUIDs.
- Freeze control/candidate source and fixture identity, clean installation and
  unique run IDs. Require the unchanged SDK to fail the ownership discriminator,
  then all candidate checks and affected unit suites plus strict source lint.
  Retain every invalid/inconclusive attempt with its cause; no count-only oracle.

### Environment and evidence limits

Xcode27 workspaces and run destinations were rediscovered. iOS27 and26.5
simulators are available; minimum iOS15 is unavailable. Paired iPad(A16),
iPhone16ProMax and Watch remain unavailable. This deterministic simulator
regression may close D08, never H09's OS-driven physical ordering. Local dummy
credentials/loopback need no backend access; reauthenticate before a backend
query. No accepted experiment is rerun merely to resume. No registry retirement,
API expansion, extraction, networking format or dependency change is admitted.

### Attempts and result

Definition `c559b3b3e` preceded implementation (unsigned after signing timeout).
Three controls fail on unchanged source. Signed SDK `66d1ccb02` reports consumer
acceptance, fences inherited traits after lifetime teardown and gives a fresh
attachment generation to the next occurrence. Seven decisive tests and314
affected tests pass. Repository source/test strict lint passes after formatting.
An initial test build used Set on an Equatable-only identity; that fixture error
was corrected without changing production identity semantics. An initial lint
invocation omitted the repository configuration and is not acceptance evidence.

Native attempt1 is INVALID: the isolated package assumed a nonexistent Logs
Resources directory. Attempt2 is INCONCLUSIVE: both variants lacked the authority
registry because the fixture disabled automatic tracking. It also supplied an
artificial nil UI handoff immediately after enqueueing a start, although these
reader lifecycle callbacks run outside UI dispatch. Attempt3 enables the registry
with a filtering predicate and submits ordinary Resource/Log calls immediately
at the reader boundary; the oracle inspects their already-submitted ownership
after queue drain. The predicate invents no automatic view. Peer setup is drained
before the boundary, and ApplicationLaunch is the only allowed startup view.
Reader callback rebinding proves a real stale-trait reconstruction occurred.

Attempt3 passes. Control is39/51 and candidate51/51 for the same frozen fixture,
with clean installation, fresh run IDs and unchanged SDK/fixture/installed binary
hashes. Both control variants send immediate reconnect Resource/Log work to Peer;
both candidates use the fresh Home UUID/session. No await, render or navigation
command intervenes between reader delivery and marker submission. Duplicate
readers create no occurrence. Delayed200ms detach and remount of the retained
controller produces a third distinct Home UUID; peer UUID/count stay unchanged.

### Durable evidence and next gate

[EXP-170-reconnect-acceptance.json](../Results/EXP-170-reconnect-acceptance.json)
contains every native attempt, all314 selected tests, failed controls, build/test
locators and compact frozen identities. The signed SDK repair closes D08. The
incremental COMPONENT_REVIEW.md reassessment keeps R04 open: this delayed remount
reconstructs the body, while the reviewed gap also requires retained-reader
remount without body/source rebind. Source still clears its selected/latest input
on final detach and the reader currently only reconciles attachment.
Release progress is25/66; production findings11/12. Next define that R04 regression,
then P03 retirement and its unchanged Release retention budget, followed by
D10 presentation/R05-R06 remaining
discriminators. No backend, minimum-OS, physical ordering, extraction or final
independent review acceptance is claimed.

## EXP-171 — Restore retained hosts from the reader without body reconstruction

Status: PASS

### Question and release gate

Can a retained semantic container reattach through its existing reader callback,
after final detach or disconnect, without requiring SwiftUI to reevaluate its
body and resubscribe the transition source first? This experiment closes the
remaining R04 attachment discriminator. EXP-170 covered delayed remount after a
new root value; its result is preserved and does not answer this case.

### Control and decisive acceptance

Control is signed `2da21c041` (SDK `66d1ccb02`). Commit this definition first.
Reproduce with actual host/source/handler and no intervening call to reconcile
transitions between teardown and the retained reader attachment. Require:

- final detach/disconnect unsubscribes and releases suppression; commits while
  detached produce no view, stop or peer mutation;
- a valid retained reader mount republishes the latest accepted input exactly
  once, with a fresh identity and correct source scene, without body rebind;
- a reader arriving before scene reconnection remains rejected; stale inherited
  traits remain fenced, and duplicate callbacks create no extra occurrence;
- remembered configuration must not retain a source or SDK handler after their
  owner releases them; a new body with no source must not resurrect old authority;
- use a real mounted host and retained reader on iOS27, delayed removal/readdition
  of the same UIHostingController with no new root value, and markers submitted
  inside the first reader mount callback, before any later render can repair it.
  Record callback/readiness proof, exact fresh Resource/Log owners, peer continuity
  and required checks. If the callback cannot be intercepted before a framework
  body rebuild, preserve that attempt as inconclusive rather than weakening the
  boundary;
- run the affected host/reader/view-handler tests and strict changed-file lint.
  Keep EXP-168 ownership/teardown and EXP-170 stale-trait guards intact.

A weak remembered source, reobserved only after a live accepted attachment, is a
candidate implementation. Do not retain dormant subscriptions, acquire authority
from a trait, invent public API, or extend the extraction scope. P03 registry
retirement follows; R05 reentrant fan-out and D10/R06 presentation remain separate.
H08/H09 still require genuine OS/physical lifecycle ordering.

### Environment

Fresh Xcode workspace and device discovery finds iOS27/26.5 simulators. Paired
iPad and iPhone remain unavailable; the Watch is disconnected. Minimum iOS15 is
not installed. Local dummy credentials/loopback only; reauthenticate before any
future backend query. Release remains25/66 pending decisive R04 evidence.

### Implementation and evidence

Signed definition `dbe425193` preceded production changes. Both no-body controls
fail on unchanged SDK source, after final detach and scene reconnect. Signed SDK
`4ba7179c6` adds weak configured-source storage and reobserves it only when a live
reader is accepted. Teardown still removes the observer and suppression; a body
that withdraws the source clears the weak configuration. Weak source/handler
release tests pass. All318 affected tests and strict source/test lint pass.

The first native comparison passes: control43/57, candidate57/57. Both explicit
and capability hosts are removed for200ms and input changes while detached. The
same reader/controller and exact root value are retained. At its first mounted
callback the source has zero observers; after forwarding SDK delivery the fixture
submits Resource/Log work before a body rebind. Candidate markers belong to the
fresh Latest UUID/session; control markers go to the peer. Two unique owned
occurrences, unchanged peer, duplicate mount and final unsubscription assertions
pass. Frozen source/fixture/binary identity, clean install and new run IDs pass.
No invalid native attempt or SDK candidate retry was needed.

[EXP-171-retained-reader.json](../Results/EXP-171-retained-reader.json) records the
control failures, all318 selectors, test-result hashes and full native checks/
mapper owners with compact build identities. `tools/multi-scene/retained-reader`
repeats the native comparison. R04 closes its bounded component review; progress
is26/66, with production safety findings11/12. Next define P03 registry retirement
and preserve its frozen budget. D10/R06 presentation and R05 observer fan-out
follow. H08/H09, backend, minimum runtime, extraction and final independent review
remain separate.

## EXP-172 — Retire disconnected scene history without accepting stale callbacks

Status: PASS — P03 closed at signed `4653e0a72`

### Question and finite release gate

Can RUMViewsHandler retain only live scene state while still rejecting callbacks
from disconnected scenes and preserving connected peers? P03 is the sole gate.
EXP-160 independently measured220 entries in each disconnect/activity registry
after20 warm-up plus200 unique lifetimes. D03/D08 and bounded R04 are now closed;
this experiment repairs the remaining registry growth, not those accepted slices.

### Pinned control and required invariants

Control is signed `a76133570` (SDK `4ba7179c6`). Commit this definition before
production changes. First reproduce growing history with the actual handler and
real weak controller references; no archive or accepted experiment rerun is needed.

- Replace unbounded disconnected history with bounded connected-state bookkeeping
  only if all stale-callback controls remain decisive. A disconnect must retire its
  activity/stack/split metadata. A later background or view callback must not
  recreate that scene; only the accepted connection/foreground boundary may do so.
- Preserve views and scene activity that already existed when the SDK initialized,
  including an initially connected peer with no tracked view yet. Preserve early
  first-view compatibility before the first observed scene lifecycle boundary.
  A main-thread initial connected-scene inventory is a candidate; it must not
  retain UIKit objects or read UIKit hierarchy from a background initializer.
- Cover repeated unique IDs, reused IDs after genuine handler reconnection,
  stale UIKit/SwiftUI/manual/semantic callbacks, inactive-scene staging, app versus
  scene background/foreground, disconnected-no-stack and peer continuity. Retain
  D08/R04 trait and accepted-reader guards. Do not replace rejection with a timeout,
  a fixed tombstone capacity or arbitrary identifier eviction.
- Run affected handler/reader/semantic and instrumentation regressions, strict
  changed-file lint, and an ordinary automatic/manual compatibility smoke on the
  available runtimes because lifecycle admission changes shared view handling.
  Supported watchOS source guards must remain valid; no new public API/dependency.

### Frozen performance and lifetime acceptance

The BASELINES.md protocol prefix and thresholds stay unchanged: zero SDK-owned
scene/controller/host weak survivors and zero retired registry entries after
teardown/drain; after20 warm-up lifetimes, the first100 retain at most64KiB and the
second100 add at most16KiB. Inspect all scene-owning collections so a rename cannot
hide retained history. Live, externally owned native scene state is not silently
excluded: the isolated logical-cycle fixture must start with an explicit empty
scene inventory and tear down every scene it introduces. A candidate-only internal
constructor adapter may inject that empty inventory; the control already starts
empty. Record that adaptation, use identical workload/optimization and preserve
all source/fixture/binary hashes. No threshold may be increased after a failure.

Use fresh Release simulator processes on27 and26.5 in control/candidate/candidate/
control order, preserving every lifetime sample and invalid attempt. The original
20+100+100 retention workload is the decisive measure; this does not require a
new dispatch timing/allocation ABBA because event dispatch is unchanged. Keep
P01/P02/P04's accepted evidence pinned. Existing EXP-168/171 mounted lifetime
results remain prerequisites, with physical H08/H09 still separate.

### Implementation and unit controls

Signed `4653e0a72` replaces historical disconnect tombstones with live activity
membership, a one-time value-only connected-scene inventory on main, and an early
UIKit admission check before split metadata capture. The initializer schedules
its inventory weakly when called off-main. Connection/foreground admits a new
lifetime; disconnect removes membership; stale background does not reinsert it.
Initial and bootstrap peers retain their independent activity.

The unchanged control fails all three discriminators:220 activity/tombstone
entries, a disconnected scene without a stack, and stale UIKit split metadata.
The candidate passes326 affected tests including all three controls, initially
connected active/inactive peers, reused IDs, stale view-source rejection, and
background initializer lifetime. The first broad run passed325/326: a new test
incorrectly assumed `DispatchQueue.global().sync` must leave the main thread.
Its corrected asynchronous dispatch plus bounded semaphore proves the intended
queued-main boundary; no SDK workaround was needed. Source/test strict lint pass
with repository configurations after new-test argument formatting fixes. A first
sandboxed lint attempt could not create Xcode caches and is not acceptance.

The baseline protocol hash remains unchanged. The prescribed Release ABBA and automatic/manual compatibility smoke are
complete; results follow.

### Environment and next boundary

Workspaces, simulators and devices were rediscovered: iOS27/26.5 are available,
minimum15 is absent, paired iPad/iPhone remain unavailable and Watch disconnected.
Use local dummy credentials/loopback and isolated projects; do not read the
protected project/configuration or reuse old runtime artifacts. Reauthenticate
before any backend query. Release advances to27/66 after P03 acceptance. D10/R06
presentation and R05 observer fan-out follow; no telemetry family expansion here.

### Release acceptance and durable evidence

The first native attempt is valid and PASS. Every launch proves clean uninstall,
fresh run identity, exactly one native scene, and matching installed executable.
Frozen source/package/fixture identities remain unchanged. Both arms run the same
original20+100+100 workload; the documented candidate-only constructor argument
injects an empty initial scene inventory. All collections are inspected.

| Runtime | Arm/order | First100 heap delta | Second100 heap delta | Final retired entries | Weak controllers |
| --- | --- | --- | --- | --- | --- |
| 27.0 | control0 | 14,336 B | 15,936 B | 220 in each history registry | 0 |
| 27.0 | candidate1 | 512 B | 512 B | 0 | 0 |
| 27.0 | candidate2 | 512 B | 1,152 B | 0 | 0 |
| 27.0 | control3 | 14,336 B | 15,616 B | 220 in each history registry | 0 |
| 26.5 | control0 | 14,336 B | 15,616 B | 220 in each history registry | 0 |
| 26.5 | candidate1 | 832 B | 512 B | 0 | 0 |
| 26.5 | candidate2 | 832 B | 512 B | 0 | 0 |
| 26.5 | control3 | 14,336 B | 15,616 B | 220 in each history registry | 0 |

Every candidate boundary is empty, including after warm-up. The64KiB/16KiB
limits are unchanged. All eight control/candidate automatic/manual runs pass the
original Home→Detail→fresh Home occurrence/Action/Resource ownership oracle.
The full watchOS RUM Release product builds. Seven Python discriminator controls
reject missing/shortened evidence, an initially live scene, renamed retained
collections, weak survivors, and one byte beyond either heap threshold.

[Durable result](../Results/EXP-172-scene-retention.json) retains all16 launches,
raw heap/registry samples, frozen identities,326 selectors, five unit attempts
and artifact hashes. Native root: `scene-retention-zz9o16do`; full local summary:
`/tmp/exp172-scene-retention-attempt1.json`. Accepted mounted SwiftUI release from
EXP-168/171 remains a prerequisite, not a new physical-device claim. This closes
P03 only, moving release progress to27/66. D10/R06 presentation is next; R05
multi-observer delivery follows. No frozen archive or accepted experiment was
rerun to resume, and no backend or push was performed.

## EXP-173 — Commit accepted presentation state and fence occurrence callbacks

Status: PASS — D10/R06 closed at signed `7619eb8a2`;337 tests and native77/77

### Gates, control and admission

D10 is the final production-review repair; R06 owns the same-ID presentation
occurrence gap in the same native adapter. Control is signed `a0eed5c92`
(SDK `4653e0a72`). P03 and all previous accepted identities stay pinned. Commit
this definition before behavior changes; do not broaden telemetry families or
extract the modifier file.

The current native presentation Binding reconciles the proposed item before the
customer setter. Rejection can stop a still-accepted sheet; canonicalization can
track the wrong pending presentation. Existing mount/disappear callbacks match
only item ID, so an old same-ID sheet or earlier A can affect a later cover/A.
The presentation wrapper also discards the handler's acceptance result; test a
rejected disconnected mount before treating publication as active.

### Decisive controls and implementation constraints

- Exercise the actual production Binding factory with rejecting nil/item and
  canonicalizing setters, preserving the transaction. A behavior-neutral move
  of that factory into the internal state object is permitted to expose it to
  tests; the control order must remain unchanged until failures are recorded.
- The setter must receive the proposal exactly once. Reconcile its accepted
  getter synchronously after it returns. Work inside a rejecting setter must
  retain the accepted mounted owner, and work immediately after return or in
  actual onDismiss must see the accepted occurrence, before a later render.
  An opaque setter's internal mutation cannot be inferred before it returns:
  exact work at an interior accepted-state boundary requires the existing
  observed/explicit transition-source contract. Preserve that supported path;
  do not speculate from the proposal or claim a new opaque-setter guarantee.
- Carry a unique presentation occurrence identity through each materialized
  boundary. Cover same-ID sheet→cover, same-ID descriptor refresh, A→B→A,
  delayed/duplicate old mount/disappear, unmounted replacement, scene migration,
  rejection while disconnected, fresh accepted remount, and peer continuity.
  Old callbacks cannot stop/revive a later occurrence. Only accepted handler
  publication may mark a presentation started or acquire tracking authority.
- Dismissal is recorded/delivered only for accepted mounted occurrences, exactly
  once. Preserve underlying latest destination and its fresh reveal identity;
  no intermediate Home during mounted replacement. Retain custom/NOP and normal
  standard-container behavior. No new public API, dependency or wire fields.

### Required evidence and closure

Use targeted failing controls, the full affected SwiftUI/handler selection and
strict repository lint. Then run frozen control/candidate apps in an actual
mounted iOS27 SwiftUI host with real sheet/full-screen-cover materialization,
actual Binding delivery and native dismissal callbacks. Submit immediate
Resource/Log markers at the asserted boundaries; validate exact view/session IDs,
occurrence order/count and peer continuity. Assertions executed after a render
cannot substitute for boundary-time work. Keep every invalid/inconclusive attempt.

D10 closes on accepted-state/transaction/reject/canonicalization/native dismissal
proof. R06 closes separately only after occurrence and lifecycle discriminators
pass and its responsibility review is updated. If one passes, close only that gate.
Physical H08/H09/H13, F01 API sign-off and minimum runtime remain separate.
P03/dispatch measurements need no rerun unless a measured regression appears.

### Implementation and failing controls

The actual Binding factory was moved unchanged into the internal state object,
with the native adapter calling it; no order changed before the control run.
Factory SHA-256 at that boundary:
`faca924ac412595b8258f53ae4119f7a06af2b6b2d6308cd470555d92f5f7be0`.
All six controls fail: rejected nil stops accepted tracking and records dismissal;
canonicalized/rejected items leave the wrong descriptor; old same-ID sheet/A
callbacks stop Cover/later A; a rejected mount prevents reconnect retry.
The first compile attempt had redundant optional chaining in the factory move;
GetBuildLog identified it, and it was corrected before executing controls.

The candidate forwards the transaction once, then synchronously reads accepted
state. Each materialized boundary captures an occurrence UUID and uses it as its
SwiftUI identity; mount/disappear rejects old tokens. Presentation wrappers return
the handler's acceptance result, preserving an old owner on rejected migration
and acquiring suppression only after accepted publication. Rejected mounts clear
suppression. No public API changed and extraction remains deferred.

All336 affected tests and strict source/test lint pass, including ten new controls
for accepted return timing, canonicalized nil, stale style callbacks, same-ID
refresh, duplicate/nil-handler mounts, rejected migration and reconnect tokens.
Native mounted Binding, sheet/cover and Resource/Log evidence remains required
before D10 or R06 closes.

Native attempt1 (`presentation-acceptance-mdc08d8o`) is INVALID at build: the
observation-only adapter needed an explicit `Binding<Presentation?>` local type,
and its initializer hook captured mutating `self`. The adapter now captures the
already-created mount/disappear functions in local constants. The first SDK candidate
is unchanged at this boundary. Attempts2/3 below use fresh isolated snapshots and installation.

The native fixture observes the actual private Binding factory and materialized
boundary closures through identical Debug hooks in both copied SDK arms. Hooks
only retain/forward existing values; they do not change routing or accepted state.
Archive identity and post-hook compiled-source identity are recorded separately.
It mounts actual sheet/cover content, invokes the captured Binding directly for
rejection/canonicalization, preserves old actual boundary closures for late
callbacks, and uses SwiftUI's native dismiss action for final onDismiss checks.
These deterministic callback deliveries are not a physical OS-ordering claim.


### Native regression and accepted result

Attempt2 (`presentation-acceptance-imfrmf2n`) is a valid failure: control54/76,
initial signed candidate `7f13b2203`71/76. Rejection/canonicalization/native
onDismiss pass, but returning to same-ID SheetAgain creates extra Home and
presentation occurrences. Attempt3 (`presentation-acceptance-p9e754ko`) adds a
pre-injection stability assertion: control55/77, candidate71/77. The candidate
already changed UUID before delivery of the retained old callback. SwiftUI
rematerializes content for the same accepted presentation.

The added actual-state regression fails on `7f13b2203` (three commands rather
than one before accepted dismissal). Revised signed `7619eb8a2` reads the
accepted customer Binding in the current occurrence's disappearance callback.
An unchanged accepted item preserves its UUID; accepted nil still balances and
records dismissal once. All337 affected tests and strict source/test lint pass.
The strengthened final test also passes through accepted-nil disappearance itself.

Attempt4 (`presentation-acceptance-00zoyqpj`) is PASS: unchanged control55/77,
revised candidate77/77. Both clean installations retain frozen source/build
identities and fresh run IDs. Every scenario mounts actual SwiftUI presentation
content and uses its native dismiss action. Exact Resource/Log view/session
owners, one occurrence per presentation, two Home occurrences, one unchanged
logical peer, transactions and before-render boundaries all pass. The four
predeclared control ownership failures remain present.

Eight Python negative controls reject missing/unknown checks, missing setup,
late boundary assertions, wrong semantic results, truthy nonbooleans, readiness
failure and restored run IDs. After native execution the existing result classifier
was extracted for those tests: replay of attempts3/4 preserves their statuses.
Attempt2 remains a valid76-check failure under its original oracle and is incomplete
under the later77-check oracle. No accepted native run was repeated for this extraction.

[Durable result](../Results/EXP-173-presentation-acceptance.json) keeps all four
native attempts, raw events, archived/compiled source hashes, observation hooks,
unit failures and337 selectors. This closes D10 and, after inspection of accepted
state, occurrence identity, authority and teardown, bounded R06. Release progress
is29/66; all12 original safety findings have repair evidence. Physical ordering,
minimum runtime, backend telemetry-family acceptance and final API review remain
separate. R05 observer fan-out is next.


### Environment and next boundary

Use fresh isolated projects and clean installs, rediscovered simulator identity,
new run IDs and frozen source/fixture/executable hashes. Local dummy credentials
and mapper/loopback evidence suffice for this internal repair; reauthenticate
before any backend query. Release started27/66, with11/12 safety findings closed;
accepted evidence advances it to29/66 with12/12 safety findings closed.
After this slice, R05 multi-observer nested commit/remove/add is the next bounded
review gate. Continue autonomously under the current signed-when-available,
explicit-path, no-push policy.

## EXP-174 — Preserve monotonic reentrant observer delivery

Status: PASS — R05 closed at signed `368c62a72`;346 affected tests

### Gate, control and admission

R05 is the final bounded responsibility-review gate. Control is signed
`f99f39d2f` (SDK `7619eb8a2`); all12 original safety repairs and29 closed gates
remain pinned. Commit this definition before behavior changes. Inspect the actual
`RUMNavigationTransitions` source, observed adapter and host subscriptions. Both
initial publication and commit currently iterate copied observer values, allowing
a nested generation to reach a peer before an older outer callback resumes.
This experiment closes the named coverage gap, not a new telemetry family.

### Decisive controls

- With at least three observers, whichever receives the outer generation first
  commits a nested destination. Every remaining live observer must finish on
  the latest generation, never receive an older generation after a newer one,
  and receive the latest exactly once. Exercise both initial publication and
  ordinary commit; do not rely on dictionary iteration order or random retries.
- Remove observers during delivery: removed callbacks must not run later from
  an already-captured delivery collection. Add an observer during delivery: it
  gets current state once immediately, then subsequent accepted generations.
  Combine add/remove with nested commit and preserve synchronous return timing.
- Exercise the actual observed publisher/Observation adapter with multiple
  observers; rearming and FIFO behavior must remain correct. Cancellation and
  repeated initial state must not create extra generations.
- Exercise two actual semantic host states with scene-local handler commands
  and a peer. Nested delivery must not regress the emitted destination or revive
  a detached host. Exact source unsubscribe and weak owner release stay valid.

### Implementation and acceptance boundary

Prefer a single small internal publication path: enumerate registration IDs,
recheck membership before delivery and stop an obsolete outer generation after
nested publication. Do not defer to a queue or render: the latest accepted
snapshot must have reached every remaining observer when nested commit returns.
No new API, dependencies, availability changes or modifier extraction.

First record failing source controls at the frozen SDK. Then require all new
controls and the affected source/host/observed-adapter/SwiftUI/handler tests plus
strict source/test lint. The decisive environment is the real SDK XCTest target
on the freshly rediscovered iOS27 simulator. This in-memory synchronous delivery
contract does not require physical gestures or a new backend run. Record exact
selectors, source identity and every failed/invalid attempt. Review the source
responsibility after passing regressions before closing R05. No final API,
minimum-runtime or hardware gate is closed by this slice.

### Environment and next boundary

Xcode workspace and simulator identifiers were rediscovered. iOS27/26.5 are
available; minimum15 is absent. The paired iPad/iPhone remain unavailable; Watch
is now paired/available but cannot satisfy iPad/Duo scene gates. An initial
sandboxed discovery could not access Xcode caches/services; the authorized
service-access retry succeeded, with no SDK failure inferred. Datadog auth is
revalidated before any later backend query. T03 Resource start/completion
ownership is the next executable telemetry gate after R05.

### Results and review disposition

All three controls fail against unchanged SDK `7619eb8a2`: two observers receive
`[0, 2, 1]` during nested commit, two receive `[1, 0]` during initial publication,
and removed callbacks still deliver three times instead of once. The source file
hash is identical at the frozen control and definition checkpoints.

Signed `368c62a72` adds one internal publication helper shared by initial state
and commit. It enumerates IDs, rechecks membership and stops an obsolete outer
generation after synchronous nested publication. All seven focused source/adapter
tests pass. Two further actual-host regressions preserve distinct latest identities,
peer continuity and exact final detach; a removed host cannot revive. All346
affected tests pass. Source strict lint passes; five new test formatting violations
were corrected before a clean test lint pass. No test or runtime was rerun merely
to resume, and no new native fixture/backend run was necessary for this gate.

The responsibility review verifies current identity before delivery, Observation
rearming, weak host callbacks, source pinning and exact removal against these
regressions. R05 closes; all six bounded component reviews are complete. Release
progress is30/66. [Durable result](../Results/EXP-174-observer-delivery.json)
contains the failures,346 selectors, source hashes and environment. T03 Resource
ownership is next. Physical, minimum-runtime, API and independent release review
remain separate; no push was performed.

## EXP-175 — Keep Resource completion on its owning scope

Status: PASS

### Regression, finite gate and control

Investigate the specific pre-existing late-Resource failure identified by the
production review and already included in T03's decisive test. Control is signed
`bdc4dafa4` (SDK `368c62a72`). T03 remains one finite family gate: explicit start
plus captured start ownership. This internal repair is admitted as a concrete
regression investigation; it does not close the full T03 target/backend contract.
No new deliverable, public API or dependency is introduced.

Audit: manual Resource starts carry inferred execution context; completion and
metrics use the Resource key. URLSession captures its start target/key and retains
it through completion. Within a session, process-representative completion finds
an existing owner, but a session with no matching Resource falls back to its
current view; scene targets fan out to all matching scene branches. Application
scope sends the same completion through old retained and new active sessions.
The existing stopped-session test uses an instantaneous custom action, which
cannot expose a later completion incrementing a live continuous action's counts.

### Predeclared controls and acceptance

- Start a Resource on old A; stop the session, restart A/B and begin a continuous
  action in the new session. Complete old A successfully or with error while the
  new action is active. The Resource/error keeps the old view/session and appears
  once; the new action must have zero unrelated Resource/error counts and preserve
  its own stop attributes. Test inferred, exact-view and scene completion targets.
- Within one scene, navigate away from a Resource-owning inactive view while a
  later view has a live action. Scene-targeted metrics/completion must update only
  the Resource owner; opposite-scene explicit targets cannot retarget completion.
- Unknown and duplicate completions cannot change a current action's counters.
  Metrics retain their owner's timing and attributes. Keep clock-only expiration
  of unrelated actions; an ignored completion is not a new user interaction.
- Resource keys remain required to be unique among currently tracked resources.
  Do not invent new duplicate-start semantics or a key-generation API in this
  regression repair; record current limitations for the remaining T03 slice.

Record failing controls against unchanged SDK first. Prefer resolving the actual
Resource scope before per-view/session routing; completion identity outranks later
representative or scene selection. Preserve ordinary resource starts and session
creation/expiration policy. Run the affected Application/Session/View/Resource/
Action scopes, Monitor and URLSession instrumentation tests and strict lint.
Use actual SDK objects and serialized events in the iOS27 XCTest target. This
count/ownership regression needs no physical device; backend and explicit-start
acceptance remain required before T03 closes.

### Environment and continuation

Rediscover workspace and available simulator/device identities. iOS27/26.5 are
available, minimum15 and physical iPad/iPhone are unavailable. A fresh nonexistent
probe discriminator returns a successful empty Datadog aggregate, proving query
read access only. No customer payload, old session identifier or local xcconfig
is used. Release stays30/66 until a complete named gate closes. After this repair,
finish the existing T03 explicit-start/URLSession/backend deliverable; do not
expand to another telemetry family first.

### Attempts and accepted result

Definition is signed `69c53d408`; SDK repair is signed `aefe337b6`.
Four original controls fail against unchanged SDK: late success/error increments
new-session actions, scene completion reaches a later view, and an opposite-scene
target can prevent completion on the original owner. All four pass after resolving
metrics/completion solely through the existing Resource scope before target routing.
Resource starts retain their current policy; ordinary clock-only action expiry
still runs before propagation.

The extended focused run passes6/7: its only failure is a test-clock representation
of100,000,024 ns versus100,000,000 ns. Replacing the wall-clock origin with the fixed
reference-date origin preserves the exact100ms versus150ms discriminator. No SDK
change was made for this fixture issue. The final affected run passes379/379,
including seven new tests covering inferred/view/scene/peer targets, old sessions,
same-scene navigation, metrics, duplicates, unknown completions and clock expiry.
Resource/error events keep the exact old session/view and occur once; both new
continuous actions have zero foreign counts and their own stop attributes.
Source and test strict lint pass with zero violations. Repository usage audit
finds all Resource command implementations and routing in the existing internal
scope path; no bridge, format, generated or public API change is required.

[Durable evidence](../Results/EXP-175-resource-completion.json) retains all four
unit attempts, failure messages, summary/source hashes and379 exact selectors.
The full T03 gate remains OPEN: explicit Resource start targets and URLSession/
mapper/backend ownership are the next acceptance slice. Release accounting remains
30/66. No physical, backend or duplicate-start contract claim is made here.

## EXP-176 — Accept explicit Resource starts and captured completion owners

Status: PLANNED

### Gate, control and implementation boundary

Close existing T03, whose completion mode is explicit start target plus captured
start ownership. Control is signed `568127251` (SDK `aefe337b6`); EXP-175's
late-completion repair and379-test result are retained. This adds no release gate.
Define the experiment before implementation and freeze the resulting candidate
before native/backend acceptance. Owner: SDK implementer. Environment: iOS27
simulator with two serially created native scenes, real URLSession instrumentation,
focused SDK/compatibility/Objective-C tests, Release compile, mapper and Datadog.
No simultaneous visibility, hardware ordering or stable API sign-off is claimed.

Add experimental iOS27 `startResource(..., view:)` overloads for URLRequest, URL
and method/string, using the existing RUMViewTarget and a private capability
bridge. Debug-only Objective-C peers follow the existing target prototypes.
Keep all existing public protocol requirements unchanged; custom/NOP monitors
receive their legacy method exactly once. Stable Release API naming/Objective-C
exposure remains F01. No new dependency, swizzle, endpoint, wire format, generated
model, API baseline, build-script or protected-file change is authorized here.

Carry explicit and inferred start candidates independently. Resolve an explicit
scene only when it has a live current RUM view at command processing; otherwise
keep existing inference and process-representative fallback. Resource start is
not user interaction and must not change the representative. Once a Resource
scope exists, its key owns all metrics/success/error completion across navigation,
scene teardown and session stop. Completion APIs gain no target argument.
Keys retain their documented requirement to be unique among currently tracked
Resources. Reuse is valid after completion; conflicting concurrent starts remain
outside the supported contract. No new per-scene key namespace is introduced.
Automatic URLSession capture should need no behavior change; change it only if
this experiment reproduces a concrete failed ownership discriminator.

### Predeclared SDK and compatibility oracle

- All three start forms target A while B represents and while inference points
  to B. They preserve URL/method/type/attributes and do not change representative B.
- Missing/closed explicit scenes preserve valid inferred A; without inference,
  use representative B. Resolve the current view at processing rather than retain
  a scene/controller/view object. Session expiration/restoration retains the
  existing eligibility policy. Starts do not reopen an explicitly stopped session.
- Success, Error/message failure and metrics remain on the original scope after
  same-scene navigation, disconnect and stopSession/new-session activity. Repeated
  or unknown completion changes neither counts nor ownership. Retain EXP-175's
  live continuous-action discriminator in the combined fixture.
- Capture URLSession ownership at task creation and complete it after a peer
  takes over/navigation. Test success and error, no duplicate starts, preserved
  metrics/attributes and no new-owner counter effects. Manual/automatic keys do
  not collide; reuse a completed manual key once to prove the old owner is gone.
- Extension/bridge, custom/NOP and Objective-C forwarding execute once. Existing
  inferred/manual APIs and all affected scope/Monitor/URLSession checks pass;
  source/test lint and Debug/Release compilation pass. Keep iOS15 deployment.

Introduce the private capability and explicit command field without target
routing first so focused new controls can fail on equivalent unchanged behavior.
Record that mechanical control adaptation separately from the routing repair.

### Native and backend oracle

Extend the existing acceptance harness with one named Resource scenario using
serial native A/B scene creation and an explicitly frozen scenario contract.
Use paired start/completion commands, deterministic request release barriers,
and distinct source labels independent of mapper-observed owner IDs. Keep A's
manual and automatic Resources pending while B represents; navigate or remove
A's owning view before success/error release. Require exact original view/session
IDs, one event per start, preserved phase/metrics/attributes and zero foreign
Resource/error counts on a live peer/new-session action. Include unchanged legacy
source-A start while B represents as the compatibility control.

Bind every expected Resource/error/action to the mapper occurrence established
before its start; a name-only or final-count-only pass is insufficient. Verify
complete session-only view and telemetry inventories in Datadog, with separate
expected network errors and zero crash count. A backend run-ID filter cannot hide
restored identifiers or an extra owner. Local passing assertions must occur before
their declared critical boundary. Do not infer accepted asynchronous ownership
from a marker emitted later on the settled screen.

Reuse preflight, unique run IDs, frozen source/build identity, clean uninstall/
absent-container/install checks and nonce-bound paginated backend collection.
Extend existing negative controls for swapped Resource owners, stale metrics,
missing/duplicate completion, leaked peer-action counts, restored IDs and early
request release. Preserve every INVALID/FAIL/INCONCLUSIVE attempt. Produce a
compact durable summary, keeping raw logs/xcresult outside the repository.

### Decision

T03 closes only when SDK/compatibility and the complete mapper/backend oracle
pass on one frozen candidate. If native/backend is unavailable, commit verified
work and leave T03 OPEN with the exact missing evidence; do not redefine the gate.
After T03, take T04 current-view errors as the next finite telemetry family.

## Record template

Use this template for subsequent experiments:

```markdown
## EXP-NNN — Short title

Status: PASS | FAIL | INCONCLUSIVE | INVALID | SKIPPED | PLANNED | PREPARED

### Question

### Preconditions and implementation revision

### Attempts

### Local and mapper evidence

### Backend evidence

### Validation and artifacts

### Conclusion and next boundary
```
