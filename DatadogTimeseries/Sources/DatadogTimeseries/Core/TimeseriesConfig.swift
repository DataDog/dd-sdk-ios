import Foundation

struct TimeseriesConfig {
    let applicationId: String
    let sessionId: String
    let sessionType: String
    let source: String
    let service: String?
    let version: String?
}
