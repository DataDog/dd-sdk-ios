{
    "title": "LogsConfig",
    "description": "Schema with const-based union",
    "type": "object",
    "properties": {
        "forwardConsoleLogs": {
            "description": "Console methods",
            "oneOf": [
                { "type": "string", "const": "all" },
                {
                    "type": "array",
                    "items": {
                        "type": "string",
                        "enum": ["log", "debug", "info", "warn", "error"]
                    }
                }
            ]
        }
    }
}
